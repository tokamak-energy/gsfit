//! Static description of the pulse_schedule data dictionary, used to build and resolve paths.
//!
//! **WARNING** This file is autogenerated by `rust/imas_rs/imas_updater/build_ids.py`.
//! Any changes will be overwritten.
//!
//! Each leaf carries the function that reads it out of the IDS, so the path a
//! reader follows is written once and checked by the compiler. The `read` closures
//! capture nothing, so they coerce to plain `fn` pointers and the whole description
//! stays a `static`.

#![allow(clippy::all)]

use super::{IndexSpec, Leaf, Node, NodeKind, gather, no_levels};
use crate::dd_base_types::{FLT_0D, FLT_1D, INT_0D, INT_1D, STR_0D, STR_1D};
use crate::ids::pulse_schedule::PulseSchedule;
use std::any::Any;

// ============================================================================
// Array-of-structures lengths
// ============================================================================

/// Length of each array-of-structures level along `code_library`.
fn lengths_code_library(pulse_schedule: &PulseSchedule, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.code.library.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `density_control_ion`.
fn lengths_density_control_ion(pulse_schedule: &PulseSchedule, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.density_control.ion.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `density_control_ion_element`.
fn lengths_density_control_ion_element(pulse_schedule: &PulseSchedule, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.density_control.ion.len()),
        1 => return Some(pulse_schedule.density_control.ion[at[0]].element.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `density_control_valve`.
fn lengths_density_control_valve(pulse_schedule: &PulseSchedule, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.density_control.valve.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `density_control_valve_species`.
fn lengths_density_control_valve_species(pulse_schedule: &PulseSchedule, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.density_control.valve.len()),
        1 => return Some(pulse_schedule.density_control.valve[at[0]].species.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `density_control_valve_species_element`.
fn lengths_density_control_valve_species_element(pulse_schedule: &PulseSchedule, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.density_control.valve.len()),
        1 => return Some(pulse_schedule.density_control.valve[at[0]].species.len()),
        2 => return Some(pulse_schedule.density_control.valve[at[0]].species[at[1]].element.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `ec_beam`.
fn lengths_ec_beam(pulse_schedule: &PulseSchedule, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.ec.beam.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `event`.
fn lengths_event(pulse_schedule: &PulseSchedule, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.event.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `ic_antenna`.
fn lengths_ic_antenna(pulse_schedule: &PulseSchedule, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.ic.antenna.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `lh_antenna`.
fn lengths_lh_antenna(pulse_schedule: &PulseSchedule, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.lh.antenna.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `nbi_unit`.
fn lengths_nbi_unit(pulse_schedule: &PulseSchedule, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.nbi.unit.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `nbi_unit_species`.
fn lengths_nbi_unit_species(pulse_schedule: &PulseSchedule, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.nbi.unit.len()),
        1 => return Some(pulse_schedule.nbi.unit[at[0]].species.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `nbi_unit_species_element`.
fn lengths_nbi_unit_species_element(pulse_schedule: &PulseSchedule, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.nbi.unit.len()),
        1 => return Some(pulse_schedule.nbi.unit[at[0]].species.len()),
        2 => return Some(pulse_schedule.nbi.unit[at[0]].species[at[1]].element.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `pf_active_coil`.
fn lengths_pf_active_coil(pulse_schedule: &PulseSchedule, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.pf_active.coil.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `pf_active_supply`.
fn lengths_pf_active_supply(pulse_schedule: &PulseSchedule, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.pf_active.supply.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `position_control_boundary_outline`.
fn lengths_position_control_boundary_outline(pulse_schedule: &PulseSchedule, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.position_control.boundary_outline.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `position_control_gap`.
fn lengths_position_control_gap(pulse_schedule: &PulseSchedule, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.position_control.gap.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `position_control_strike_point`.
fn lengths_position_control_strike_point(pulse_schedule: &PulseSchedule, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.position_control.strike_point.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `position_control_x_point`.
fn lengths_position_control_x_point(pulse_schedule: &PulseSchedule, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(pulse_schedule.position_control.x_point.len()),
        _ => return None,
    }
}

// ============================================================================
// Nodes
// ============================================================================

static NODES_IC_ANTENNA_POWER_TYPE: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ic_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.ic.antenna[at[0]].power_type.name.clone() },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ic_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.ic.antenna[at[0]].power_type.index.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ic_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.ic.antenna[at[0]].power_type.description.clone() },
                )
            },
        }),
    },
];

static NODES_IC_ANTENNA_POWER: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ic_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.ic.antenna[at[0]].power.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ic_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.ic.antenna[at[0]].power.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ic_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.ic.antenna[at[0]].power.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ic_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.ic.antenna[at[0]].power.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_IC_ANTENNA_PHASE: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ic_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.ic.antenna[at[0]].phase.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ic_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.ic.antenna[at[0]].phase.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ic_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.ic.antenna[at[0]].phase.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ic_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.ic.antenna[at[0]].phase.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_IC_ANTENNA_FREQUENCY: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ic_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.ic.antenna[at[0]].frequency.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ic_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.ic.antenna[at[0]].frequency.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ic_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.ic.antenna[at[0]].frequency.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ic_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.ic.antenna[at[0]].frequency.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_IC_ANTENNA: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier (unique for a given device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ic_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.ic.antenna[at[0]].name.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Description, e.g. “channel viewing the upper divertor”",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ic_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.ic.antenna[at[0]].description.clone() },
                )
            },
        }),
    },
    Node {
        name: "power_type",
        documentation: "Type of power used in the sibling power node (defining which power is referred to in this pulse_schedule). Index = 1: power_launched, 2: power_forward (see definitions in the ic_antennas  IDS)",
        units: "",
        kind: NodeKind::Structure(NODES_IC_ANTENNA_POWER_TYPE),
    },
    Node {
        name: "power",
        documentation: "Power",
        units: "W",
        kind: NodeKind::Structure(NODES_IC_ANTENNA_POWER),
    },
    Node {
        name: "phase",
        documentation: "Phase",
        units: "rad",
        kind: NodeKind::Structure(NODES_IC_ANTENNA_PHASE),
    },
    Node {
        name: "frequency",
        documentation: "Frequency",
        units: "Hz",
        kind: NodeKind::Structure(NODES_IC_ANTENNA_FREQUENCY),
    },
];

static NODES_IC_POWER: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.ic.power.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.ic.power.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.ic.power.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.ic.power.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_IC: &[Node] = &[
    Node {
        name: "antenna",
        documentation: "Set of ICRH antennas",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_IC_ANTENNA),
    },
    Node {
        name: "power",
        documentation: "Total IC power (sum over the antennas)",
        units: "W",
        kind: NodeKind::Structure(NODES_IC_POWER),
    },
    Node {
        name: "mode",
        documentation: "Control mode (operation mode and/or settings used by the controller)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_1D { pulse_schedule.ic.mode.clone() },
                )
            },
        }),
    },
    Node {
        name: "time",
        documentation: "Timebase for the dynamic nodes located at this level of the IDS structure and below",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.ic.time.clone() },
                )
            },
        }),
    },
];

static NODES_EC_BEAM_POWER_LAUNCHED: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.ec.beam[at[0]].power_launched.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.ec.beam[at[0]].power_launched.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.ec.beam[at[0]].power_launched.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.ec.beam[at[0]].power_launched.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_EC_BEAM_FREQUENCY: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.ec.beam[at[0]].frequency.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.ec.beam[at[0]].frequency.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.ec.beam[at[0]].frequency.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.ec.beam[at[0]].frequency.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_EC_BEAM_DEPOSITION_RHO_TOR_NORM: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.ec.beam[at[0]].deposition_rho_tor_norm.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.ec.beam[at[0]].deposition_rho_tor_norm.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.ec.beam[at[0]].deposition_rho_tor_norm.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.ec.beam[at[0]].deposition_rho_tor_norm.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_EC_BEAM_STEERING_ANGLE_POL: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.ec.beam[at[0]].steering_angle_pol.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.ec.beam[at[0]].steering_angle_pol.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.ec.beam[at[0]].steering_angle_pol.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.ec.beam[at[0]].steering_angle_pol.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_EC_BEAM_STEERING_ANGLE_TOR: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.ec.beam[at[0]].steering_angle_tor.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.ec.beam[at[0]].steering_angle_tor.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.ec.beam[at[0]].steering_angle_tor.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.ec.beam[at[0]].steering_angle_tor.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_EC_BEAM: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier (unique for a given device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.ec.beam[at[0]].name.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Description, e.g. “channel viewing the upper divertor”",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_ec_beam,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.ec.beam[at[0]].description.clone() },
                )
            },
        }),
    },
    Node {
        name: "power_launched",
        documentation: "Beam power launched into the vacuum vessel",
        units: "W",
        kind: NodeKind::Structure(NODES_EC_BEAM_POWER_LAUNCHED),
    },
    Node {
        name: "frequency",
        documentation: "Frequency",
        units: "Hz",
        kind: NodeKind::Structure(NODES_EC_BEAM_FREQUENCY),
    },
    Node {
        name: "deposition_rho_tor_norm",
        documentation: "Normalized toroidal flux coordinate at which the main deposition should occur",
        units: "1",
        kind: NodeKind::Structure(NODES_EC_BEAM_DEPOSITION_RHO_TOR_NORM),
    },
    Node {
        name: "steering_angle_pol",
        documentation: "Steering angle of the EC beam in the R,Z plane (from the -R axis towards the -Z axis), angle_pol=atan2(-k_Z,-k_R), where k_Z and k_R are the Z and R components of the mean wave vector in the EC beam",
        units: "rad",
        kind: NodeKind::Structure(NODES_EC_BEAM_STEERING_ANGLE_POL),
    },
    Node {
        name: "steering_angle_tor",
        documentation: "Steering angle of the EC beam away from the poloidal plane that is increasing towards the positive phi axis, angle_tor=arcsin(k_phi/k), where k_phi is the component of the wave vector in the phi direction and k is the length of the wave vector. Here the term wave vector refers to the mean wave vector in the EC beam",
        units: "rad",
        kind: NodeKind::Structure(NODES_EC_BEAM_STEERING_ANGLE_TOR),
    },
];

static NODES_EC_POWER_LAUNCHED: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.ec.power_launched.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.ec.power_launched.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.ec.power_launched.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.ec.power_launched.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_EC: &[Node] = &[
    Node {
        name: "beam",
        documentation: "Set of Electron Cyclotron beams",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_EC_BEAM),
    },
    Node {
        name: "power_launched",
        documentation: "Total EC power launched in the plasma (sum over the beams)",
        units: "W",
        kind: NodeKind::Structure(NODES_EC_POWER_LAUNCHED),
    },
    Node {
        name: "mode",
        documentation: "Control mode (operation mode and/or settings used by the controller)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_1D { pulse_schedule.ec.mode.clone() },
                )
            },
        }),
    },
    Node {
        name: "time",
        documentation: "Timebase for the dynamic nodes located at this level of the IDS structure and below",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.ec.time.clone() },
                )
            },
        }),
    },
];

static NODES_LH_ANTENNA_POWER_TYPE: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.lh.antenna[at[0]].power_type.name.clone() },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.lh.antenna[at[0]].power_type.index.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.lh.antenna[at[0]].power_type.description.clone() },
                )
            },
        }),
    },
];

static NODES_LH_ANTENNA_POWER: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.lh.antenna[at[0]].power.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.lh.antenna[at[0]].power.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.lh.antenna[at[0]].power.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.lh.antenna[at[0]].power.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_LH_ANTENNA_PHASE: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.lh.antenna[at[0]].phase.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.lh.antenna[at[0]].phase.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.lh.antenna[at[0]].phase.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.lh.antenna[at[0]].phase.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_LH_ANTENNA_N_PARALLEL: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.lh.antenna[at[0]].n_parallel.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.lh.antenna[at[0]].n_parallel.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.lh.antenna[at[0]].n_parallel.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.lh.antenna[at[0]].n_parallel.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_LH_ANTENNA_FREQUENCY: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.lh.antenna[at[0]].frequency.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.lh.antenna[at[0]].frequency.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.lh.antenna[at[0]].frequency.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.lh.antenna[at[0]].frequency.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_LH_ANTENNA: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier (unique for a given device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.lh.antenna[at[0]].name.clone() },
                )
            },
        }),
    },
    Node {
        name: "power_type",
        documentation: "Type of power used in the sibling power node (defining which power is referred to in this pulse_schedule). Index = 1: power_launched, 2: power_forward (see definitions in the lh_antennas  IDS)",
        units: "",
        kind: NodeKind::Structure(NODES_LH_ANTENNA_POWER_TYPE),
    },
    Node {
        name: "power",
        documentation: "Power",
        units: "W",
        kind: NodeKind::Structure(NODES_LH_ANTENNA_POWER),
    },
    Node {
        name: "phase",
        documentation: "Phasing between neighbour waveguides (in the toroidal direction)",
        units: "rad",
        kind: NodeKind::Structure(NODES_LH_ANTENNA_PHASE),
    },
    Node {
        name: "n_parallel",
        documentation: "Main parallel refractive index of the injected wave power spectrum",
        units: "1",
        kind: NodeKind::Structure(NODES_LH_ANTENNA_N_PARALLEL),
    },
    Node {
        name: "frequency",
        documentation: "Frequency",
        units: "Hz",
        kind: NodeKind::Structure(NODES_LH_ANTENNA_FREQUENCY),
    },
    Node {
        name: "description",
        documentation: "Description, e.g. “channel viewing the upper divertor”",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_lh_antenna,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.lh.antenna[at[0]].description.clone() },
                )
            },
        }),
    },
];

static NODES_LH_POWER: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.lh.power.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.lh.power.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.lh.power.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.lh.power.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_LH: &[Node] = &[
    Node {
        name: "antenna",
        documentation: "Set of LH antennas",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_LH_ANTENNA),
    },
    Node {
        name: "power",
        documentation: "Total LH power (sum over the antennas)",
        units: "W",
        kind: NodeKind::Structure(NODES_LH_POWER),
    },
    Node {
        name: "mode",
        documentation: "Control mode (operation mode and/or settings used by the controller)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_1D { pulse_schedule.lh.mode.clone() },
                )
            },
        }),
    },
    Node {
        name: "time",
        documentation: "Timebase for the dynamic nodes located at this level of the IDS structure and below",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.lh.time.clone() },
                )
            },
        }),
    },
];

static NODES_NBI_UNIT_SPECIES_ELEMENT: &[Node] = &[
    Node {
        name: "a",
        documentation: "Mass of atom",
        units: "u",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    3,
                    lengths_nbi_unit_species_element,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_0D { pulse_schedule.nbi.unit[at[0]].species[at[1]].element[at[2]].a.clone() },
                )
            },
        }),
    },
    Node {
        name: "z_n",
        documentation: "Nuclear charge",
        units: "e",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    3,
                    lengths_nbi_unit_species_element,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.nbi.unit[at[0]].species[at[1]].element[at[2]].z_n.clone() },
                )
            },
        }),
    },
    Node {
        name: "atoms_n",
        documentation: "Number of atoms of this element in the molecule",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    3,
                    lengths_nbi_unit_species_element,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.nbi.unit[at[0]].species[at[1]].element[at[2]].atoms_n.clone() },
                )
            },
        }),
    },
];

static NODES_NBI_UNIT_SPECIES: &[Node] = &[
    Node {
        name: "element",
        documentation: "List of elements forming the atom or molecule",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_NBI_UNIT_SPECIES_ELEMENT),
    },
    Node {
        name: "name",
        documentation: "String identifying neutral (e.g. H, D, T, He, C, ...)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    2,
                    lengths_nbi_unit_species,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.nbi.unit[at[0]].species[at[1]].name.clone() },
                )
            },
        }),
    },
    Node {
        name: "fraction",
        documentation: "Relative fraction of this species (in molecules) in the gas mixture",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    2,
                    lengths_nbi_unit_species,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_0D { pulse_schedule.nbi.unit[at[0]].species[at[1]].fraction.clone() },
                )
            },
        }),
    },
];

static NODES_NBI_UNIT_POWER: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_nbi_unit,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.nbi.unit[at[0]].power.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_nbi_unit,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.nbi.unit[at[0]].power.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_nbi_unit,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.nbi.unit[at[0]].power.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_nbi_unit,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.nbi.unit[at[0]].power.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_NBI_UNIT_ENERGY: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_nbi_unit,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.nbi.unit[at[0]].energy.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_nbi_unit,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.nbi.unit[at[0]].energy.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_nbi_unit,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.nbi.unit[at[0]].energy.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_nbi_unit,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.nbi.unit[at[0]].energy.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_NBI_UNIT: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier (unique for a given device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_nbi_unit,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.nbi.unit[at[0]].name.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Description, e.g. “channel viewing the upper divertor”",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_nbi_unit,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.nbi.unit[at[0]].description.clone() },
                )
            },
        }),
    },
    Node {
        name: "species",
        documentation: "Species injected by the NBI unit (may be more than one in case the unit injects a gas mixture)",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_NBI_UNIT_SPECIES),
    },
    Node {
        name: "power",
        documentation: "Power launched from this unit into the vacuum vessel",
        units: "W",
        kind: NodeKind::Structure(NODES_NBI_UNIT_POWER),
    },
    Node {
        name: "energy",
        documentation: "Full energy of the injected species (acceleration of a single atom)",
        units: "eV",
        kind: NodeKind::Structure(NODES_NBI_UNIT_ENERGY),
    },
];

static NODES_NBI_POWER: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.nbi.power.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.nbi.power.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.nbi.power.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.nbi.power.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_NBI: &[Node] = &[
    Node {
        name: "unit",
        documentation: "Set of NBI units",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_NBI_UNIT),
    },
    Node {
        name: "power",
        documentation: "Total NBI power (sum over the units)",
        units: "W",
        kind: NodeKind::Structure(NODES_NBI_POWER),
    },
    Node {
        name: "mode",
        documentation: "Control mode (operation mode and/or settings used by the controller)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_1D { pulse_schedule.nbi.mode.clone() },
                )
            },
        }),
    },
    Node {
        name: "time",
        documentation: "Timebase for the dynamic nodes located at this level of the IDS structure and below",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.nbi.time.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_VALVE_FLOW_RATE: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_density_control_valve,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.density_control.valve[at[0]].flow_rate.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_density_control_valve,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.density_control.valve[at[0]].flow_rate.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_density_control_valve,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.density_control.valve[at[0]].flow_rate.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_density_control_valve,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.density_control.valve[at[0]].flow_rate.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_VALVE_SPECIES_ELEMENT: &[Node] = &[
    Node {
        name: "a",
        documentation: "Mass of atom",
        units: "u",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    3,
                    lengths_density_control_valve_species_element,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_0D {
                        pulse_schedule.density_control.valve[at[0]].species[at[1]].element[at[2]].a.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "z_n",
        documentation: "Nuclear charge",
        units: "e",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    3,
                    lengths_density_control_valve_species_element,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D {
                        pulse_schedule.density_control.valve[at[0]].species[at[1]].element[at[2]].z_n.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "atoms_n",
        documentation: "Number of atoms of this element in the molecule",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    3,
                    lengths_density_control_valve_species_element,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D {
                        pulse_schedule.density_control.valve[at[0]].species[at[1]].element[at[2]].atoms_n.clone()
                    },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_VALVE_SPECIES: &[Node] = &[
    Node {
        name: "element",
        documentation: "List of elements forming the atom or molecule",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_DENSITY_CONTROL_VALVE_SPECIES_ELEMENT),
    },
    Node {
        name: "name",
        documentation: "String identifying neutral (e.g. H, D, T, He, C, ...)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    2,
                    lengths_density_control_valve_species,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.density_control.valve[at[0]].species[at[1]].name.clone() },
                )
            },
        }),
    },
    Node {
        name: "fraction",
        documentation: "Relative fraction of this species (in molecules) in the gas mixture",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    2,
                    lengths_density_control_valve_species,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_0D { pulse_schedule.density_control.valve[at[0]].species[at[1]].fraction.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_VALVE: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier (unique for a given device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_density_control_valve,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.density_control.valve[at[0]].name.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Description, e.g. “channel viewing the upper divertor”",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_density_control_valve,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.density_control.valve[at[0]].description.clone() },
                )
            },
        }),
    },
    Node {
        name: "flow_rate",
        documentation: "Flow rate of the valve",
        units: "Pa.m^3.s^-1",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_VALVE_FLOW_RATE),
    },
    Node {
        name: "species",
        documentation: "Species injected by the valve (may be more than one in case the valve injects a gas mixture)",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_DENSITY_CONTROL_VALVE_SPECIES),
    },
];

static NODES_DENSITY_CONTROL_N_E_LINE: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.density_control.n_e_line.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.density_control.n_e_line.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.density_control.n_e_line.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.density_control.n_e_line.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_N_E_LINE_LCFS: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.density_control.n_e_line_lcfs.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.density_control.n_e_line_lcfs.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.density_control.n_e_line_lcfs.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.density_control.n_e_line_lcfs.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_N_E_PROFILE_AVERAGE: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.density_control.n_e_profile_average.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.density_control.n_e_profile_average.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.density_control.n_e_profile_average.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.density_control.n_e_profile_average.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_N_E_LINE_OF_SIGHT_FIRST_POINT: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.n_e_line_of_sight.first_point.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "phi",
        documentation: "Toroidal angle (oriented counter-clockwise when viewed from above)",
        units: "rad",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.n_e_line_of_sight.first_point.phi.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.n_e_line_of_sight.first_point.z.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_N_E_LINE_OF_SIGHT_SECOND_POINT: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.n_e_line_of_sight.second_point.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "phi",
        documentation: "Toroidal angle (oriented counter-clockwise when viewed from above)",
        units: "rad",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.n_e_line_of_sight.second_point.phi.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.n_e_line_of_sight.second_point.z.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_N_E_LINE_OF_SIGHT_THIRD_POINT: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.n_e_line_of_sight.third_point.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "phi",
        documentation: "Toroidal angle (oriented counter-clockwise when viewed from above)",
        units: "rad",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.n_e_line_of_sight.third_point.phi.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.n_e_line_of_sight.third_point.z.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_N_E_LINE_OF_SIGHT: &[Node] = &[
    Node {
        name: "first_point",
        documentation: "Position of the first point",
        units: "",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_N_E_LINE_OF_SIGHT_FIRST_POINT),
    },
    Node {
        name: "second_point",
        documentation: "Position of the second point",
        units: "",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_N_E_LINE_OF_SIGHT_SECOND_POINT),
    },
    Node {
        name: "third_point",
        documentation: "Position of the third point",
        units: "",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_N_E_LINE_OF_SIGHT_THIRD_POINT),
    },
];

static NODES_DENSITY_CONTROL_N_E_VOLUME_AVERAGE: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.density_control.n_e_volume_average.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.density_control.n_e_volume_average.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.density_control.n_e_volume_average.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.density_control.n_e_volume_average.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_ZEFF: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.density_control.zeff.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.density_control.zeff.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.density_control.zeff.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.density_control.zeff.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_ZEFF_METHOD: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.density_control.zeff_method.name.clone() },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.density_control.zeff_method.index.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.density_control.zeff_method.description.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_ZEFF_LINE_OF_SIGHT_FIRST_POINT: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.zeff_line_of_sight.first_point.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "phi",
        documentation: "Toroidal angle (oriented counter-clockwise when viewed from above)",
        units: "rad",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.zeff_line_of_sight.first_point.phi.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.zeff_line_of_sight.first_point.z.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_ZEFF_LINE_OF_SIGHT_SECOND_POINT: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.zeff_line_of_sight.second_point.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "phi",
        documentation: "Toroidal angle (oriented counter-clockwise when viewed from above)",
        units: "rad",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.zeff_line_of_sight.second_point.phi.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.zeff_line_of_sight.second_point.z.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_ZEFF_LINE_OF_SIGHT_THIRD_POINT: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.zeff_line_of_sight.third_point.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "phi",
        documentation: "Toroidal angle (oriented counter-clockwise when viewed from above)",
        units: "rad",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.zeff_line_of_sight.third_point.phi.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_0D { pulse_schedule.density_control.zeff_line_of_sight.third_point.z.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_ZEFF_LINE_OF_SIGHT: &[Node] = &[
    Node {
        name: "first_point",
        documentation: "Position of the first point",
        units: "",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_ZEFF_LINE_OF_SIGHT_FIRST_POINT),
    },
    Node {
        name: "second_point",
        documentation: "Position of the second point",
        units: "",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_ZEFF_LINE_OF_SIGHT_SECOND_POINT),
    },
    Node {
        name: "third_point",
        documentation: "Position of the third point",
        units: "",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_ZEFF_LINE_OF_SIGHT_THIRD_POINT),
    },
];

static NODES_DENSITY_CONTROL_N_T_OVER_N_D: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.density_control.n_t_over_n_d.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.density_control.n_t_over_n_d.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.density_control.n_t_over_n_d.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.density_control.n_t_over_n_d.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_N_H_OVER_N_D: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.density_control.n_h_over_n_d.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.density_control.n_h_over_n_d.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.density_control.n_h_over_n_d.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.density_control.n_h_over_n_d.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_ION_ELEMENT: &[Node] = &[
    Node {
        name: "a",
        documentation: "Mass of atom",
        units: "u",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    2,
                    lengths_density_control_ion_element,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_0D { pulse_schedule.density_control.ion[at[0]].element[at[1]].a.clone() },
                )
            },
        }),
    },
    Node {
        name: "z_n",
        documentation: "Nuclear charge",
        units: "e",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    2,
                    lengths_density_control_ion_element,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.density_control.ion[at[0]].element[at[1]].z_n.clone() },
                )
            },
        }),
    },
    Node {
        name: "atoms_n",
        documentation: "Number of atoms of this element in the molecule",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    2,
                    lengths_density_control_ion_element,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.density_control.ion[at[0]].element[at[1]].atoms_n.clone() },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_ION_N_I_VOLUME_AVERAGE: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_density_control_ion,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D {
                        pulse_schedule.density_control.ion[at[0]].n_i_volume_average.reference_name.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_density_control_ion,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.density_control.ion[at[0]].n_i_volume_average.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_density_control_ion,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D {
                        pulse_schedule.density_control.ion[at[0]].n_i_volume_average.reference_type.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_density_control_ion,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D {
                        pulse_schedule.density_control.ion[at[0]].n_i_volume_average.envelope_type.clone()
                    },
                )
            },
        }),
    },
];

static NODES_DENSITY_CONTROL_ION: &[Node] = &[
    Node {
        name: "element",
        documentation: "List of elements forming the atom or molecule",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_DENSITY_CONTROL_ION_ELEMENT),
    },
    Node {
        name: "z_ion",
        documentation: "Ion charge",
        units: "e",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_density_control_ion,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_0D { pulse_schedule.density_control.ion[at[0]].z_ion.clone() },
                )
            },
        }),
    },
    Node {
        name: "name",
        documentation: "String identifying ion (e.g. H, D, T, He, C, D2, ...)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_density_control_ion,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.density_control.ion[at[0]].name.clone() },
                )
            },
        }),
    },
    Node {
        name: "n_i_volume_average",
        documentation: "Volume averaged ion density (average over the plasma volume up to the LCFS)",
        units: "m^-3",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_ION_N_I_VOLUME_AVERAGE),
    },
];

static NODES_DENSITY_CONTROL: &[Node] = &[
    Node {
        name: "valve",
        documentation: "Set of injection valves. Time-dependent",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_DENSITY_CONTROL_VALVE),
    },
    Node {
        name: "n_e_line",
        documentation: "Line integrated electron density over a line of sight in the whole vacuum chamber",
        units: "m^-2",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_N_E_LINE),
    },
    Node {
        name: "n_e_line_lcfs",
        documentation: "Line integrated electron density over a line of sight within the LCFS",
        units: "m^-2",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_N_E_LINE_LCFS),
    },
    Node {
        name: "n_e_profile_average",
        documentation: "Integral of a 1D core profile over rho_tor_norm up to the LCFS",
        units: "m^-3",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_N_E_PROFILE_AVERAGE),
    },
    Node {
        name: "n_e_line_of_sight",
        documentation: "Description of the line of sight for calculating n_e, defined by two points when the beam is not reflected, a third point is added to define the reflected beam path",
        units: "",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_N_E_LINE_OF_SIGHT),
    },
    Node {
        name: "n_e_volume_average",
        documentation: "Volume averaged electron density (average over the plasma volume up to the LCFS)",
        units: "m^-3",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_N_E_VOLUME_AVERAGE),
    },
    Node {
        name: "zeff",
        documentation: "Line averaged effective charge",
        units: "1",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_ZEFF),
    },
    Node {
        name: "zeff_method",
        documentation: "Method for zeff calculation : Index = 1: average over a line of sight in the whole vacuum chamber, 2 : average over a line of sight within the LCFS, 3 : average of a 1D core profile over rho_tor_norm up to the LCFS",
        units: "",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_ZEFF_METHOD),
    },
    Node {
        name: "zeff_line_of_sight",
        documentation: "Description of the line of sight for calculating zeff, defined by two points when the beam is not reflected, a third point is added to define the reflected beam path",
        units: "",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_ZEFF_LINE_OF_SIGHT),
    },
    Node {
        name: "n_t_over_n_d",
        documentation: "Average ratio of tritium over deuterium density",
        units: "1",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_N_T_OVER_N_D),
    },
    Node {
        name: "n_h_over_n_d",
        documentation: "Average ratio of hydrogen over deuterium density",
        units: "1",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL_N_H_OVER_N_D),
    },
    Node {
        name: "ion",
        documentation: "Quantities related to the different ion species, in the sense of isonuclear or isomolecular sequences",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_DENSITY_CONTROL_ION),
    },
    Node {
        name: "mode",
        documentation: "Control mode (operation mode and/or settings used by the controller)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_1D { pulse_schedule.density_control.mode.clone() },
                )
            },
        }),
    },
    Node {
        name: "time",
        documentation: "Timebase for the dynamic nodes located at this level of the IDS structure and below",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.density_control.time.clone() },
                )
            },
        }),
    },
];

static NODES_EVENT_TYPE: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_event,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.event[at[0]].r#type.name.clone() },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_event,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.event[at[0]].r#type.index.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_event,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.event[at[0]].r#type.description.clone() },
                )
            },
        }),
    },
];

static NODES_EVENT_ACQUISITION_STRATEGY: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_event,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.event[at[0]].acquisition_strategy.name.clone() },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_event,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.event[at[0]].acquisition_strategy.index.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_event,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.event[at[0]].acquisition_strategy.description.clone() },
                )
            },
        }),
    },
];

static NODES_EVENT_ACQUISITION_STATE: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_event,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.event[at[0]].acquisition_state.name.clone() },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_event,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.event[at[0]].acquisition_state.index.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_event,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.event[at[0]].acquisition_state.description.clone() },
                )
            },
        }),
    },
];

static NODES_EVENT: &[Node] = &[
    Node {
        name: "type",
        documentation: "Type of this event",
        units: "",
        kind: NodeKind::Structure(NODES_EVENT_TYPE),
    },
    Node {
        name: "identifier",
        documentation: "Unique identifier of this event provided by the scheduling / event handler",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_event,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.event[at[0]].identifier.clone() },
                )
            },
        }),
    },
    Node {
        name: "time_stamp",
        documentation: "Time stamp of this event",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_event,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_0D { pulse_schedule.event[at[0]].time_stamp.clone() },
                )
            },
        }),
    },
    Node {
        name: "duration",
        documentation: "Duration of this event",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_event,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_0D { pulse_schedule.event[at[0]].duration.clone() },
                )
            },
        }),
    },
    Node {
        name: "acquisition_strategy",
        documentation: "Acquisition strategy related to this event: index = 1 : on-trigger; index = 2 : pre-trigger; index = 3 : post-trigger",
        units: "",
        kind: NodeKind::Structure(NODES_EVENT_ACQUISITION_STRATEGY),
    },
    Node {
        name: "acquisition_state",
        documentation: "Acquisition state of the related system : index = 1 : armed; index = 2 : on; index = 3 : off; index = 4 : closed",
        units: "",
        kind: NodeKind::Structure(NODES_EVENT_ACQUISITION_STATE),
    },
    Node {
        name: "provider",
        documentation: "System having generated this event",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_event,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.event[at[0]].provider.clone() },
                )
            },
        }),
    },
    Node {
        name: "listeners",
        documentation: "Systems listening to this event",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_event,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_1D { pulse_schedule.event[at[0]].listeners.clone() },
                )
            },
        }),
    },
];

static NODES_FLUX_CONTROL_IP: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.flux_control.ip.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.flux_control.ip.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.flux_control.ip.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.flux_control.ip.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_FLUX_CONTROL_V_LOOP: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.flux_control.v_loop.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.flux_control.v_loop.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.flux_control.v_loop.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.flux_control.v_loop.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_FLUX_CONTROL_LI_3: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.flux_control.li_3.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.flux_control.li_3.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.flux_control.li_3.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.flux_control.li_3.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_FLUX_CONTROL_BETA_TOR_NORM: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.flux_control.beta_tor_norm.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.flux_control.beta_tor_norm.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.flux_control.beta_tor_norm.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.flux_control.beta_tor_norm.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_FLUX_CONTROL: &[Node] = &[
    Node {
        name: "ip",
        documentation: "Plasma current",
        units: "A",
        kind: NodeKind::Structure(NODES_FLUX_CONTROL_IP),
    },
    Node {
        name: "v_loop",
        documentation: "Loop voltage",
        units: "V",
        kind: NodeKind::Structure(NODES_FLUX_CONTROL_V_LOOP),
    },
    Node {
        name: "li_3",
        documentation: "Internal inductance",
        units: "1",
        kind: NodeKind::Structure(NODES_FLUX_CONTROL_LI_3),
    },
    Node {
        name: "beta_tor_norm",
        documentation: "Normalized toroidal beta, defined as 100 * beta_tor * a[m] * B0 [T] / ip [MA]",
        units: "1",
        kind: NodeKind::Structure(NODES_FLUX_CONTROL_BETA_TOR_NORM),
    },
    Node {
        name: "mode",
        documentation: "Control mode (operation mode and/or settings used by the controller)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_1D { pulse_schedule.flux_control.mode.clone() },
                )
            },
        }),
    },
    Node {
        name: "time",
        documentation: "Timebase for the dynamic nodes located at this level of the IDS structure and below",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.flux_control.time.clone() },
                )
            },
        }),
    },
];

static NODES_PF_ACTIVE_COIL_CURRENT: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_coil,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.pf_active.coil[at[0]].current.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_coil,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.pf_active.coil[at[0]].current.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_coil,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.pf_active.coil[at[0]].current.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_coil,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.pf_active.coil[at[0]].current.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_PF_ACTIVE_COIL_RESISTANCE_ADDITIONAL: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_coil,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D {
                        pulse_schedule.pf_active.coil[at[0]].resistance_additional.reference_name.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_coil,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.pf_active.coil[at[0]].resistance_additional.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_coil,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D {
                        pulse_schedule.pf_active.coil[at[0]].resistance_additional.reference_type.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_coil,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D {
                        pulse_schedule.pf_active.coil[at[0]].resistance_additional.envelope_type.clone()
                    },
                )
            },
        }),
    },
];

static NODES_PF_ACTIVE_COIL: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier (unique for a given device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_coil,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.pf_active.coil[at[0]].name.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Description, e.g. “channel viewing the upper divertor”",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_coil,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.pf_active.coil[at[0]].description.clone() },
                )
            },
        }),
    },
    Node {
        name: "current",
        documentation: "Reference waveform of current to be fed into the coil (for 1 turn, to be multiplied by the number of turns to obtain the generated magnetic field).  Positive when the current flows counter-clockwise in the coil when viewed from above.",
        units: "A",
        kind: NodeKind::Structure(NODES_PF_ACTIVE_COIL_CURRENT),
    },
    Node {
        name: "resistance_additional",
        documentation: "Additional resistance due to e.g. dynamically switchable resistors",
        units: "ohm",
        kind: NodeKind::Structure(NODES_PF_ACTIVE_COIL_RESISTANCE_ADDITIONAL),
    },
];

static NODES_PF_ACTIVE_SUPPLY_VOLTAGE: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_supply,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.pf_active.supply[at[0]].voltage.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_supply,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.pf_active.supply[at[0]].voltage.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_supply,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.pf_active.supply[at[0]].voltage.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_supply,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.pf_active.supply[at[0]].voltage.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_PF_ACTIVE_SUPPLY_CURRENT: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_supply,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.pf_active.supply[at[0]].current.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_supply,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.pf_active.supply[at[0]].current.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_supply,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.pf_active.supply[at[0]].current.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_supply,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.pf_active.supply[at[0]].current.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_PF_ACTIVE_SUPPLY: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier (unique for a given device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_supply,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.pf_active.supply[at[0]].name.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Description, e.g. “channel viewing the upper divertor”",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_pf_active_supply,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.pf_active.supply[at[0]].description.clone() },
                )
            },
        }),
    },
    Node {
        name: "voltage",
        documentation: "Voltage at the supply output (Vside1-Vside2)",
        units: "V",
        kind: NodeKind::Structure(NODES_PF_ACTIVE_SUPPLY_VOLTAGE),
    },
    Node {
        name: "current",
        documentation: "Current fed into one turn of the coil. (Multiply by number of turns to obtain generated magnetic field). Positive when flowing from side 1 to side 2 of the coil, this numbering being made consistently with the convention that the current flows counter-clockwise when viewed from above.",
        units: "A",
        kind: NodeKind::Structure(NODES_PF_ACTIVE_SUPPLY_CURRENT),
    },
];

static NODES_PF_ACTIVE: &[Node] = &[
    Node {
        name: "coil",
        documentation: "Set of poloidal field coils",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_PF_ACTIVE_COIL),
    },
    Node {
        name: "supply",
        documentation: "Set of PF power supplies",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_PF_ACTIVE_SUPPLY),
    },
    Node {
        name: "mode",
        documentation: "Control mode (operation mode and/or settings used by the controller)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_1D { pulse_schedule.pf_active.mode.clone() },
                )
            },
        }),
    },
    Node {
        name: "time",
        documentation: "Timebase for the dynamic nodes located at this level of the IDS structure and below",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.pf_active.time.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_MAGNETIC_AXIS_R: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.magnetic_axis.r.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.magnetic_axis.r.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.magnetic_axis.r.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.magnetic_axis.r.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_MAGNETIC_AXIS_Z: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.magnetic_axis.z.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.magnetic_axis.z.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.magnetic_axis.z.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.magnetic_axis.z.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_MAGNETIC_AXIS: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_MAGNETIC_AXIS_R),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_MAGNETIC_AXIS_Z),
    },
];

static NODES_POSITION_CONTROL_GEOMETRIC_AXIS_R: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.geometric_axis.r.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.geometric_axis.r.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.geometric_axis.r.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.geometric_axis.r.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_GEOMETRIC_AXIS_Z: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.geometric_axis.z.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.geometric_axis.z.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.geometric_axis.z.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.geometric_axis.z.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_GEOMETRIC_AXIS: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_GEOMETRIC_AXIS_R),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_GEOMETRIC_AXIS_Z),
    },
];

static NODES_POSITION_CONTROL_MINOR_RADIUS: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.minor_radius.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.minor_radius.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.minor_radius.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.minor_radius.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_ELONGATION: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.elongation.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.elongation.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.elongation.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.elongation.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_ELONGATION_UPPER: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.elongation_upper.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.elongation_upper.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.elongation_upper.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.elongation_upper.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_ELONGATION_LOWER: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.elongation_lower.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.elongation_lower.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.elongation_lower.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.elongation_lower.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_TRIANGULARITY: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.triangularity.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.triangularity.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.triangularity.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.triangularity.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_TRIANGULARITY_UPPER: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.triangularity_upper.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.triangularity_upper.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.triangularity_upper.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.triangularity_upper.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_TRIANGULARITY_LOWER: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.triangularity_lower.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.triangularity_lower.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.triangularity_lower.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.triangularity_lower.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_TRIANGULARITY_INNER: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.triangularity_inner.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.triangularity_inner.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.triangularity_inner.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.triangularity_inner.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_TRIANGULARITY_OUTER: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.triangularity_outer.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.triangularity_outer.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.triangularity_outer.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.triangularity_outer.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_TRIANGULARITY_MINOR: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.triangularity_minor.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.triangularity_minor.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.triangularity_minor.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.triangularity_minor.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_SQUARENESS_UPPER_OUTER: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.squareness_upper_outer.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.squareness_upper_outer.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.squareness_upper_outer.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.squareness_upper_outer.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_SQUARENESS_UPPER_INNER: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.squareness_upper_inner.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.squareness_upper_inner.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.squareness_upper_inner.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.squareness_upper_inner.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_SQUARENESS_LOWER_OUTER: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.squareness_lower_outer.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.squareness_lower_outer.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.squareness_lower_outer.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.squareness_lower_outer.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_SQUARENESS_LOWER_INNER: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.squareness_lower_inner.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.squareness_lower_inner.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.squareness_lower_inner.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.squareness_lower_inner.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_X_POINT_R: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_x_point,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.position_control.x_point[at[0]].r.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_x_point,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.position_control.x_point[at[0]].r.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_x_point,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.position_control.x_point[at[0]].r.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_x_point,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.position_control.x_point[at[0]].r.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_X_POINT_Z: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_x_point,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.position_control.x_point[at[0]].z.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_x_point,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.position_control.x_point[at[0]].z.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_x_point,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.position_control.x_point[at[0]].z.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_x_point,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.position_control.x_point[at[0]].z.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_X_POINT: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_X_POINT_R),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_X_POINT_Z),
    },
];

static NODES_POSITION_CONTROL_STRIKE_POINT_R: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_strike_point,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.position_control.strike_point[at[0]].r.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_strike_point,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.position_control.strike_point[at[0]].r.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_strike_point,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.position_control.strike_point[at[0]].r.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_strike_point,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.position_control.strike_point[at[0]].r.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_STRIKE_POINT_Z: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_strike_point,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.position_control.strike_point[at[0]].z.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_strike_point,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.position_control.strike_point[at[0]].z.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_strike_point,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.position_control.strike_point[at[0]].z.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_strike_point,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.position_control.strike_point[at[0]].z.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_STRIKE_POINT: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_STRIKE_POINT_R),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_STRIKE_POINT_Z),
    },
];

static NODES_POSITION_CONTROL_ACTIVE_LIMITER_POINT_R: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.active_limiter_point.r.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.active_limiter_point.r.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.active_limiter_point.r.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.active_limiter_point.r.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_ACTIVE_LIMITER_POINT_Z: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.active_limiter_point.z.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.active_limiter_point.z.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.active_limiter_point.z.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.active_limiter_point.z.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_ACTIVE_LIMITER_POINT: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_ACTIVE_LIMITER_POINT_R),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_ACTIVE_LIMITER_POINT_Z),
    },
];

static NODES_POSITION_CONTROL_BOUNDARY_OUTLINE_R: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_boundary_outline,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D {
                        pulse_schedule.position_control.boundary_outline[at[0]].r.reference_name.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_boundary_outline,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.position_control.boundary_outline[at[0]].r.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_boundary_outline,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D {
                        pulse_schedule.position_control.boundary_outline[at[0]].r.reference_type.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_boundary_outline,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D {
                        pulse_schedule.position_control.boundary_outline[at[0]].r.envelope_type.clone()
                    },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_BOUNDARY_OUTLINE_Z: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_boundary_outline,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D {
                        pulse_schedule.position_control.boundary_outline[at[0]].z.reference_name.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_boundary_outline,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.position_control.boundary_outline[at[0]].z.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_boundary_outline,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D {
                        pulse_schedule.position_control.boundary_outline[at[0]].z.reference_type.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_boundary_outline,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D {
                        pulse_schedule.position_control.boundary_outline[at[0]].z.envelope_type.clone()
                    },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_BOUNDARY_OUTLINE: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_BOUNDARY_OUTLINE_R),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_BOUNDARY_OUTLINE_Z),
    },
];

static NODES_POSITION_CONTROL_Z_R_MAX: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.z_r_max.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.z_r_max.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.z_r_max.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.z_r_max.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_Z_R_MIN: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.z_r_min.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.z_r_min.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.z_r_min.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.z_r_min.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_GAP_VALUE: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_gap,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.position_control.gap[at[0]].value.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_gap,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_1D { pulse_schedule.position_control.gap[at[0]].value.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_gap,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.position_control.gap[at[0]].value.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_gap,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> INT_0D { pulse_schedule.position_control.gap[at[0]].value.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_GAP: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier (unique for a given device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_gap,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.position_control.gap[at[0]].name.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Description, e.g. “channel viewing the upper divertor”",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_gap,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.position_control.gap[at[0]].description.clone() },
                )
            },
        }),
    },
    Node {
        name: "r",
        documentation: "Major radius of the reference point",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_gap,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_0D { pulse_schedule.position_control.gap[at[0]].r.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height of the reference point",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_gap,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_0D { pulse_schedule.position_control.gap[at[0]].z.clone() },
                )
            },
        }),
    },
    Node {
        name: "angle",
        documentation: "Angle between the direction in which the gap is measured (in the poloidal cross-section) and the horizontal axis.",
        units: "rad",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_position_control_gap,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> FLT_0D { pulse_schedule.position_control.gap[at[0]].angle.clone() },
                )
            },
        }),
    },
    Node {
        name: "value",
        documentation: "Value of the gap, i.e. distance between the reference point and the separatrix along the gap direction",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_GAP_VALUE),
    },
];

static NODES_POSITION_CONTROL_CURRENT_CENTROID_R: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.current_centroid.r.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.current_centroid.r.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.current_centroid.r.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.current_centroid.r.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_CURRENT_CENTROID_Z: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.position_control.current_centroid.z.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.current_centroid.z.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.current_centroid.z.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.position_control.current_centroid.z.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_POSITION_CONTROL_CURRENT_CENTROID: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_CURRENT_CENTROID_R),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_CURRENT_CENTROID_Z),
    },
];

static NODES_POSITION_CONTROL: &[Node] = &[
    Node {
        name: "magnetic_axis",
        documentation: "Magnetic axis position",
        units: "",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_MAGNETIC_AXIS),
    },
    Node {
        name: "geometric_axis",
        documentation: "RZ position of the geometric axis (defined as (Rmin+Rmax) / 2 and (Zmin+Zmax) / 2 of the boundary)",
        units: "",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_GEOMETRIC_AXIS),
    },
    Node {
        name: "minor_radius",
        documentation: "Minor radius of the plasma boundary (defined as (Rmax-Rmin) / 2 of the boundary)",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_MINOR_RADIUS),
    },
    Node {
        name: "elongation",
        documentation: "Elongation of the plasma boundary",
        units: "1",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_ELONGATION),
    },
    Node {
        name: "elongation_upper",
        documentation: "Elongation (upper half w.r.t. geometric axis) of the plasma boundary",
        units: "1",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_ELONGATION_UPPER),
    },
    Node {
        name: "elongation_lower",
        documentation: "Elongation (lower half w.r.t. geometric axis) of the plasma boundary",
        units: "1",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_ELONGATION_LOWER),
    },
    Node {
        name: "triangularity",
        documentation: "Triangularity of the plasma boundary",
        units: "1",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_TRIANGULARITY),
    },
    Node {
        name: "triangularity_upper",
        documentation: "Upper triangularity of the plasma boundary",
        units: "1",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_TRIANGULARITY_UPPER),
    },
    Node {
        name: "triangularity_lower",
        documentation: "Lower triangularity of the plasma boundary",
        units: "1",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_TRIANGULARITY_LOWER),
    },
    Node {
        name: "triangularity_inner",
        documentation: "Inner triangularity of the plasma boundary",
        units: "1",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_TRIANGULARITY_INNER),
    },
    Node {
        name: "triangularity_outer",
        documentation: "Outer triangularity of the plasma boundary",
        units: "1",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_TRIANGULARITY_OUTER),
    },
    Node {
        name: "triangularity_minor",
        documentation: "Minor triangularity of the plasma boundary",
        units: "1",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_TRIANGULARITY_MINOR),
    },
    Node {
        name: "squareness_upper_outer",
        documentation: "Upper outer squareness of the plasma boundary (definition from T. Luce, Plasma Phys. Control. Fusion 55 (2013) 095009)",
        units: "1",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_SQUARENESS_UPPER_OUTER),
    },
    Node {
        name: "squareness_upper_inner",
        documentation: "Upper inner squareness of the plasma boundary (definition from T. Luce, Plasma Phys. Control. Fusion 55 (2013) 095009)",
        units: "1",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_SQUARENESS_UPPER_INNER),
    },
    Node {
        name: "squareness_lower_outer",
        documentation: "Lower outer squareness of the plasma boundary (definition from T. Luce, Plasma Phys. Control. Fusion 55 (2013) 095009)",
        units: "1",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_SQUARENESS_LOWER_OUTER),
    },
    Node {
        name: "squareness_lower_inner",
        documentation: "Lower inner squareness of the plasma boundary (definition from T. Luce, Plasma Phys. Control. Fusion 55 (2013) 095009)",
        units: "1",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_SQUARENESS_LOWER_INNER),
    },
    Node {
        name: "x_point",
        documentation: "Array of X-points, for each of them the RZ position is given",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_POSITION_CONTROL_X_POINT),
    },
    Node {
        name: "strike_point",
        documentation: "Array of strike points, for each of them the RZ position is given",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_POSITION_CONTROL_STRIKE_POINT),
    },
    Node {
        name: "active_limiter_point",
        documentation: "RZ position of the active limiter point (point of the plasma boundary in contact with the limiter)",
        units: "",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_ACTIVE_LIMITER_POINT),
    },
    Node {
        name: "boundary_outline",
        documentation: "Set of (R,Z) points defining the outline of the plasma boundary",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_POSITION_CONTROL_BOUNDARY_OUTLINE),
    },
    Node {
        name: "z_r_max",
        documentation: "Height of the separatrix point of maximum major radius",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_Z_R_MAX),
    },
    Node {
        name: "z_r_min",
        documentation: "Height of the separatrix point of minimum major radius",
        units: "m",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_Z_R_MIN),
    },
    Node {
        name: "gap",
        documentation: "Set of gaps, defined by a reference point and a direction.",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_POSITION_CONTROL_GAP),
    },
    Node {
        name: "current_centroid",
        documentation: "RZ position of the current centroid",
        units: "",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL_CURRENT_CENTROID),
    },
    Node {
        name: "mode",
        documentation: "Control mode (operation mode and/or settings used by the controller)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_1D { pulse_schedule.position_control.mode.clone() },
                )
            },
        }),
    },
    Node {
        name: "time",
        documentation: "Timebase for the dynamic nodes located at this level of the IDS structure and below",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.position_control.time.clone() },
                )
            },
        }),
    },
];

static NODES_TF_B_FIELD_TOR_VACUUM_R: &[Node] = &[
    Node {
        name: "reference_name",
        documentation: "Reference name (e.g. in the native pulse schedule system of the device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.tf.b_field_tor_vacuum_r.reference_name.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference",
        documentation: "Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.tf.b_field_tor_vacuum_r.reference.clone() },
                )
            },
        }),
    },
    Node {
        name: "reference_type",
        documentation: "Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.tf.b_field_tor_vacuum_r.reference_type.clone() },
                )
            },
        }),
    },
    Node {
        name: "envelope_type",
        documentation: "Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_0D { pulse_schedule.tf.b_field_tor_vacuum_r.envelope_type.clone() },
                )
            },
        }),
    },
];

static NODES_TF: &[Node] = &[
    Node {
        name: "b_field_tor_vacuum_r",
        documentation: "Vacuum magnetic field times major radius within the cage of toroidal field coils. Positive sign means magnetic field is orientated counter-clockwise when viewed from above.",
        units: "T.m",
        kind: NodeKind::Structure(NODES_TF_B_FIELD_TOR_VACUUM_R),
    },
    Node {
        name: "mode",
        documentation: "Control mode (operation mode and/or settings used by the controller)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_1D { pulse_schedule.tf.mode.clone() },
                )
            },
        }),
    },
    Node {
        name: "time",
        documentation: "Timebase for the dynamic nodes located at this level of the IDS structure and below",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> FLT_1D { pulse_schedule.tf.time.clone() },
                )
            },
        }),
    },
];

static NODES_CODE_LIBRARY: &[Node] = &[
    Node {
        name: "name",
        documentation: "Name of software",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_code_library,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.code.library[at[0]].name.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Short description of the software (type, purpose)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_code_library,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.code.library[at[0]].description.clone() },
                )
            },
        }),
    },
    Node {
        name: "commit",
        documentation: "Unique commit reference of software",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_code_library,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.code.library[at[0]].commit.clone() },
                )
            },
        }),
    },
    Node {
        name: "version",
        documentation: "Unique version (tag) of software",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_code_library,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.code.library[at[0]].version.clone() },
                )
            },
        }),
    },
    Node {
        name: "repository",
        documentation: "URL of software repository",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_code_library,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.code.library[at[0]].repository.clone() },
                )
            },
        }),
    },
    Node {
        name: "parameters",
        documentation: "List of the code specific parameters in XML format",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    1,
                    lengths_code_library,
                    |pulse_schedule: &PulseSchedule, at: &[usize]| -> STR_0D { pulse_schedule.code.library[at[0]].parameters.clone() },
                )
            },
        }),
    },
];

static NODES_CODE: &[Node] = &[
    Node {
        name: "name",
        documentation: "Name of software generating IDS",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.code.name.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Short description of the software (type, purpose)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.code.description.clone() },
                )
            },
        }),
    },
    Node {
        name: "commit",
        documentation: "Unique commit reference of software",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.code.commit.clone() },
                )
            },
        }),
    },
    Node {
        name: "version",
        documentation: "Unique version (tag) of software",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.code.version.clone() },
                )
            },
        }),
    },
    Node {
        name: "repository",
        documentation: "URL of software repository",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.code.repository.clone() },
                )
            },
        }),
    },
    Node {
        name: "parameters",
        documentation: "List of the code specific parameters in XML format",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> STR_0D { pulse_schedule.code.parameters.clone() },
                )
            },
        }),
    },
    Node {
        name: "output_flag",
        documentation: "Output flag : 0 means the run is successful, other values mean some difficulty has been encountered, the exact meaning is then code specific. Negative values mean the result shall not be used.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let pulse_schedule: &PulseSchedule = ids.downcast_ref().ok_or_else(|| "not a pulse_schedule IDS".to_string())?;
                gather(
                    pulse_schedule,
                    indices,
                    0,
                    no_levels,
                    |pulse_schedule: &PulseSchedule, _at: &[usize]| -> INT_1D { pulse_schedule.code.output_flag.clone() },
                )
            },
        }),
    },
    Node {
        name: "library",
        documentation: "List of external libraries used by the code that has produced this IDS",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_CODE_LIBRARY),
    },
];

static NODES_ROOT: &[Node] = &[
    Node {
        name: "ic",
        documentation: "Ion cyclotron heating and current drive system",
        units: "",
        kind: NodeKind::Structure(NODES_IC),
    },
    Node {
        name: "ec",
        documentation: "Electron cyclotron heating and current drive system",
        units: "",
        kind: NodeKind::Structure(NODES_EC),
    },
    Node {
        name: "lh",
        documentation: "Lower Hybrid heating and current drive system",
        units: "",
        kind: NodeKind::Structure(NODES_LH),
    },
    Node {
        name: "nbi",
        documentation: "Neutral beam heating and current drive system",
        units: "",
        kind: NodeKind::Structure(NODES_NBI),
    },
    Node {
        name: "density_control",
        documentation: "Gas injection system and density control references",
        units: "",
        kind: NodeKind::Structure(NODES_DENSITY_CONTROL),
    },
    Node {
        name: "event",
        documentation: "List of events, either predefined triggers  or events recorded during the pulse",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_EVENT),
    },
    Node {
        name: "flux_control",
        documentation: "Magnetic flux control references",
        units: "",
        kind: NodeKind::Structure(NODES_FLUX_CONTROL),
    },
    Node {
        name: "pf_active",
        documentation: "Poloidal field coil references",
        units: "",
        kind: NodeKind::Structure(NODES_PF_ACTIVE),
    },
    Node {
        name: "position_control",
        documentation: "Plasma position and shape control references",
        units: "",
        kind: NodeKind::Structure(NODES_POSITION_CONTROL),
    },
    Node {
        name: "tf",
        documentation: "Toroidal field references",
        units: "",
        kind: NodeKind::Structure(NODES_TF),
    },
    Node {
        name: "code",
        documentation: "",
        units: "",
        kind: NodeKind::Structure(NODES_CODE),
    },
];

/// The root of the pulse_schedule data dictionary.
pub static PULSE_SCHEDULE_ROOT: Node = Node {
    name: "pulse_schedule",
    documentation: "Description of Pulse Schedule, described by subsystems waveform references and an enveloppe around them. The controllers, pulse schedule and SDN are defined in separate IDSs. All names and identifiers of subsystems appearing in the pulse_schedule must be identical to those used in the IDSs describing the related subsystems.",
    units: "",
    kind: NodeKind::Structure(NODES_ROOT),
};
