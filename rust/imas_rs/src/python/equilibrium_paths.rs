//! Static description of the equilibrium data dictionary, used to build and resolve paths.
//!
//! **WARNING** This file is autogenerated by `imas_updater/build_ids.py`.
//! Any changes will be overwritten.
//!
//! Each leaf carries the function that reads it out of the IDS, so the path a
//! reader follows is written once and checked by the compiler. The `read` closures
//! capture nothing, so they coerce to plain `fn` pointers and the whole description
//! stays a `static`.

#![allow(clippy::all)]

use super::{IndexSpec, Leaf, Node, NodeKind, gather, no_levels};
use crate::dd_base_types::{FLT_0D, FLT_1D, FLT_2D, FLT_3D, FLT_4D, INT_0D, INT_1D, INT_2D, STR_0D};
use crate::ids::equilibrium::Equilibrium;
use std::any::Any;

// ============================================================================
// Array-of-structures lengths
// ============================================================================

/// Length of each array-of-structures level along `code_library`.
fn lengths_code_library(equilibrium: &Equilibrium, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.code.library.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `greens_pf_active`.
fn lengths_greens_pf_active(equilibrium: &Equilibrium, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.greens.pf_active.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `greens_pf_passive`.
fn lengths_greens_pf_passive(equilibrium: &Equilibrium, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.greens.pf_passive.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `greens_pf_passive_dof`.
fn lengths_greens_pf_passive_dof(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.greens.pf_passive.len()),
        1 => return Some(equilibrium.greens.pf_passive.get(at[0])?.dof.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `grids_ggd`.
fn lengths_grids_ggd(equilibrium: &Equilibrium, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.grids_ggd.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `grids_ggd_grid`.
fn lengths_grids_ggd_grid(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.grids_ggd.len()),
        1 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `grids_ggd_grid_grid_subset`.
fn lengths_grids_ggd_grid_grid_subset(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.grids_ggd.len()),
        1 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.len()),
        2 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.grid_subset.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `grids_ggd_grid_grid_subset_base`.
fn lengths_grids_ggd_grid_grid_subset_base(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.grids_ggd.len()),
        1 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.len()),
        2 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.grid_subset.len()),
        3 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.grid_subset.get(at[2])?.base.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `grids_ggd_grid_grid_subset_element_object`.
fn lengths_grids_ggd_grid_grid_subset_element_object(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.grids_ggd.len()),
        1 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.len()),
        2 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.grid_subset.len()),
        3 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.grid_subset.get(at[2])?.element.len()),
        4 => {
            return Some(
                equilibrium
                    .grids_ggd
                    .get(at[0])?
                    .grid
                    .get(at[1])?
                    .grid_subset
                    .get(at[2])?
                    .element
                    .get(at[3])?
                    .object
                    .len(),
            );
        }
        _ => return None,
    }
}

/// Length of each array-of-structures level along `grids_ggd_grid_space`.
fn lengths_grids_ggd_grid_space(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.grids_ggd.len()),
        1 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.len()),
        2 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.space.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `grids_ggd_grid_space_coordinates_type`.
fn lengths_grids_ggd_grid_space_coordinates_type(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.grids_ggd.len()),
        1 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.len()),
        2 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.space.len()),
        3 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.space.get(at[2])?.coordinates_type.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `grids_ggd_grid_space_objects_per_dimension`.
fn lengths_grids_ggd_grid_space_objects_per_dimension(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.grids_ggd.len()),
        1 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.len()),
        2 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.space.len()),
        3 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.space.get(at[2])?.objects_per_dimension.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `grids_ggd_grid_space_objects_per_dimension_object`.
fn lengths_grids_ggd_grid_space_objects_per_dimension_object(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.grids_ggd.len()),
        1 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.len()),
        2 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.space.len()),
        3 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.space.get(at[2])?.objects_per_dimension.len()),
        4 => {
            return Some(
                equilibrium
                    .grids_ggd
                    .get(at[0])?
                    .grid
                    .get(at[1])?
                    .space
                    .get(at[2])?
                    .objects_per_dimension
                    .get(at[3])?
                    .object
                    .len(),
            );
        }
        _ => return None,
    }
}

/// Length of each array-of-structures level along `grids_ggd_grid_space_objects_per_dimension_object_boundary`.
fn lengths_grids_ggd_grid_space_objects_per_dimension_object_boundary(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.grids_ggd.len()),
        1 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.len()),
        2 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.space.len()),
        3 => return Some(equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.space.get(at[2])?.objects_per_dimension.len()),
        4 => {
            return Some(
                equilibrium
                    .grids_ggd
                    .get(at[0])?
                    .grid
                    .get(at[1])?
                    .space
                    .get(at[2])?
                    .objects_per_dimension
                    .get(at[3])?
                    .object
                    .len(),
            );
        }
        5 => {
            return Some(
                equilibrium
                    .grids_ggd
                    .get(at[0])?
                    .grid
                    .get(at[1])?
                    .space
                    .get(at[2])?
                    .objects_per_dimension
                    .get(at[3])?
                    .object
                    .get(at[4])?
                    .boundary
                    .len(),
            );
        }
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice`.
fn lengths_time_slice(equilibrium: &Equilibrium, level: usize, _at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_boundary_gap`.
fn lengths_time_slice_boundary_gap(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.boundary.gap.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_constraints_b_field_pol_probe`.
fn lengths_time_slice_constraints_b_field_pol_probe(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.constraints.b_field_pol_probe.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_constraints_faraday_angle`.
fn lengths_time_slice_constraints_faraday_angle(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.constraints.faraday_angle.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_constraints_flux_loop`.
fn lengths_time_slice_constraints_flux_loop(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.constraints.flux_loop.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_constraints_iron_core_segment`.
fn lengths_time_slice_constraints_iron_core_segment(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.constraints.iron_core_segment.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_constraints_j_parallel`.
fn lengths_time_slice_constraints_j_parallel(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.constraints.j_parallel.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_constraints_j_phi`.
fn lengths_time_slice_constraints_j_phi(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.constraints.j_phi.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_constraints_mse_polarization_angle`.
fn lengths_time_slice_constraints_mse_polarization_angle(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.constraints.mse_polarization_angle.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_constraints_n_e`.
fn lengths_time_slice_constraints_n_e(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.constraints.n_e.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_constraints_n_e_line`.
fn lengths_time_slice_constraints_n_e_line(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.constraints.n_e_line.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_constraints_pf_current`.
fn lengths_time_slice_constraints_pf_current(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.constraints.pf_current.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_constraints_pf_passive_current`.
fn lengths_time_slice_constraints_pf_passive_current(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.constraints.pf_passive_current.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_constraints_pressure`.
fn lengths_time_slice_constraints_pressure(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.constraints.pressure.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_constraints_pressure_rotational`.
fn lengths_time_slice_constraints_pressure_rotational(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.constraints.pressure_rotational.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_constraints_q`.
fn lengths_time_slice_constraints_q(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.constraints.q.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_constraints_strike_point`.
fn lengths_time_slice_constraints_strike_point(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.constraints.strike_point.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_constraints_x_point`.
fn lengths_time_slice_constraints_x_point(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.constraints.x_point.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_contour_tree_node`.
fn lengths_time_slice_contour_tree_node(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.contour_tree.node.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_contour_tree_node_levelset`.
fn lengths_time_slice_contour_tree_node_levelset(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.contour_tree.node.len()),
        2 => return Some(equilibrium.time_slice.get(at[0])?.contour_tree.node.get(at[1])?.levelset.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_ggd_b_field_phi`.
fn lengths_time_slice_ggd_b_field_phi(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.ggd.len()),
        2 => return Some(equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.b_field_phi.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_ggd_b_field_r`.
fn lengths_time_slice_ggd_b_field_r(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.ggd.len()),
        2 => return Some(equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.b_field_r.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_ggd_b_field_z`.
fn lengths_time_slice_ggd_b_field_z(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.ggd.len()),
        2 => return Some(equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.b_field_z.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_ggd_j_parallel`.
fn lengths_time_slice_ggd_j_parallel(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.ggd.len()),
        2 => return Some(equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.j_parallel.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_ggd_j_phi`.
fn lengths_time_slice_ggd_j_phi(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.ggd.len()),
        2 => return Some(equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.j_phi.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_ggd_phi`.
fn lengths_time_slice_ggd_phi(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.ggd.len()),
        2 => return Some(equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.phi.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_ggd_psi`.
fn lengths_time_slice_ggd_psi(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.ggd.len()),
        2 => return Some(equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.psi.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_ggd_r`.
fn lengths_time_slice_ggd_r(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.ggd.len()),
        2 => return Some(equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.r.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_ggd_theta`.
fn lengths_time_slice_ggd_theta(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.ggd.len()),
        2 => return Some(equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.theta.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_ggd_z`.
fn lengths_time_slice_ggd_z(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.ggd.len()),
        2 => return Some(equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.z.len()),
        _ => return None,
    }
}

/// Length of each array-of-structures level along `time_slice_profiles_2d`.
fn lengths_time_slice_profiles_2d(equilibrium: &Equilibrium, level: usize, at: &[usize]) -> Option<usize> {
    match level {
        0 => return Some(equilibrium.time_slice.len()),
        1 => return Some(equilibrium.time_slice.get(at[0])?.profiles_2d.len()),
        _ => return None,
    }
}

// ============================================================================
// Nodes
// ============================================================================

static NODES_VACUUM_TOROIDAL_FIELD: &[Node] = &[
    Node {
        name: "r0",
        documentation: "Reference major radius where the vacuum toroidal magnetic field is given (usually a fixed position such as the middle of the vessel at the equatorial midplane)",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_0D> { equilibrium.vacuum_toroidal_field.r0.clone() },
                )
            },
        }),
    },
    Node {
        name: "b0",
        documentation: "Vacuum toroidal field at R0 [T]; Positive sign means counter-clockwise when viewed from above. The product R0B0 must be consistent with the b_tor_vacuum_r field of the tf IDS.",
        units: "T",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_1D> { equilibrium.vacuum_toroidal_field.b0.clone() },
                )
            },
        }),
    },
];

static NODES_GRIDS_GGD_GRID_IDENTIFIER: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_grids_ggd_grid,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> { equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.identifier.name.clone() },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_grids_ggd_grid,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> { equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.identifier.index.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_grids_ggd_grid,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.identifier.description.clone()
                    },
                )
            },
        }),
    },
];

static NODES_GRIDS_GGD_GRID_SPACE_IDENTIFIER: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_grids_ggd_grid_space,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.space.get(at[2])?.identifier.name.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_grids_ggd_grid_space,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.space.get(at[2])?.identifier.index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_grids_ggd_grid_space,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .space
                            .get(at[2])?
                            .identifier
                            .description
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_GRIDS_GGD_GRID_SPACE_GEOMETRY_TYPE: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_grids_ggd_grid_space,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.space.get(at[2])?.geometry_type.name.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_grids_ggd_grid_space,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.space.get(at[2])?.geometry_type.index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_grids_ggd_grid_space,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .space
                            .get(at[2])?
                            .geometry_type
                            .description
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_GRIDS_GGD_GRID_SPACE_COORDINATES_TYPE: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    4,
                    lengths_grids_ggd_grid_space_coordinates_type,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .space
                            .get(at[2])?
                            .coordinates_type
                            .get(at[3])?
                            .name
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    4,
                    lengths_grids_ggd_grid_space_coordinates_type,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .space
                            .get(at[2])?
                            .coordinates_type
                            .get(at[3])?
                            .index
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    4,
                    lengths_grids_ggd_grid_space_coordinates_type,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .space
                            .get(at[2])?
                            .coordinates_type
                            .get(at[3])?
                            .description
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_GRIDS_GGD_GRID_SPACE_OBJECTS_PER_DIMENSION_OBJECT_BOUNDARY: &[Node] = &[
    Node {
        name: "index",
        documentation: "Index of this (n-1)-dimensional boundary object",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    6,
                    lengths_grids_ggd_grid_space_objects_per_dimension_object_boundary,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .space
                            .get(at[2])?
                            .objects_per_dimension
                            .get(at[3])?
                            .object
                            .get(at[4])?
                            .boundary
                            .get(at[5])?
                            .index
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "neighbours",
        documentation: "List of indices of the n-dimensional objects adjacent to the given n-dimensional object. An object can possibly have multiple neighbours on a boundary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    6,
                    lengths_grids_ggd_grid_space_objects_per_dimension_object_boundary,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_1D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .space
                            .get(at[2])?
                            .objects_per_dimension
                            .get(at[3])?
                            .object
                            .get(at[4])?
                            .boundary
                            .get(at[5])?
                            .neighbours
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_GRIDS_GGD_GRID_SPACE_OBJECTS_PER_DIMENSION_OBJECT: &[Node] = &[
    Node {
        name: "boundary",
        documentation: "Set of  (n-1)-dimensional objects defining the boundary of this n-dimensional object",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_GRIDS_GGD_GRID_SPACE_OBJECTS_PER_DIMENSION_OBJECT_BOUNDARY),
    },
    Node {
        name: "geometry",
        documentation: "Geometry data associated with the object, its detailed content is defined by ../../geometry_content. Its dimension depends on the type of object, geometry and coordinate considered.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    5,
                    lengths_grids_ggd_grid_space_objects_per_dimension_object,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .space
                            .get(at[2])?
                            .objects_per_dimension
                            .get(at[3])?
                            .object
                            .get(at[4])?
                            .geometry
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "nodes",
        documentation: "List of nodes forming this object (indices to objects_per_dimension(1)%object(:) in Fortran notation)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    5,
                    lengths_grids_ggd_grid_space_objects_per_dimension_object,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_1D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .space
                            .get(at[2])?
                            .objects_per_dimension
                            .get(at[3])?
                            .object
                            .get(at[4])?
                            .nodes
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "measure",
        documentation: "Measure of the space object, i.e. physical size (length for 1d, area for 2d, volume for 3d objects,...)",
        units: "m^dimension",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    5,
                    lengths_grids_ggd_grid_space_objects_per_dimension_object,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .space
                            .get(at[2])?
                            .objects_per_dimension
                            .get(at[3])?
                            .object
                            .get(at[4])?
                            .measure
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "geometry_2d",
        documentation: "2D geometry data associated with the object. Its dimension depends on the type of object, geometry and coordinate considered. Typically, the first dimension represents the object coordinates, while the second dimension would represent the values of the various degrees of freedom of the finite element attached to the object.",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    5,
                    lengths_grids_ggd_grid_space_objects_per_dimension_object,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .space
                            .get(at[2])?
                            .objects_per_dimension
                            .get(at[3])?
                            .object
                            .get(at[4])?
                            .geometry_2d
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_GRIDS_GGD_GRID_SPACE_OBJECTS_PER_DIMENSION_GEOMETRY_CONTENT: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    4,
                    lengths_grids_ggd_grid_space_objects_per_dimension,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .space
                            .get(at[2])?
                            .objects_per_dimension
                            .get(at[3])?
                            .geometry_content
                            .name
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    4,
                    lengths_grids_ggd_grid_space_objects_per_dimension,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .space
                            .get(at[2])?
                            .objects_per_dimension
                            .get(at[3])?
                            .geometry_content
                            .index
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    4,
                    lengths_grids_ggd_grid_space_objects_per_dimension,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .space
                            .get(at[2])?
                            .objects_per_dimension
                            .get(at[3])?
                            .geometry_content
                            .description
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_GRIDS_GGD_GRID_SPACE_OBJECTS_PER_DIMENSION: &[Node] = &[
    Node {
        name: "object",
        documentation: "Set of objects for a given dimension",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_GRIDS_GGD_GRID_SPACE_OBJECTS_PER_DIMENSION_OBJECT),
    },
    Node {
        name: "geometry_content",
        documentation: "Content of the ../object/geometry node for this dimension",
        units: "",
        kind: NodeKind::Structure(NODES_GRIDS_GGD_GRID_SPACE_OBJECTS_PER_DIMENSION_GEOMETRY_CONTENT),
    },
];

static NODES_GRIDS_GGD_GRID_SPACE: &[Node] = &[
    Node {
        name: "identifier",
        documentation: "Space identifier",
        units: "",
        kind: NodeKind::Structure(NODES_GRIDS_GGD_GRID_SPACE_IDENTIFIER),
    },
    Node {
        name: "geometry_type",
        documentation: "Type of space geometry (0: standard, 1:Fourier, >1: Fourier with periodicity)",
        units: "",
        kind: NodeKind::Structure(NODES_GRIDS_GGD_GRID_SPACE_GEOMETRY_TYPE),
    },
    Node {
        name: "coordinates_type",
        documentation: "Type of coordinates describing the physical space, for every coordinate of the space. The size of this node therefore defines the dimension of the space.",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_GRIDS_GGD_GRID_SPACE_COORDINATES_TYPE),
    },
    Node {
        name: "objects_per_dimension",
        documentation: "Definition of the space objects for every dimension (from one to the dimension of the highest-dimensional objects). The index correspond to 1=nodes, 2=edges, 3=faces, 4=cells/volumes, .... For every index, a collection of objects of that dimension is described.",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_GRIDS_GGD_GRID_SPACE_OBJECTS_PER_DIMENSION),
    },
];

static NODES_GRIDS_GGD_GRID_GRID_SUBSET_IDENTIFIER: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_grids_ggd_grid_grid_subset,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .grid_subset
                            .get(at[2])?
                            .identifier
                            .name
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_grids_ggd_grid_grid_subset,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .grid_subset
                            .get(at[2])?
                            .identifier
                            .index
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_grids_ggd_grid_grid_subset,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .grid_subset
                            .get(at[2])?
                            .identifier
                            .description
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_GRIDS_GGD_GRID_GRID_SUBSET_ELEMENT_OBJECT: &[Node] = &[
    Node {
        name: "space",
        documentation: "Index of the space from which that object is taken",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    5,
                    lengths_grids_ggd_grid_grid_subset_element_object,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .grid_subset
                            .get(at[2])?
                            .element
                            .get(at[3])?
                            .object
                            .get(at[4])?
                            .space
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "dimension",
        documentation: "Dimension of the object - using the convention  1=nodes, 2=edges, 3=faces, 4=cells/volumes",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    5,
                    lengths_grids_ggd_grid_grid_subset_element_object,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .grid_subset
                            .get(at[2])?
                            .element
                            .get(at[3])?
                            .object
                            .get(at[4])?
                            .dimension
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Object index",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    5,
                    lengths_grids_ggd_grid_grid_subset_element_object,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .grid_subset
                            .get(at[2])?
                            .element
                            .get(at[3])?
                            .object
                            .get(at[4])?
                            .index
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_GRIDS_GGD_GRID_GRID_SUBSET_ELEMENT: &[Node] = &[Node {
    name: "object",
    documentation: "Set of objects defining the element",
    units: "",
    kind: NodeKind::ArrayOfStructures(NODES_GRIDS_GGD_GRID_GRID_SUBSET_ELEMENT_OBJECT),
}];

static NODES_GRIDS_GGD_GRID_GRID_SUBSET_BASE: &[Node] = &[
    Node {
        name: "jacobian",
        documentation: "Metric Jacobian",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    4,
                    lengths_grids_ggd_grid_grid_subset_base,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .grid_subset
                            .get(at[2])?
                            .base
                            .get(at[3])?
                            .jacobian
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "tensor_covariant",
        documentation: "Covariant metric tensor, given on each element of the subgrid (first dimension)",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_3D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    4,
                    lengths_grids_ggd_grid_grid_subset_base,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_3D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .grid_subset
                            .get(at[2])?
                            .base
                            .get(at[3])?
                            .tensor_covariant
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "tensor_contravariant",
        documentation: "Contravariant metric tensor, given on each element of the subgrid (first dimension)",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_3D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    4,
                    lengths_grids_ggd_grid_grid_subset_base,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_3D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .grid_subset
                            .get(at[2])?
                            .base
                            .get(at[3])?
                            .tensor_contravariant
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_GRIDS_GGD_GRID_GRID_SUBSET_METRIC: &[Node] = &[
    Node {
        name: "jacobian",
        documentation: "Metric Jacobian",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_grids_ggd_grid_grid_subset,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .grid_subset
                            .get(at[2])?
                            .metric
                            .jacobian
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "tensor_covariant",
        documentation: "Covariant metric tensor, given on each element of the subgrid (first dimension)",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_3D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_grids_ggd_grid_grid_subset,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_3D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .grid_subset
                            .get(at[2])?
                            .metric
                            .tensor_covariant
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "tensor_contravariant",
        documentation: "Contravariant metric tensor, given on each element of the subgrid (first dimension)",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_3D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_grids_ggd_grid_grid_subset,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_3D> {
                        equilibrium
                            .grids_ggd
                            .get(at[0])?
                            .grid
                            .get(at[1])?
                            .grid_subset
                            .get(at[2])?
                            .metric
                            .tensor_contravariant
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_GRIDS_GGD_GRID_GRID_SUBSET: &[Node] = &[
    Node {
        name: "identifier",
        documentation: "Grid subset identifier",
        units: "",
        kind: NodeKind::Structure(NODES_GRIDS_GGD_GRID_GRID_SUBSET_IDENTIFIER),
    },
    Node {
        name: "dimension",
        documentation: "Space dimension of the grid subset elements, using the convention 1=nodes, 2=edges, 3=faces, 4=cells/volumes",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_grids_ggd_grid_grid_subset,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.grid_subset.get(at[2])?.dimension.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "element",
        documentation: "Set of elements defining the grid subset. An element is defined by a combination of objects from potentially all spaces",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_GRIDS_GGD_GRID_GRID_SUBSET_ELEMENT),
    },
    Node {
        name: "base",
        documentation: "Set of bases for the grid subset. For each base, the structure describes the projection of the base vectors on the canonical frame of the grid.",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_GRIDS_GGD_GRID_GRID_SUBSET_BASE),
    },
    Node {
        name: "metric",
        documentation: "Metric of the canonical frame onto Cartesian coordinates",
        units: "",
        kind: NodeKind::Structure(NODES_GRIDS_GGD_GRID_GRID_SUBSET_METRIC),
    },
];

static NODES_GRIDS_GGD_GRID: &[Node] = &[
    Node {
        name: "identifier",
        documentation: "Grid identifier",
        units: "",
        kind: NodeKind::Structure(NODES_GRIDS_GGD_GRID_IDENTIFIER),
    },
    Node {
        name: "path",
        documentation: "Path of the grid, including the IDS name, in case of implicit reference to a grid_ggd node described in another IDS. To be filled only if the grid is not described explicitly in this grid_ggd structure. Example syntax: #wall:2/description_ggd(1)/grid_ggd, means that the grid is located in the wall IDS, occurrence 2, with relative path description_ggd(1)/grid_ggd, using Fortran index convention (here : first index of the array)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_grids_ggd_grid,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> { equilibrium.grids_ggd.get(at[0])?.grid.get(at[1])?.path.clone() },
                )
            },
        }),
    },
    Node {
        name: "space",
        documentation: "Set of grid spaces",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_GRIDS_GGD_GRID_SPACE),
    },
    Node {
        name: "grid_subset",
        documentation: "Grid subsets",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_GRIDS_GGD_GRID_GRID_SUBSET),
    },
];

static NODES_GRIDS_GGD: &[Node] = &[
    Node {
        name: "grid",
        documentation: "Set of GGD grids for describing the equilibrium, at a given time slice",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_GRIDS_GGD_GRID),
    },
    Node {
        name: "time",
        documentation: "Time",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_grids_ggd,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.grids_ggd.get(at[0])?.time.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_BOUNDARY_OUTLINE: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.boundary.outline.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.boundary.outline.z.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_BOUNDARY_GEOMETRIC_AXIS: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.geometric_axis.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.geometric_axis.z.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_BOUNDARY_CLOSEST_WALL_POINT: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.closest_wall_point.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.closest_wall_point.z.clone() },
                )
            },
        }),
    },
    Node {
        name: "distance",
        documentation: "Distance to the plasma boundary",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.boundary.closest_wall_point.distance.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_BOUNDARY_DR_DZ_ZERO_POINT: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.dr_dz_zero_point.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.dr_dz_zero_point.z.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_BOUNDARY_GAP: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier (unique for a given device)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_boundary_gap,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> { equilibrium.time_slice.get(at[0])?.boundary.gap.get(at[1])?.name.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Description, e.g. mid-plane gap",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_boundary_gap,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.boundary.gap.get(at[1])?.description.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "r",
        documentation: "Major radius of the reference point",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_boundary_gap,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.gap.get(at[1])?.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height of the reference point",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_boundary_gap,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.gap.get(at[1])?.z.clone() },
                )
            },
        }),
    },
    Node {
        name: "angle",
        documentation: "Angle measured clockwise from radial cylindrical vector (grad R) to gap vector (pointing away from reference point)",
        units: "rad",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_boundary_gap,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.gap.get(at[1])?.angle.clone() },
                )
            },
        }),
    },
    Node {
        name: "value",
        documentation: "Value of the gap, i.e. distance between the reference point and the separatrix along the gap direction",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_boundary_gap,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.gap.get(at[1])?.value.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_BOUNDARY_BOUNDING: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius of the point which defines the plasma boundary",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.bounding.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height of the point which defines the plasma boundary",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.bounding.z.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_BOUNDARY: &[Node] = &[
    Node {
        name: "type",
        documentation: "0 (limiter) or 1 (diverted)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> { equilibrium.time_slice.get(at[0])?.boundary.r#type.clone() },
                )
            },
        }),
    },
    Node {
        name: "outline",
        documentation: "RZ outline of the plasma boundary",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_BOUNDARY_OUTLINE),
    },
    Node {
        name: "psi_norm",
        documentation: "Value of the normalized poloidal flux at which the boundary is taken, the flux being normalized to its value at the separatrix (so psi_norm = 1 if the boundary is the separatrix)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.psi_norm.clone() },
                )
            },
        }),
    },
    Node {
        name: "psi",
        documentation: "Value of the poloidal flux at which the boundary is taken. For a positive plasma current (counter-clockwise when viewed from above), increases from the magnetic axis to the boundary",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.psi.clone() },
                )
            },
        }),
    },
    Node {
        name: "geometric_axis",
        documentation: "RZ position of the geometric axis (defined as (Rmin+Rmax) / 2 and (Zmin+Zmax) / 2 of the boundary)",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_BOUNDARY_GEOMETRIC_AXIS),
    },
    Node {
        name: "minor_radius",
        documentation: "Minor radius of the plasma boundary (defined as (Rmax-Rmin) / 2 of the boundary)",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.minor_radius.clone() },
                )
            },
        }),
    },
    Node {
        name: "elongation",
        documentation: "Elongation of the plasma boundary",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.elongation.clone() },
                )
            },
        }),
    },
    Node {
        name: "triangularity",
        documentation: "Triangularity of the plasma boundary",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.triangularity.clone() },
                )
            },
        }),
    },
    Node {
        name: "triangularity_upper",
        documentation: "Upper triangularity of the plasma boundary",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.triangularity_upper.clone() },
                )
            },
        }),
    },
    Node {
        name: "triangularity_lower",
        documentation: "Lower triangularity of the plasma boundary",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.triangularity_lower.clone() },
                )
            },
        }),
    },
    Node {
        name: "squareness_upper_inner",
        documentation: "Upper inner squareness of the plasma boundary (definition from T. Luce, Plasma Phys. Control. Fusion 55 (2013) 095009)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.squareness_upper_inner.clone() },
                )
            },
        }),
    },
    Node {
        name: "squareness_upper_outer",
        documentation: "Upper outer squareness of the plasma boundary (definition from T. Luce, Plasma Phys. Control. Fusion 55 (2013) 095009)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.squareness_upper_outer.clone() },
                )
            },
        }),
    },
    Node {
        name: "squareness_lower_inner",
        documentation: "Lower inner squareness of the plasma boundary (definition from T. Luce, Plasma Phys. Control. Fusion 55 (2013) 095009)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.squareness_lower_inner.clone() },
                )
            },
        }),
    },
    Node {
        name: "squareness_lower_outer",
        documentation: "Lower outer squareness of the plasma boundary (definition from T. Luce, Plasma Phys. Control. Fusion 55 (2013) 095009)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.squareness_lower_outer.clone() },
                )
            },
        }),
    },
    Node {
        name: "closest_wall_point",
        documentation: "Position and distance to the plasma boundary of the point of the first wall which is the closest to plasma boundary",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_BOUNDARY_CLOSEST_WALL_POINT),
    },
    Node {
        name: "dr_dz_zero_point",
        documentation: "Outboard point on the separatrix on which dr/dz = 0 (local maximum of the major radius of the separatrix). In case of multiple local maxima, the closest one from z=z_magnetic_axis is chosen.",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_BOUNDARY_DR_DZ_ZERO_POINT),
    },
    Node {
        name: "gap",
        documentation: "Set of gaps, defined by a reference point and a direction.",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_BOUNDARY_GAP),
    },
    Node {
        name: "rho_tor",
        documentation: "Toroidal flux coordinate at the selected plasma boundary",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.rho_tor.clone() },
                )
            },
        }),
    },
    Node {
        name: "phi",
        documentation: "Toroidal flux at the selected plasma boundary. Positive when the toroidal magnetic field is counter-clockwise when viewed from above",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.phi.clone() },
                )
            },
        }),
    },
    Node {
        name: "phi_poloidal_current",
        documentation: "Toroidal flux at the selected plasma boundary generated by the plasma poloidal current. Positive when the toroidal magnetic field is counter-clockwise when viewed from above",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.boundary.phi_poloidal_current.clone() },
                )
            },
        }),
    },
    Node {
        name: "bounding",
        documentation: "Point which defines the plasma boundary: the limiter point when the plasma is limited, or
the X-point when it is diverted",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_BOUNDARY_BOUNDING),
    },
];

static NODES_TIME_SLICE_CONTOUR_TREE_NODE_NODE_TYPE: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_contour_tree_node,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.contour_tree.node.get(at[1])?.node_type.name.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_contour_tree_node,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.contour_tree.node.get(at[1])?.node_type.index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_contour_tree_node,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.contour_tree.node.get(at[1])?.node_type.description.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONTOUR_TREE_NODE_LEVELSET: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_contour_tree_node_levelset,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.contour_tree.node.get(at[1])?.levelset.get(at[2])?.r.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_contour_tree_node_levelset,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.contour_tree.node.get(at[1])?.levelset.get(at[2])?.z.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONTOUR_TREE_NODE: &[Node] = &[
    Node {
        name: "critical_type",
        documentation: "Critical-point type of the poloidal flux: 0 = local minimum, 1 = saddle (X-point), 2 = local maximum, 3 = limiter point. Whether the magnetic axis is a minimum or maximum of psi depends on the sign of the plasma current. A limiter point represents a point on the first wall where the plasma boundary makes contact. Limiter point nodes are inserted by splitting an existing tree edge at the limiter point's psi level; they have degree 2 in the tree. A limiter point is valid only if it belongs to the same connected component as at least one O-point in the subgraph obtained by removing all saddle nodes (critical_type = 1). Limiter points store the last closed flux surface passing through the limiter point in their levelset",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_contour_tree_node,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.contour_tree.node.get(at[1])?.critical_type.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "node_type",
        documentation: "Identifies whether this node represents a confined plasma feature or a vacuum field feature. Only meaningful for O-point nodes (critical_type 0 or 2). Limiter points are always plasma features. X-points and saddle points do not require classification",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONTOUR_TREE_NODE_NODE_TYPE),
    },
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_contour_tree_node,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.contour_tree.node.get(at[1])?.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_contour_tree_node,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.contour_tree.node.get(at[1])?.z.clone() },
                )
            },
        }),
    },
    Node {
        name: "psi",
        documentation: "Value of the poloidal flux at the node location. Whether psi increases or decreases from the magnetic axis outward depends on the sign of the plasma current. All ordering rules in the contour tree (node ordering, levelset segment ordering) are defined in terms of normalised poloidal flux, psi_norm = (psi - psi_axis) / (psi_boundary - psi_axis), which always increases from 0 at the magnetic axis to 1 at the plasma boundary",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_contour_tree_node,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.contour_tree.node.get(at[1])?.psi.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "levelset",
        documentation: "Poloidal flux contour segments at the node's psi value that are topologically connected to the node. Each element of the array of structures stores one distinct contour segment. Only segments whose contour passes through the node are included; disconnected contours that share the same psi value but are spatially remote must be excluded. For O-points (flux extrema), no contour passes through the extremum and this field must contain a single entry with empty r and z arrays. For limiter points, the levelset contains the last closed flux surface (LCFS) that passes through the limiter point. For X-points, the segments are ordered as follows: segment 0 is the last closed flux surface (LCFS), the closed contour bounding the confined plasma that passes through the X-point. Segments 1..N are divertor or scrape-off layer leg contours, ordered by their departure angle from the X-point measured counterclockwise from the outboard midplane (theta=0 at R_max, Z of the X-point) in the standard (R, Z) poloidal plane. A standard single-null divertor (SND) X-point has 2 segments: the LCFS and the divertor legs. A double-null divertor (DND) has 2 segments per X-point; the primary X-point (lower psi_norm, closer to the magnetic axis) is stored at a lower node index than the secondary, so their levelsets are unambiguously associated. An exact snowflake, where two X-points merge into a single higher-order null with 6 separatrix branches, produces 3 segments: the LCFS and 2 distinct divertor channel contours.",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONTOUR_TREE_NODE_LEVELSET),
    },
];

static NODES_TIME_SLICE_CONTOUR_TREE: &[Node] = &[
    Node {
        name: "node",
        documentation: "Set of critical points and limiter points of the poloidal flux. Each node is defined by its critical type (see critical_type), node type (see node_type), and position within the poloidal plane. All ordering rules below are defined in terms of normalised poloidal flux, psi_norm = (psi - psi_axis) / (psi_boundary - psi_axis), which is 0 at the magnetic axis and 1 at the plasma boundary. Nodes are partitioned into two groups: critical points (critical_type 0, 1, 2) occupy the leading indices, followed by limiter points (critical_type 3). Within each partition, nodes are ordered by ascending psi_norm; ties are broken by ascending R, then ascending Z. Node 0 must contain the primary O-point (magnetic axis), which has psi_norm = 0. Node 1 must contain the primary X-point, defined as the X-point with the lowest psi_norm value. For double-null divertor (DND) configurations, the secondary X-point has a higher psi_norm value and must be stored at a higher node index than the primary; the two psi_norm values are always distinct in practice. For limiter-bounded plasmas where no X-point defines the LCFS, node 1 should contain the X-point with the lowest psi_norm value (nearest to the plasma boundary), if any X-point exists. If no X-points exist, nodes are ordered by ascending psi_norm starting from node 1. For doublet or multi-region plasmas, each confined plasma region has its own O-point; additional O-points follow the same ascending psi_norm ordering. When limiter points are absent, only the critical point partition is present and the ordering is unchanged from earlier versions. The node_type identifier distinguishes O-points that represent confined plasma regions (node_type = plasma) from vacuum field extrema (node_type = vacuum).",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONTOUR_TREE_NODE),
    },
    Node {
        name: "edges",
        documentation: "Edges encode the Reeb graph (contour tree) of the poloidal flux by connecting topologically adjacent nodes. For each edge (1st dimension), the indices of the two connected nodes are listed (indices referring to the ../node array). An edge (i, j) indicates that sweeping the contour level continuously between the psi values at nodes i and j encounters no other critical point or limiter point. Limiter point nodes participate in edges like any other node; they are inserted by splitting an existing edge at the limiter point's psi level, replacing one edge with two. The number of edges equals n_nodes minus the number of connected components of the graph. For simply-connected 2D domains the graph is a tree with n_edges = n_nodes - 1. For doublet or multi-island configurations the graph may be a forest of disconnected trees. Each edge pair must be stored with the lower node index first (edges[k, 0] < edges[k, 1]).",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_2D> { equilibrium.time_slice.get(at[0])?.contour_tree.edges.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_B_FIELD_TOR_VACUUM_R: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.b_field_tor_vacuum_r.measured.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.b_field_tor_vacuum_r.source.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.b_field_tor_vacuum_r.time_measurement.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.b_field_tor_vacuum_r.exact.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.b_field_tor_vacuum_r.weight.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.b_field_tor_vacuum_r.sigma.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.b_field_tor_vacuum_r.reconstructed.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.b_field_tor_vacuum_r.chi_squared.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_B_FIELD_POL_PROBE: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_b_field_pol_probe,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.b_field_pol_probe.get(at[1])?.measured.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_b_field_pol_probe,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.b_field_pol_probe.get(at[1])?.source.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_b_field_pol_probe,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .b_field_pol_probe
                            .get(at[1])?
                            .time_measurement
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_b_field_pol_probe,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.b_field_pol_probe.get(at[1])?.exact.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_b_field_pol_probe,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.b_field_pol_probe.get(at[1])?.weight.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_b_field_pol_probe,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.b_field_pol_probe.get(at[1])?.sigma.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_b_field_pol_probe,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .b_field_pol_probe
                            .get(at[1])?
                            .reconstructed
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_b_field_pol_probe,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.b_field_pol_probe.get(at[1])?.chi_squared.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_DIAMAGNETIC_FLUX: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.diamagnetic_flux.measured.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.diamagnetic_flux.source.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.diamagnetic_flux.time_measurement.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.diamagnetic_flux.exact.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.diamagnetic_flux.weight.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.diamagnetic_flux.sigma.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.diamagnetic_flux.reconstructed.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.diamagnetic_flux.chi_squared.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_FARADAY_ANGLE: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_faraday_angle,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.faraday_angle.get(at[1])?.measured.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_faraday_angle,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.faraday_angle.get(at[1])?.source.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_faraday_angle,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .faraday_angle
                            .get(at[1])?
                            .time_measurement
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_faraday_angle,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.faraday_angle.get(at[1])?.exact.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_faraday_angle,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.faraday_angle.get(at[1])?.weight.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_faraday_angle,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.faraday_angle.get(at[1])?.sigma.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_faraday_angle,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.faraday_angle.get(at[1])?.reconstructed.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_faraday_angle,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.faraday_angle.get(at[1])?.chi_squared.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_MSE_POLARIZATION_ANGLE: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_mse_polarization_angle,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .mse_polarization_angle
                            .get(at[1])?
                            .measured
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_mse_polarization_angle,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.mse_polarization_angle.get(at[1])?.source.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_mse_polarization_angle,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .mse_polarization_angle
                            .get(at[1])?
                            .time_measurement
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_mse_polarization_angle,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.mse_polarization_angle.get(at[1])?.exact.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_mse_polarization_angle,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.mse_polarization_angle.get(at[1])?.weight.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_mse_polarization_angle,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.mse_polarization_angle.get(at[1])?.sigma.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_mse_polarization_angle,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .mse_polarization_angle
                            .get(at[1])?
                            .reconstructed
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_mse_polarization_angle,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .mse_polarization_angle
                            .get(at[1])?
                            .chi_squared
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_FLUX_LOOP: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_flux_loop,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.flux_loop.get(at[1])?.measured.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_flux_loop,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.flux_loop.get(at[1])?.source.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_flux_loop,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.flux_loop.get(at[1])?.time_measurement.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_flux_loop,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.flux_loop.get(at[1])?.exact.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_flux_loop,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.flux_loop.get(at[1])?.weight.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_flux_loop,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.flux_loop.get(at[1])?.sigma.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_flux_loop,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.flux_loop.get(at[1])?.reconstructed.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_flux_loop,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.flux_loop.get(at[1])?.chi_squared.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_IP: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.constraints.ip.measured.clone() },
                )
            },
        }),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> { equilibrium.time_slice.get(at[0])?.constraints.ip.source.clone() },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.constraints.ip.time_measurement.clone() },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> { equilibrium.time_slice.get(at[0])?.constraints.ip.exact.clone() },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.constraints.ip.weight.clone() },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.constraints.ip.sigma.clone() },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.constraints.ip.reconstructed.clone() },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.constraints.ip.chi_squared.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_IRON_CORE_SEGMENT_MAGNETIZATION_R: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_iron_core_segment,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .iron_core_segment
                            .get(at[1])?
                            .magnetization_r
                            .measured
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_iron_core_segment,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .iron_core_segment
                            .get(at[1])?
                            .magnetization_r
                            .source
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_iron_core_segment,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .iron_core_segment
                            .get(at[1])?
                            .magnetization_r
                            .time_measurement
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_iron_core_segment,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .iron_core_segment
                            .get(at[1])?
                            .magnetization_r
                            .exact
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_iron_core_segment,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .iron_core_segment
                            .get(at[1])?
                            .magnetization_r
                            .weight
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_iron_core_segment,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .iron_core_segment
                            .get(at[1])?
                            .magnetization_r
                            .sigma
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_iron_core_segment,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .iron_core_segment
                            .get(at[1])?
                            .magnetization_r
                            .reconstructed
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_iron_core_segment,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .iron_core_segment
                            .get(at[1])?
                            .magnetization_r
                            .chi_squared
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_IRON_CORE_SEGMENT_MAGNETIZATION_Z: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_iron_core_segment,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .iron_core_segment
                            .get(at[1])?
                            .magnetization_z
                            .measured
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_iron_core_segment,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .iron_core_segment
                            .get(at[1])?
                            .magnetization_z
                            .source
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_iron_core_segment,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .iron_core_segment
                            .get(at[1])?
                            .magnetization_z
                            .time_measurement
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_iron_core_segment,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .iron_core_segment
                            .get(at[1])?
                            .magnetization_z
                            .exact
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_iron_core_segment,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .iron_core_segment
                            .get(at[1])?
                            .magnetization_z
                            .weight
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_iron_core_segment,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .iron_core_segment
                            .get(at[1])?
                            .magnetization_z
                            .sigma
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_iron_core_segment,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .iron_core_segment
                            .get(at[1])?
                            .magnetization_z
                            .reconstructed
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_iron_core_segment,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .iron_core_segment
                            .get(at[1])?
                            .magnetization_z
                            .chi_squared
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_IRON_CORE_SEGMENT: &[Node] = &[
    Node {
        name: "magnetization_r",
        documentation: "Magnetization M of the iron core segment along the major radius axis, assumed to be constant inside a given iron segment. Reminder : H = 1/mu0 * B - mur * M;",
        units: "T",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONSTRAINTS_IRON_CORE_SEGMENT_MAGNETIZATION_R),
    },
    Node {
        name: "magnetization_z",
        documentation: "Magnetization M of the iron core segment along the vertical axis, assumed to be constant inside a given iron segment. Reminder : H = 1/mu0 * B - mur * M;",
        units: "T",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONSTRAINTS_IRON_CORE_SEGMENT_MAGNETIZATION_Z),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_N_E_POSITION: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e.get(at[1])?.position.r.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "phi",
        documentation: "Toroidal angle (oriented counter-clockwise when viewed from above)",
        units: "rad",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e.get(at[1])?.position.phi.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e.get(at[1])?.position.z.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "rho_tor_norm",
        documentation: "Normalized toroidal flux coordinate. The normalizing value for rho_tor_norm, is the toroidal flux coordinate at the equilibrium boundary (LCFS or 99.x % of the LCFS in case of a fixed boundary equilibium calculation, see time_slice/boundary/b_flux_pol_norm in the equilibrium IDS)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e.get(at[1])?.position.rho_tor_norm.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "psi",
        documentation: "Poloidal magnetic flux. For a positive plasma current (counter-clockwise when viewed from above), increases from the magnetic axis to the boundary",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e.get(at[1])?.position.psi.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_N_E: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e.get(at[1])?.measured.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "position",
        documentation: "Position at which this measurement is given",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONSTRAINTS_N_E_POSITION),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e.get(at[1])?.source.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e.get(at[1])?.time_measurement.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e.get(at[1])?.exact.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e.get(at[1])?.weight.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e.get(at[1])?.sigma.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e.get(at[1])?.reconstructed.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e.get(at[1])?.chi_squared.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_N_E_LINE: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e_line,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e_line.get(at[1])?.measured.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e_line,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e_line.get(at[1])?.source.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e_line,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e_line.get(at[1])?.time_measurement.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e_line,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e_line.get(at[1])?.exact.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e_line,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e_line.get(at[1])?.weight.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e_line,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e_line.get(at[1])?.sigma.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e_line,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e_line.get(at[1])?.reconstructed.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_n_e_line,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.n_e_line.get(at[1])?.chi_squared.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_PF_CURRENT: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pf_current,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pf_current.get(at[1])?.measured.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pf_current,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pf_current.get(at[1])?.source.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pf_current,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pf_current.get(at[1])?.time_measurement.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pf_current,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pf_current.get(at[1])?.exact.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pf_current,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pf_current.get(at[1])?.weight.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pf_current,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pf_current.get(at[1])?.sigma.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pf_current,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pf_current.get(at[1])?.reconstructed.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pf_current,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pf_current.get(at[1])?.chi_squared.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_PF_PASSIVE_CURRENT: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pf_passive_current,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pf_passive_current.get(at[1])?.measured.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pf_passive_current,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pf_passive_current.get(at[1])?.source.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pf_passive_current,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .pf_passive_current
                            .get(at[1])?
                            .time_measurement
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pf_passive_current,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pf_passive_current.get(at[1])?.exact.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pf_passive_current,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pf_passive_current.get(at[1])?.weight.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pf_passive_current,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pf_passive_current.get(at[1])?.sigma.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pf_passive_current,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .pf_passive_current
                            .get(at[1])?
                            .reconstructed
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pf_passive_current,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .pf_passive_current
                            .get(at[1])?
                            .chi_squared
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_PRESSURE_POSITION: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pressure.get(at[1])?.position.r.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "phi",
        documentation: "Toroidal angle (oriented counter-clockwise when viewed from above)",
        units: "rad",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pressure.get(at[1])?.position.phi.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pressure.get(at[1])?.position.z.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "rho_tor_norm",
        documentation: "Normalized toroidal flux coordinate. The normalizing value for rho_tor_norm, is the toroidal flux coordinate at the equilibrium boundary (LCFS or 99.x % of the LCFS in case of a fixed boundary equilibium calculation, see time_slice/boundary/b_flux_pol_norm in the equilibrium IDS)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .pressure
                            .get(at[1])?
                            .position
                            .rho_tor_norm
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "psi",
        documentation: "Poloidal magnetic flux. For a positive plasma current (counter-clockwise when viewed from above), increases from the magnetic axis to the boundary",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pressure.get(at[1])?.position.psi.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_PRESSURE: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pressure.get(at[1])?.measured.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "position",
        documentation: "Position at which this measurement is given",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONSTRAINTS_PRESSURE_POSITION),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pressure.get(at[1])?.source.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pressure.get(at[1])?.time_measurement.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pressure.get(at[1])?.exact.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pressure.get(at[1])?.weight.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pressure.get(at[1])?.sigma.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pressure.get(at[1])?.reconstructed.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pressure.get(at[1])?.chi_squared.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_PRESSURE_ROTATIONAL_POSITION: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure_rotational,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .pressure_rotational
                            .get(at[1])?
                            .position
                            .r
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "phi",
        documentation: "Toroidal angle (oriented counter-clockwise when viewed from above)",
        units: "rad",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure_rotational,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .pressure_rotational
                            .get(at[1])?
                            .position
                            .phi
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure_rotational,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .pressure_rotational
                            .get(at[1])?
                            .position
                            .z
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "rho_tor_norm",
        documentation: "Normalized toroidal flux coordinate. The normalizing value for rho_tor_norm, is the toroidal flux coordinate at the equilibrium boundary (LCFS or 99.x % of the LCFS in case of a fixed boundary equilibium calculation, see time_slice/boundary/b_flux_pol_norm in the equilibrium IDS)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure_rotational,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .pressure_rotational
                            .get(at[1])?
                            .position
                            .rho_tor_norm
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "psi",
        documentation: "Poloidal magnetic flux. For a positive plasma current (counter-clockwise when viewed from above), increases from the magnetic axis to the boundary",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure_rotational,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .pressure_rotational
                            .get(at[1])?
                            .position
                            .psi
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_PRESSURE_ROTATIONAL: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure_rotational,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pressure_rotational.get(at[1])?.measured.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "position",
        documentation: "Position at which this measurement is given",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONSTRAINTS_PRESSURE_ROTATIONAL_POSITION),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure_rotational,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pressure_rotational.get(at[1])?.source.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure_rotational,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .pressure_rotational
                            .get(at[1])?
                            .time_measurement
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure_rotational,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pressure_rotational.get(at[1])?.exact.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure_rotational,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pressure_rotational.get(at[1])?.weight.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure_rotational,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.pressure_rotational.get(at[1])?.sigma.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure_rotational,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .pressure_rotational
                            .get(at[1])?
                            .reconstructed
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_pressure_rotational,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .pressure_rotational
                            .get(at[1])?
                            .chi_squared
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_Q_POSITION: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_q,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.q.get(at[1])?.position.r.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "phi",
        documentation: "Toroidal angle (oriented counter-clockwise when viewed from above)",
        units: "rad",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_q,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.q.get(at[1])?.position.phi.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_q,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.q.get(at[1])?.position.z.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "rho_tor_norm",
        documentation: "Normalized toroidal flux coordinate. The normalizing value for rho_tor_norm, is the toroidal flux coordinate at the equilibrium boundary (LCFS or 99.x % of the LCFS in case of a fixed boundary equilibium calculation, see time_slice/boundary/b_flux_pol_norm in the equilibrium IDS)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_q,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.q.get(at[1])?.position.rho_tor_norm.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "psi",
        documentation: "Poloidal magnetic flux. For a positive plasma current (counter-clockwise when viewed from above), increases from the magnetic axis to the boundary",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_q,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.q.get(at[1])?.position.psi.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_Q: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_q,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.q.get(at[1])?.measured.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "position",
        documentation: "Position at which this measurement is given",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONSTRAINTS_Q_POSITION),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_q,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> { equilibrium.time_slice.get(at[0])?.constraints.q.get(at[1])?.source.clone() },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_q,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.q.get(at[1])?.time_measurement.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_q,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> { equilibrium.time_slice.get(at[0])?.constraints.q.get(at[1])?.exact.clone() },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_q,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.constraints.q.get(at[1])?.weight.clone() },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_q,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.constraints.q.get(at[1])?.sigma.clone() },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_q,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.q.get(at[1])?.reconstructed.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_q,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.q.get(at[1])?.chi_squared.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_J_PHI_POSITION: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_phi.get(at[1])?.position.r.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "phi",
        documentation: "Toroidal angle (oriented counter-clockwise when viewed from above)",
        units: "rad",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_phi.get(at[1])?.position.phi.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_phi.get(at[1])?.position.z.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "rho_tor_norm",
        documentation: "Normalized toroidal flux coordinate. The normalizing value for rho_tor_norm, is the toroidal flux coordinate at the equilibrium boundary (LCFS or 99.x % of the LCFS in case of a fixed boundary equilibium calculation, see time_slice/boundary/b_flux_pol_norm in the equilibrium IDS)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_phi.get(at[1])?.position.rho_tor_norm.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "psi",
        documentation: "Poloidal magnetic flux. For a positive plasma current (counter-clockwise when viewed from above), increases from the magnetic axis to the boundary",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_phi.get(at[1])?.position.psi.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_J_PHI: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_phi.get(at[1])?.measured.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "position",
        documentation: "Position at which this measurement is given",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONSTRAINTS_J_PHI_POSITION),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_phi.get(at[1])?.source.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_phi.get(at[1])?.time_measurement.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_phi.get(at[1])?.exact.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_phi.get(at[1])?.weight.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_phi.get(at[1])?.sigma.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_phi.get(at[1])?.reconstructed.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_phi.get(at[1])?.chi_squared.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_J_PARALLEL_POSITION: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_parallel,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_parallel.get(at[1])?.position.r.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "phi",
        documentation: "Toroidal angle (oriented counter-clockwise when viewed from above)",
        units: "rad",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_parallel,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_parallel.get(at[1])?.position.phi.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_parallel,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_parallel.get(at[1])?.position.z.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "rho_tor_norm",
        documentation: "Normalized toroidal flux coordinate. The normalizing value for rho_tor_norm, is the toroidal flux coordinate at the equilibrium boundary (LCFS or 99.x % of the LCFS in case of a fixed boundary equilibium calculation, see time_slice/boundary/b_flux_pol_norm in the equilibrium IDS)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_parallel,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .j_parallel
                            .get(at[1])?
                            .position
                            .rho_tor_norm
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "psi",
        documentation: "Poloidal magnetic flux. For a positive plasma current (counter-clockwise when viewed from above), increases from the magnetic axis to the boundary",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_parallel,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_parallel.get(at[1])?.position.psi.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_J_PARALLEL: &[Node] = &[
    Node {
        name: "measured",
        documentation: "Measured value",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_parallel,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_parallel.get(at[1])?.measured.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "position",
        documentation: "Position at which this measurement is given",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONSTRAINTS_J_PARALLEL_POSITION),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_parallel,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_parallel.get(at[1])?.source.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_parallel,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_parallel.get(at[1])?.time_measurement.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_parallel,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_parallel.get(at[1])?.exact.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_parallel,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_parallel.get(at[1])?.weight.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_parallel,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_parallel.get(at[1])?.sigma.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "reconstructed",
        documentation: "Value calculated from the reconstructed equilibrium",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_parallel,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_parallel.get(at[1])?.reconstructed.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared",
        documentation: "Squared error normalized by the variance considered in the minimization process : chi_squared = weight^2 *(reconstructed - measured)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_j_parallel,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.j_parallel.get(at[1])?.chi_squared.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_X_POINT_POSITION_MEASURED: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_x_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.x_point.get(at[1])?.position_measured.r.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_x_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.x_point.get(at[1])?.position_measured.z.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_X_POINT_POSITION_RECONSTRUCTED: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_x_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .x_point
                            .get(at[1])?
                            .position_reconstructed
                            .r
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_x_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .x_point
                            .get(at[1])?
                            .position_reconstructed
                            .z
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_X_POINT: &[Node] = &[
    Node {
        name: "position_measured",
        documentation: "Measured or estimated position",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONSTRAINTS_X_POINT_POSITION_MEASURED),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_x_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.x_point.get(at[1])?.source.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_x_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.x_point.get(at[1])?.time_measurement.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_x_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.x_point.get(at[1])?.exact.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_x_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.x_point.get(at[1])?.weight.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_x_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.x_point.get(at[1])?.sigma.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "position_reconstructed",
        documentation: "Position estimated from the reconstructed equilibrium",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONSTRAINTS_X_POINT_POSITION_RECONSTRUCTED),
    },
    Node {
        name: "chi_squared_r",
        documentation: "Squared error on the major radius normalized by the variance considered in the minimization process : chi_squared = weight^2 *(position_reconstructed/r - position_measured/r)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_x_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.x_point.get(at[1])?.chi_squared_r.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared_z",
        documentation: "Squared error on the altitude normalized by the variance considered in the minimization process : chi_squared = weight^2 *(position_reconstructed/z - position_measured/z)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_x_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.x_point.get(at[1])?.chi_squared_z.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_STRIKE_POINT_POSITION_MEASURED: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_strike_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .strike_point
                            .get(at[1])?
                            .position_measured
                            .r
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_strike_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .strike_point
                            .get(at[1])?
                            .position_measured
                            .z
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_STRIKE_POINT_POSITION_RECONSTRUCTED: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_strike_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .strike_point
                            .get(at[1])?
                            .position_reconstructed
                            .r
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_strike_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .constraints
                            .strike_point
                            .get(at[1])?
                            .position_reconstructed
                            .z
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS_STRIKE_POINT: &[Node] = &[
    Node {
        name: "position_measured",
        documentation: "Measured or estimated position",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONSTRAINTS_STRIKE_POINT_POSITION_MEASURED),
    },
    Node {
        name: "source",
        documentation: "Path to the source data for this measurement in the IMAS data dictionary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_strike_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.strike_point.get(at[1])?.source.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "time_measurement",
        documentation: "Exact time slice used from the time array of the measurement source data. If the time slice does not exist in the time array of the source data, it means linear interpolation has been used",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_strike_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.strike_point.get(at[1])?.time_measurement.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "exact",
        documentation: "Integer flag : 1 means exact data, taken as an exact input without being fitted; 0 means the equilibrium code does a least square fit",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_strike_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.strike_point.get(at[1])?.exact.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "weight",
        documentation: "Weight given to the measurement",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_strike_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.strike_point.get(at[1])?.weight.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "sigma",
        documentation: "Standard deviation of the measurement error",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_strike_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.strike_point.get(at[1])?.sigma.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "position_reconstructed",
        documentation: "Position estimated from the reconstructed equilibrium",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONSTRAINTS_STRIKE_POINT_POSITION_RECONSTRUCTED),
    },
    Node {
        name: "chi_squared_r",
        documentation: "Squared error on the major radius normalized by the variance considered in the minimization process : chi_squared = weight^2 *(position_reconstructed/r - position_measured/r)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_strike_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.strike_point.get(at[1])?.chi_squared_r.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "chi_squared_z",
        documentation: "Squared error on the altitude normalized by the variance considered in the minimization process : chi_squared = weight^2 *(position_reconstructed/z - position_measured/z)^2 / sigma^2, where sigma is the standard deviation of the measurement error",
        units: "m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_constraints_strike_point,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.constraints.strike_point.get(at[1])?.chi_squared_z.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONSTRAINTS: &[Node] = &[
    Node {
        name: "b_field_tor_vacuum_r",
        documentation: "Vacuum field times major radius in the toroidal field magnet. Positive sign means counter-clockwise when viewed from above",
        units: "T.m",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONSTRAINTS_B_FIELD_TOR_VACUUM_R),
    },
    Node {
        name: "b_field_pol_probe",
        documentation: "Set of poloidal field probes",
        units: "T",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONSTRAINTS_B_FIELD_POL_PROBE),
    },
    Node {
        name: "diamagnetic_flux",
        documentation: "Diamagnetic flux",
        units: "Wb",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONSTRAINTS_DIAMAGNETIC_FLUX),
    },
    Node {
        name: "faraday_angle",
        documentation: "Set of faraday angles",
        units: "rad",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONSTRAINTS_FARADAY_ANGLE),
    },
    Node {
        name: "mse_polarization_angle",
        documentation: "Set of MSE polarization angles",
        units: "rad",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONSTRAINTS_MSE_POLARIZATION_ANGLE),
    },
    Node {
        name: "flux_loop",
        documentation: "Set of flux loops",
        units: "Wb",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONSTRAINTS_FLUX_LOOP),
    },
    Node {
        name: "ip",
        documentation: "Plasma current. Positive sign means counter-clockwise when viewed from above",
        units: "A",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONSTRAINTS_IP),
    },
    Node {
        name: "iron_core_segment",
        documentation: "Magnetization M of a set of iron core segments",
        units: "T",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONSTRAINTS_IRON_CORE_SEGMENT),
    },
    Node {
        name: "n_e",
        documentation: "Set of local density measurements",
        units: "m^-3",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONSTRAINTS_N_E),
    },
    Node {
        name: "n_e_line",
        documentation: "Set of line integrated density measurements",
        units: "m^-2",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONSTRAINTS_N_E_LINE),
    },
    Node {
        name: "pf_current",
        documentation: "Current in a set of poloidal field coils",
        units: "A",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONSTRAINTS_PF_CURRENT),
    },
    Node {
        name: "pf_passive_current",
        documentation: "Current in a set of axisymmetric passive conductors",
        units: "A",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONSTRAINTS_PF_PASSIVE_CURRENT),
    },
    Node {
        name: "pressure",
        documentation: "Set of total pressure estimates",
        units: "Pa",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONSTRAINTS_PRESSURE),
    },
    Node {
        name: "pressure_rotational",
        documentation: "Set of rotational pressure estimates. The rotational pressure is defined as R0^2*rho*omega^2 / 2, where omega is the toroidal rotation frequency, rho=ne(R0,psi)*m, and m is the plasma equivalent mass.",
        units: "Pa",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONSTRAINTS_PRESSURE_ROTATIONAL),
    },
    Node {
        name: "q",
        documentation: "Set of safety factor estimates at various positions",
        units: "1",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONSTRAINTS_Q),
    },
    Node {
        name: "j_phi",
        documentation: "Set of flux-surface averaged toroidal current density approximations at various positions  (= average(j_tor/R) / average(1/R))",
        units: "A.m^-2",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONSTRAINTS_J_PHI),
    },
    Node {
        name: "j_parallel",
        documentation: "Set of flux-surface averaged parallel current density approximations at various positions (= average(j.B) / B0, where B0 = /vacuum_toroidal_field/b0)",
        units: "A.m^-2",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONSTRAINTS_J_PARALLEL),
    },
    Node {
        name: "x_point",
        documentation: "Array of X-points, for each of them the RZ position is given",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONSTRAINTS_X_POINT),
    },
    Node {
        name: "strike_point",
        documentation: "Array of strike points, for each of them the RZ position is given",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_CONSTRAINTS_STRIKE_POINT),
    },
    Node {
        name: "chi_squared_reduced",
        documentation: "Sum of the chi_squared of all constraints used for the equilibrium reconstruction, divided by the number of degrees of freedom of the identification model",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.constraints.chi_squared_reduced.clone() },
                )
            },
        }),
    },
    Node {
        name: "freedom_degrees_n",
        documentation: "Number of degrees of freedom of the identification model",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> { equilibrium.time_slice.get(at[0])?.constraints.freedom_degrees_n.clone() },
                )
            },
        }),
    },
    Node {
        name: "constraints_n",
        documentation: "Number of constraints used (i.e. having a non-zero weight)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> { equilibrium.time_slice.get(at[0])?.constraints.constraints_n.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_GLOBAL_QUANTITIES_MAGNETIC_AXIS: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius of the magnetic axis",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.global_quantities.magnetic_axis.r.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height of the magnetic axis",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.global_quantities.magnetic_axis.z.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "b_field_phi",
        documentation: "Total toroidal magnetic field at the magnetic axis. Positive sign means counter-clockwise when viewed from above",
        units: "T",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.global_quantities.magnetic_axis.b_field_phi.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_GLOBAL_QUANTITIES_CURRENT_CENTRE: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius of the current center, defined as integral over the poloidal cross section of (j_tor*r*dS) / Ip",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.global_quantities.current_centre.r.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height of the current center, defined as integral over the poloidal cross section of (j_tor*z*dS) / Ip",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.global_quantities.current_centre.z.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "velocity_z",
        documentation: "Vertical velocity of the current center",
        units: "m.s^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.global_quantities.current_centre.velocity_z.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_GLOBAL_QUANTITIES_Q_MIN: &[Node] = &[
    Node {
        name: "value",
        documentation: "Minimum q value. Positive when toroidal current and toroidal magnetic field are in the same direction",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.q_min.value.clone() },
                )
            },
        }),
    },
    Node {
        name: "rho_tor_norm",
        documentation: "Minimum q position in normalized toroidal flux coordinate",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.global_quantities.q_min.rho_tor_norm.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "psi_norm",
        documentation: "Minimum q position in normalised poloidal flux",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.q_min.psi_norm.clone() },
                )
            },
        }),
    },
    Node {
        name: "psi",
        documentation: "Minimum q position in poloidal flux. For a positive plasma current (counter-clockwise when viewed from above), increases from the magnetic axis to the boundary",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.q_min.psi.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_GLOBAL_QUANTITIES: &[Node] = &[
    Node {
        name: "beta_pol",
        documentation: "Poloidal beta. Defined as betap = 4 int(p dV) / [R_0 * mu_0 * Ip^2]",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.beta_pol.clone() },
                )
            },
        }),
    },
    Node {
        name: "beta_tor",
        documentation: "Toroidal beta, defined as the volume-averaged total perpendicular pressure divided by (B0^2/(2*mu0)), i.e. beta_toroidal = 2 mu0 int(p dV) / V / B0^2",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.beta_tor.clone() },
                )
            },
        }),
    },
    Node {
        name: "beta_tor_norm",
        documentation: "Normalized toroidal beta, defined as 100 * beta_tor * a[m] * B0 [T] / ip [MA]",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.beta_tor_norm.clone() },
                )
            },
        }),
    },
    Node {
        name: "ip",
        documentation: "Plasma current (toroidal component). Positive sign means counter-clockwise when viewed from above.",
        units: "A",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.ip.clone() },
                )
            },
        }),
    },
    Node {
        name: "li_3",
        documentation: "Internal inductance",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.li_3.clone() },
                )
            },
        }),
    },
    Node {
        name: "volume",
        documentation: "Total plasma volume",
        units: "m^3",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.volume.clone() },
                )
            },
        }),
    },
    Node {
        name: "area",
        documentation: "Area of the LCFS poloidal cross section",
        units: "m^2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.area.clone() },
                )
            },
        }),
    },
    Node {
        name: "surface",
        documentation: "Surface area of the toroidal flux surface",
        units: "m^2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.surface.clone() },
                )
            },
        }),
    },
    Node {
        name: "length_pol",
        documentation: "Poloidal length of the magnetic surface",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.length_pol.clone() },
                )
            },
        }),
    },
    Node {
        name: "psi_magnetic_axis",
        documentation: "Poloidal flux at the magnetic axis. For a positive plasma current (counter-clockwise when viewed from above), increases from the magnetic axis to the boundary",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.global_quantities.psi_magnetic_axis.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "magnetic_axis",
        documentation: "Magnetic axis position and toroidal field",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_GLOBAL_QUANTITIES_MAGNETIC_AXIS),
    },
    Node {
        name: "current_centre",
        documentation: "Position and vertical velocity of the current centre",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_GLOBAL_QUANTITIES_CURRENT_CENTRE),
    },
    Node {
        name: "q_axis",
        documentation: "q at the magnetic axis. Positive when toroidal current and toroidal magnetic field are in the same direction",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.q_axis.clone() },
                )
            },
        }),
    },
    Node {
        name: "q_95",
        documentation: "q at the 95% poloidal flux surface (only positive when toroidal current and magnetic field are in same direction)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.q_95.clone() },
                )
            },
        }),
    },
    Node {
        name: "q_min",
        documentation: "Minimum q value and position",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_GLOBAL_QUANTITIES_Q_MIN),
    },
    Node {
        name: "energy_mhd",
        documentation: "Plasma energy content = 3/2 * int(p,dV) with p being the total pressure (thermal + fast particles) [J]. Time-dependent; Scalar",
        units: "J",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.energy_mhd.clone() },
                )
            },
        }),
    },
    Node {
        name: "psi_external_average",
        documentation: "Average (over the plasma poloidal cross section) plasma poloidal magnetic flux produced by all external circuits (CS and PF coils, eddy currents, VS in-vessel coils), given by the following formula : int(psi_external.j_tor.dS) / Ip. For a positive plasma current (counter-clockwise when viewed from above), increases from the magnetic axis to the boundary",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.global_quantities.psi_external_average.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "v_external",
        documentation: "External voltage, i.e. time derivative of psi_external_average (with a minus sign : - d_psi_external_average/d_time)",
        units: "V",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.v_external.clone() },
                )
            },
        }),
    },
    Node {
        name: "plasma_inductance",
        documentation: "Plasma inductance 2 E_magnetic/Ip^2, where E_magnetic = 1/2 * int(psi.j_tor.dS) (integral over the plasma poloidal cross-section)",
        units: "H",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.global_quantities.plasma_inductance.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "plasma_resistance",
        documentation: "Plasma resistance = int(e_field.j.dV) / Ip^2",
        units: "ohm",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.global_quantities.plasma_resistance.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "i_rod",
        documentation: "Current flowing in the central rod of the toroidal field coil. It sets the vacuum
toroidal field function `f_vac = mu_0 * i_rod / (2 * pi)`, which the diamagnetic loop
constraint is written against. Signed: a negative value is a reversed toroidal field",
        units: "A",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.i_rod.clone() },
                )
            },
        }),
    },
    Node {
        name: "beta_pol_1",
        documentation: "Poloidal beta normalised to the flux-surface-averaged poloidal field:
`2 * mu_0 * <p> / <<b_p ** 2>>`, where `<x>` is the volume average and `<<x>>` the
flux-surface average",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.beta_pol_1.clone() },
                )
            },
        }),
    },
    Node {
        name: "beta_pol_2",
        documentation: "Poloidal beta normalised to the magnetic axis major radius:
`4 * int(p dV) / (mu_0 * ip ** 2 * magnetic_axis/r)`",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.beta_pol_2.clone() },
                )
            },
        }),
    },
    Node {
        name: "beta_pol_3",
        documentation: "Poloidal beta normalised to the geometric major radius:
`4 * int(p dV) / (mu_0 * ip ** 2 * boundary/geometric_axis/r)`
This is the closest of the three to the data dictionary's own `beta_pol`, which uses the
vacuum toroidal field reference radius `vacuum_toroidal_field/r0` instead",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.beta_pol_3.clone() },
                )
            },
        }),
    },
    Node {
        name: "bt_vac_at_r_geo",
        documentation: "Vacuum toroidal magnetic field at the plasma geometric axis,
`mu_0 * i_rod / (2 * pi * boundary/geometric_axis/r)`. Distinct from
`vacuum_toroidal_field/b0`, which is evaluated at the fixed machine reference radius
`vacuum_toroidal_field/r0` rather than following the plasma",
        units: "T",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.global_quantities.bt_vac_at_r_geo.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "li_1",
        documentation: "Internal inductance normalised to the flux-surface-averaged poloidal field:
`<b_p ** 2> / <<b_p ** 2>>`, where `<x>` is the volume average and `<<x>>` the
flux-surface average",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.li_1.clone() },
                )
            },
        }),
    },
    Node {
        name: "li_2",
        documentation: "Internal inductance normalised to the magnetic axis major radius:
`2 * int(b_p ** 2 dV) / (mu_0 ** 2 * ip ** 2 * magnetic_axis/r)`
The data dictionary's own `li_3` is the same quantity normalised to
`boundary/geometric_axis/r` instead",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.li_2.clone() },
                )
            },
        }),
    },
    Node {
        name: "pressure_2d_sum",
        documentation: "Plain sum of `profiles_2d/pressure` over every grid cell. Not an integral: the cells are
not weighted by their volume, so this is a diagnostic of the 2D pressure rather than a
physical quantity. Kept because GSFit has always reported it as `global/p`",
        units: "Pa",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.global_quantities.pressure_2d_sum.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "v_loop",
        documentation: "Loop voltage at the plasma boundary, `-d(boundary/psi)/d(time)`, by finite differences over
the reconstruction times. Distinct from the data dictionary's `v_external`, which
differentiates `psi_external_average` instead",
        units: "V",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.global_quantities.v_loop.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_PROFILES_1D_GEOMETRIC_AXIS: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.geometric_axis.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.geometric_axis.z.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_PROFILES_1D: &[Node] = &[
    Node {
        name: "psi",
        documentation: "Poloidal flux. Integral of magnetic field passing through a contour defined by the intersection of a flux surface passing through the point of interest and a Z=constant plane. If the integration surface is flat, the surface normal vector is in the increasing vertical coordinate direction, Z, namely upwards.",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.psi.clone() },
                )
            },
        }),
    },
    Node {
        name: "psi_norm",
        documentation: "Normalised poloidal flux, namely (psi(rho)-psi(magnetic_axis)) / (psi(LCFS)-psi(magnetic_axis))",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.psi_norm.clone() },
                )
            },
        }),
    },
    Node {
        name: "phi",
        documentation: "Toroidal flux. Positive when the toroidal magnetic field is counter-clockwise when viewed from above",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.phi.clone() },
                )
            },
        }),
    },
    Node {
        name: "pressure",
        documentation: "Pressure",
        units: "Pa",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.pressure.clone() },
                )
            },
        }),
    },
    Node {
        name: "f",
        documentation: "Diamagnetic function (F=R B_Phi). Positive when the toroidal field is counter-clockwise when viewed from above",
        units: "T.m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.f.clone() },
                )
            },
        }),
    },
    Node {
        name: "dpressure_dpsi",
        documentation: "Derivative of pressure w.r.t. psi. Sign depends on the poloidal flux sign convention",
        units: "Pa.Wb^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.dpressure_dpsi.clone() },
                )
            },
        }),
    },
    Node {
        name: "f_df_dpsi",
        documentation: "Derivative of F w.r.t. Psi, multiplied with F. Sign depends on the poloidal flux sign convention",
        units: "T^2.m^2.Wb^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.f_df_dpsi.clone() },
                )
            },
        }),
    },
    Node {
        name: "j_phi",
        documentation: "Flux surface averaged toroidal current density = average(j_tor/R) / average(1/R). Positive sign means counter-clockwise when viewed from above",
        units: "A.m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.j_phi.clone() },
                )
            },
        }),
    },
    Node {
        name: "j_parallel",
        documentation: "Flux surface averaged approximation to parallel current density = average(j.B) / B0, where B0 = /vacuum_toroidal_field/b0. Sign is positive when the scalar product j.B is in the same direction as B0 (the signed vacuum toroidal field at R0)",
        units: "A.m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.j_parallel.clone() },
                )
            },
        }),
    },
    Node {
        name: "q",
        documentation: "Safety factor (only positive when toroidal current and magnetic field are in same direction)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.q.clone() },
                )
            },
        }),
    },
    Node {
        name: "magnetic_shear",
        documentation: "Magnetic shear, defined as rho_tor/q . dq/drho_tor",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.magnetic_shear.clone() },
                )
            },
        }),
    },
    Node {
        name: "r_inboard",
        documentation: "Radial coordinate (major radius) on the inboard side of the magnetic axis",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.r_inboard.clone() },
                )
            },
        }),
    },
    Node {
        name: "r_outboard",
        documentation: "Radial coordinate (major radius) on the outboard side of the magnetic axis",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.r_outboard.clone() },
                )
            },
        }),
    },
    Node {
        name: "rho_tor",
        documentation: "Toroidal flux coordinate = sqrt(phi/(pi*b0)), where the toroidal flux, phi, corresponds to time_slice/profiles_1d/phi, the toroidal magnetic field, b0, corresponds to that stored in vacuum_toroidal_field/b0 and pi can be found in the IMAS constants",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.rho_tor.clone() },
                )
            },
        }),
    },
    Node {
        name: "rho_tor_norm",
        documentation: "Normalized toroidal flux coordinate. The normalizing value for rho_tor_norm, is the toroidal flux coordinate at the equilibrium boundary (LCFS or 99.x % of the LCFS in case of a fixed boundary equilibium calculation). Namely (rho_tor(rho)-rho_tor(magnetic_axis)) / (rho_tor(boundary)-rho_tor(magnetic_axis))",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.rho_tor_norm.clone() },
                )
            },
        }),
    },
    Node {
        name: "dpsi_drho_tor",
        documentation: "Derivative of Psi with respect to Rho_Tor. Sign follows the poloidal flux convention",
        units: "Wb.m^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.dpsi_drho_tor.clone() },
                )
            },
        }),
    },
    Node {
        name: "geometric_axis",
        documentation: "RZ position of the geometric axis of the magnetic surfaces (defined as (Rmin+Rmax) / 2 and (Zmin+Zmax) / 2 of the surface)",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_PROFILES_1D_GEOMETRIC_AXIS),
    },
    Node {
        name: "elongation",
        documentation: "Elongation",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.elongation.clone() },
                )
            },
        }),
    },
    Node {
        name: "triangularity",
        documentation: "Triangularity",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.triangularity.clone() },
                )
            },
        }),
    },
    Node {
        name: "triangularity_upper",
        documentation: "Upper triangularity",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.triangularity_upper.clone() },
                )
            },
        }),
    },
    Node {
        name: "triangularity_lower",
        documentation: "Lower triangularity",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.triangularity_lower.clone() },
                )
            },
        }),
    },
    Node {
        name: "squareness_upper_inner",
        documentation: "Upper inner squareness (definition from T. Luce, Plasma Phys. Control. Fusion 55 (2013) 095009)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.profiles_1d.squareness_upper_inner.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "squareness_upper_outer",
        documentation: "Upper outer squareness (definition from T. Luce, Plasma Phys. Control. Fusion 55 (2013) 095009)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.profiles_1d.squareness_upper_outer.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "squareness_lower_inner",
        documentation: "Lower inner squareness (definition from T. Luce, Plasma Phys. Control. Fusion 55 (2013) 095009)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.profiles_1d.squareness_lower_inner.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "squareness_lower_outer",
        documentation: "Lower outer squareness (definition from T. Luce, Plasma Phys. Control. Fusion 55 (2013) 095009)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.profiles_1d.squareness_lower_outer.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "volume",
        documentation: "Volume enclosed in the flux surface",
        units: "m^3",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.volume.clone() },
                )
            },
        }),
    },
    Node {
        name: "rho_volume_norm",
        documentation: "Normalized square root of enclosed volume (radial coordinate). The normalizing value is the enclosed volume at the equilibrium boundary (LCFS or 99.x % of the LCFS in case of a fixed boundary equilibium calculation)",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.rho_volume_norm.clone() },
                )
            },
        }),
    },
    Node {
        name: "dvolume_dpsi",
        documentation: "Radial derivative of the volume enclosed in the flux surface with respect to Psi. Sign depends on the poloidal flux sign convention",
        units: "m^3.Wb^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.dvolume_dpsi.clone() },
                )
            },
        }),
    },
    Node {
        name: "dvolume_drho_tor",
        documentation: "Radial derivative of the volume enclosed in the flux surface with respect to Rho_Tor",
        units: "m^2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.dvolume_drho_tor.clone() },
                )
            },
        }),
    },
    Node {
        name: "area",
        documentation: "Cross-sectional area of the flux surface",
        units: "m^2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.area.clone() },
                )
            },
        }),
    },
    Node {
        name: "darea_dpsi",
        documentation: "Radial derivative of the cross-sectional area of the flux surface with respect to psi. Sign depends on the poloidal flux sign convention",
        units: "m^2.Wb^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.darea_dpsi.clone() },
                )
            },
        }),
    },
    Node {
        name: "darea_drho_tor",
        documentation: "Radial derivative of the cross-sectional area of the flux surface with respect to rho_tor",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.darea_drho_tor.clone() },
                )
            },
        }),
    },
    Node {
        name: "surface",
        documentation: "Surface area of the toroidal flux surface",
        units: "m^2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.surface.clone() },
                )
            },
        }),
    },
    Node {
        name: "trapped_fraction",
        documentation: "Trapped particle fraction",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.trapped_fraction.clone() },
                )
            },
        }),
    },
    Node {
        name: "gm1",
        documentation: "Flux surface averaged 1/R^2",
        units: "m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.gm1.clone() },
                )
            },
        }),
    },
    Node {
        name: "gm2",
        documentation: "Flux surface averaged |grad_rho_tor|^2/R^2",
        units: "m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.gm2.clone() },
                )
            },
        }),
    },
    Node {
        name: "gm3",
        documentation: "Flux surface averaged |grad_rho_tor|^2",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.gm3.clone() },
                )
            },
        }),
    },
    Node {
        name: "gm4",
        documentation: "Flux surface averaged 1/B^2",
        units: "T^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.gm4.clone() },
                )
            },
        }),
    },
    Node {
        name: "gm5",
        documentation: "Flux surface averaged B^2",
        units: "T^2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.gm5.clone() },
                )
            },
        }),
    },
    Node {
        name: "gm6",
        documentation: "Flux surface averaged |grad_rho_tor|^2/B^2",
        units: "T^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.gm6.clone() },
                )
            },
        }),
    },
    Node {
        name: "gm7",
        documentation: "Flux surface averaged |grad_rho_tor|",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.gm7.clone() },
                )
            },
        }),
    },
    Node {
        name: "gm8",
        documentation: "Flux surface averaged R",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.gm8.clone() },
                )
            },
        }),
    },
    Node {
        name: "gm9",
        documentation: "Flux surface averaged 1/R",
        units: "m^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.gm9.clone() },
                )
            },
        }),
    },
    Node {
        name: "b_field_average",
        documentation: "Flux surface averaged modulus of B (always positive, irrespective of the sign convention for the B-field direction).",
        units: "T",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.b_field_average.clone() },
                )
            },
        }),
    },
    Node {
        name: "b_field_min",
        documentation: "Minimum(modulus(B)) on the flux surface (always positive, irrespective of the sign convention for the B-field direction)",
        units: "T",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.b_field_min.clone() },
                )
            },
        }),
    },
    Node {
        name: "b_field_max",
        documentation: "Maximum(modulus(B)) on the flux surface (always positive, irrespective of the sign convention for the B-field direction)",
        units: "T",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.b_field_max.clone() },
                )
            },
        }),
    },
    Node {
        name: "beta_pol",
        documentation: "Poloidal beta profile. Defined as betap = 4 int(p dV) / [R_0 * mu_0 * Ip^2]",
        units: "1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.beta_pol.clone() },
                )
            },
        }),
    },
    Node {
        name: "mass_density",
        documentation: "Mass density",
        units: "kg.m^-3",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.mass_density.clone() },
                )
            },
        }),
    },
    Node {
        name: "rho_pol",
        documentation: "Normalised poloidal flux radius, `sqrt(psi_norm)`. This is the poloidal counterpart of the
data dictionary's `rho_tor_norm`, which the data dictionary itself does not define",
        units: "dimensionless",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_1d.rho_pol.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_PROFILES_2D_TYPE: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.r#type.name.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.r#type.index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.r#type.description.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_PROFILES_2D_GRID_TYPE: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.grid_type.name.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.grid_type.index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.grid_type.description.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_PROFILES_2D_GRID: &[Node] = &[
    Node {
        name: "dim1",
        documentation: "First dimension values",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.grid.dim1.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "dim2",
        documentation: "Second dimension values",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.grid.dim2.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "volume_element",
        documentation: "Elementary plasma volume of plasma enclosed in the cell formed by the nodes [dim1(i) dim2(j)], [dim1(i+1) dim2(j)], [dim1(i) dim2(j+1)] and [dim1(i+1) dim2(j+1)]",
        units: "m^3",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.grid.volume_element.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "d_area",
        documentation: "Area of one grid cell in the poloidal plane, `d_dim1 * d_dim2`. A single value, because
the grid is rectangular and uniformly spaced in both directions",
        units: "m^2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.grid.d_area.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_PROFILES_2D: &[Node] = &[
    Node {
        name: "type",
        documentation: "Type of profiles (distinguishes contribution from plasma, vaccum fields and total fields)",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_PROFILES_2D_TYPE),
    },
    Node {
        name: "grid_type",
        documentation: "Selection of one of a set of grid types",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_PROFILES_2D_GRID_TYPE),
    },
    Node {
        name: "grid",
        documentation: "Definition of the 2D grid (the content of dim1 and dim2 is defined by the selected grid_type)",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_PROFILES_2D_GRID),
    },
    Node {
        name: "r",
        documentation: "Values of the major radius on the grid",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Values of the Height on the grid",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.z.clone() },
                )
            },
        }),
    },
    Node {
        name: "psi",
        documentation: "Values of the poloidal flux at the grid in the poloidal plane. The poloidal flux is integral of magnetic field passing through a contour defined by the intersection of a flux surface passing through the point of interest and a Z=constant plane. If the integration surface is flat, the surface normal vector is in the increasing vertical coordinate direction, Z, namely upwards.",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.psi.clone() },
                )
            },
        }),
    },
    Node {
        name: "theta",
        documentation: "Values of poloidal angle on the grid. The poloidal angle is centered on the magnetic axis and oriented such that (grad rho_tor_norm, grad theta, grad phi) form a right-handed set where grad rho_tor_norm points away from the magnetic axis.",
        units: "rad",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.theta.clone() },
                )
            },
        }),
    },
    Node {
        name: "phi",
        documentation: "Toroidal flux. Positive when the toroidal magnetic field is counter-clockwise when viewed from above",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.phi.clone() },
                )
            },
        }),
    },
    Node {
        name: "j_phi",
        documentation: "Toroidal plasma current density. Positive sign means counter-clockwise when viewed from above",
        units: "A.m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.j_phi.clone() },
                )
            },
        }),
    },
    Node {
        name: "j_parallel",
        documentation: "Defined as (j.B)/B0 where j and B are the current density and magnetic field vectors and B0 is the (signed) vacuum toroidal magnetic field strength at the geometric reference point (R0,Z0). It is formally not the component of the plasma current density parallel to the magnetic field",
        units: "A.m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.j_parallel.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "b_field_r",
        documentation: "R component of the poloidal magnetic field",
        units: "T",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.b_field_r.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "b_field_phi",
        documentation: "Toroidal component of the magnetic field. Positive sign means counter-clockwise when viewed from above",
        units: "T",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.b_field_phi.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "b_field_z",
        documentation: "Z component of the magnetic field",
        units: "T",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.b_field_z.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "d_psi_d_r",
        documentation: "Radial derivative of the poloidal flux on the grid in the poloidal plane",
        units: "Wb.m^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.d_psi_d_r.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "d_psi_d_z",
        documentation: "Vertical derivative of the poloidal flux on the grid in the poloidal plane",
        units: "Wb.m^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.d_psi_d_z.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "d2_psi_d_r2",
        documentation: "Second radial derivative of the poloidal flux on the grid in the poloidal plane",
        units: "Wb.m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.d2_psi_d_r2.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "d2_psi_d_r_d_z",
        documentation: "Mixed second derivative of the poloidal flux on the grid in the poloidal plane",
        units: "Wb.m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.d2_psi_d_r_d_z.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "d2_psi_d_z2",
        documentation: "Second vertical derivative of the poloidal flux on the grid in the poloidal plane",
        units: "Wb.m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.d2_psi_d_z2.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "mask",
        documentation: "Mask for the grid in the poloidal plane",
        units: "dimensionless",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.mask.clone() },
                )
            },
        }),
    },
    Node {
        name: "psi_norm",
        documentation: "Normalised poloidal flux on the grid, 0 at the magnetic axis and 1 at the plasma boundary",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.psi_norm.clone() },
                )
            },
        }),
    },
    Node {
        name: "psi_coils",
        documentation: "Contribution to the poloidal flux from the PF coils alone",
        units: "Wb",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.psi_coils.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "pressure",
        documentation: "Plasma pressure on the grid in the poloidal plane. Zero outside the plasma boundary",
        units: "Pa",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.pressure.clone() },
                )
            },
        }),
    },
    Node {
        name: "d_b_field_z_d_z",
        documentation: "Vertical derivative of the vertical magnetic field on the grid in the poloidal plane. Used
by the vertical stability control",
        units: "T.m^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_time_slice_profiles_2d,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.profiles_2d.get(at[1])?.d_b_field_z_d_z.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_GGD_R: &[Node] = &[
    Node {
        name: "grid_index",
        documentation: "Index of the grid used to represent this quantity",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_r,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.r.get(at[2])?.grid_index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "grid_subset_index",
        documentation: "Index of the grid subset the data is provided on. Corresponds to the index used in the grid subset definition: grid_subset(:)/identifier/index",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_r,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.r.get(at[2])?.grid_subset_index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "values",
        documentation: "One scalar value is provided per element in the grid subset.",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_r,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.r.get(at[2])?.values.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "coefficients",
        documentation: "Interpolation coefficients, to be used for a high precision evaluation of the physical quantity with finite elements, provided per element in the grid subset (first dimension).",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_r,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.r.get(at[2])?.coefficients.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_GGD_Z: &[Node] = &[
    Node {
        name: "grid_index",
        documentation: "Index of the grid used to represent this quantity",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_z,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.z.get(at[2])?.grid_index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "grid_subset_index",
        documentation: "Index of the grid subset the data is provided on. Corresponds to the index used in the grid subset definition: grid_subset(:)/identifier/index",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_z,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.z.get(at[2])?.grid_subset_index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "values",
        documentation: "One scalar value is provided per element in the grid subset.",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_z,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.z.get(at[2])?.values.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "coefficients",
        documentation: "Interpolation coefficients, to be used for a high precision evaluation of the physical quantity with finite elements, provided per element in the grid subset (first dimension).",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_z,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.z.get(at[2])?.coefficients.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_GGD_PSI: &[Node] = &[
    Node {
        name: "grid_index",
        documentation: "Index of the grid used to represent this quantity",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_psi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.psi.get(at[2])?.grid_index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "grid_subset_index",
        documentation: "Index of the grid subset the data is provided on. Corresponds to the index used in the grid subset definition: grid_subset(:)/identifier/index",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_psi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.psi.get(at[2])?.grid_subset_index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "values",
        documentation: "One scalar value is provided per element in the grid subset.",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_psi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.psi.get(at[2])?.values.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "coefficients",
        documentation: "Interpolation coefficients, to be used for a high precision evaluation of the physical quantity with finite elements, provided per element in the grid subset (first dimension).",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_psi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.psi.get(at[2])?.coefficients.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_GGD_PHI: &[Node] = &[
    Node {
        name: "grid_index",
        documentation: "Index of the grid used to represent this quantity",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.phi.get(at[2])?.grid_index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "grid_subset_index",
        documentation: "Index of the grid subset the data is provided on. Corresponds to the index used in the grid subset definition: grid_subset(:)/identifier/index",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.phi.get(at[2])?.grid_subset_index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "values",
        documentation: "One scalar value is provided per element in the grid subset.",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.phi.get(at[2])?.values.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "coefficients",
        documentation: "Interpolation coefficients, to be used for a high precision evaluation of the physical quantity with finite elements, provided per element in the grid subset (first dimension).",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.phi.get(at[2])?.coefficients.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_GGD_THETA: &[Node] = &[
    Node {
        name: "grid_index",
        documentation: "Index of the grid used to represent this quantity",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_theta,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.theta.get(at[2])?.grid_index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "grid_subset_index",
        documentation: "Index of the grid subset the data is provided on. Corresponds to the index used in the grid subset definition: grid_subset(:)/identifier/index",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_theta,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.theta.get(at[2])?.grid_subset_index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "values",
        documentation: "One scalar value is provided per element in the grid subset.",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_theta,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.theta.get(at[2])?.values.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "coefficients",
        documentation: "Interpolation coefficients, to be used for a high precision evaluation of the physical quantity with finite elements, provided per element in the grid subset (first dimension).",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_theta,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.theta.get(at[2])?.coefficients.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_GGD_J_PHI: &[Node] = &[
    Node {
        name: "grid_index",
        documentation: "Index of the grid used to represent this quantity",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_j_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.j_phi.get(at[2])?.grid_index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "grid_subset_index",
        documentation: "Index of the grid subset the data is provided on. Corresponds to the index used in the grid subset definition: grid_subset(:)/identifier/index",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_j_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.j_phi.get(at[2])?.grid_subset_index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "values",
        documentation: "One scalar value is provided per element in the grid subset.",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_j_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.j_phi.get(at[2])?.values.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "coefficients",
        documentation: "Interpolation coefficients, to be used for a high precision evaluation of the physical quantity with finite elements, provided per element in the grid subset (first dimension).",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_j_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.j_phi.get(at[2])?.coefficients.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_GGD_J_PARALLEL: &[Node] = &[
    Node {
        name: "grid_index",
        documentation: "Index of the grid used to represent this quantity",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_j_parallel,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.j_parallel.get(at[2])?.grid_index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "grid_subset_index",
        documentation: "Index of the grid subset the data is provided on. Corresponds to the index used in the grid subset definition: grid_subset(:)/identifier/index",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_j_parallel,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .ggd
                            .get(at[1])?
                            .j_parallel
                            .get(at[2])?
                            .grid_subset_index
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "values",
        documentation: "One scalar value is provided per element in the grid subset.",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_j_parallel,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.j_parallel.get(at[2])?.values.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "coefficients",
        documentation: "Interpolation coefficients, to be used for a high precision evaluation of the physical quantity with finite elements, provided per element in the grid subset (first dimension).",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_j_parallel,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.j_parallel.get(at[2])?.coefficients.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_GGD_B_FIELD_R: &[Node] = &[
    Node {
        name: "grid_index",
        documentation: "Index of the grid used to represent this quantity",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_b_field_r,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.b_field_r.get(at[2])?.grid_index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "grid_subset_index",
        documentation: "Index of the grid subset the data is provided on. Corresponds to the index used in the grid subset definition: grid_subset(:)/identifier/index",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_b_field_r,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .ggd
                            .get(at[1])?
                            .b_field_r
                            .get(at[2])?
                            .grid_subset_index
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "values",
        documentation: "One scalar value is provided per element in the grid subset.",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_b_field_r,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.b_field_r.get(at[2])?.values.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "coefficients",
        documentation: "Interpolation coefficients, to be used for a high precision evaluation of the physical quantity with finite elements, provided per element in the grid subset (first dimension).",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_b_field_r,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.b_field_r.get(at[2])?.coefficients.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_GGD_B_FIELD_PHI: &[Node] = &[
    Node {
        name: "grid_index",
        documentation: "Index of the grid used to represent this quantity",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_b_field_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.b_field_phi.get(at[2])?.grid_index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "grid_subset_index",
        documentation: "Index of the grid subset the data is provided on. Corresponds to the index used in the grid subset definition: grid_subset(:)/identifier/index",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_b_field_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .ggd
                            .get(at[1])?
                            .b_field_phi
                            .get(at[2])?
                            .grid_subset_index
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "values",
        documentation: "One scalar value is provided per element in the grid subset.",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_b_field_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.b_field_phi.get(at[2])?.values.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "coefficients",
        documentation: "Interpolation coefficients, to be used for a high precision evaluation of the physical quantity with finite elements, provided per element in the grid subset (first dimension).",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_b_field_phi,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.b_field_phi.get(at[2])?.coefficients.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_GGD_B_FIELD_Z: &[Node] = &[
    Node {
        name: "grid_index",
        documentation: "Index of the grid used to represent this quantity",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_b_field_z,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.b_field_z.get(at[2])?.grid_index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "grid_subset_index",
        documentation: "Index of the grid subset the data is provided on. Corresponds to the index used in the grid subset definition: grid_subset(:)/identifier/index",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_b_field_z,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .ggd
                            .get(at[1])?
                            .b_field_z
                            .get(at[2])?
                            .grid_subset_index
                            .clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "values",
        documentation: "One scalar value is provided per element in the grid subset.",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_b_field_z,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.b_field_z.get(at[2])?.values.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "coefficients",
        documentation: "Interpolation coefficients, to be used for a high precision evaluation of the physical quantity with finite elements, provided per element in the grid subset (first dimension).",
        units: "as_parent",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    3,
                    lengths_time_slice_ggd_b_field_z,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.ggd.get(at[1])?.b_field_z.get(at[2])?.coefficients.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_GGD: &[Node] = &[
    Node {
        name: "r",
        documentation: "Values of the major radius on various grid subsets",
        units: "m",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_GGD_R),
    },
    Node {
        name: "z",
        documentation: "Values of the Height on various grid subsets",
        units: "m",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_GGD_Z),
    },
    Node {
        name: "psi",
        documentation: "Values of the poloidal flux, given on various grid subsets. For a positive plasma current (counter-clockwise when viewed from above), increases from the magnetic axis to the boundary",
        units: "Wb",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_GGD_PSI),
    },
    Node {
        name: "phi",
        documentation: "Values of the toroidal flux, given on various grid subsets. Positive sign means counter-clockwise when viewed from above",
        units: "Wb",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_GGD_PHI),
    },
    Node {
        name: "theta",
        documentation: "Values of the poloidal angle, given on various grid subsets. The poloidal angle is centered on the magnetic axis and oriented such that (grad rho_tor_norm, grad theta, grad phi) form a right-handed set where grad rho_tor_norm points away from the magnetic axis.",
        units: "rad",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_GGD_THETA),
    },
    Node {
        name: "j_phi",
        documentation: "Toroidal plasma current density, given on various grid subsets",
        units: "A.m^-2",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_GGD_J_PHI),
    },
    Node {
        name: "j_parallel",
        documentation: "Parallel (to magnetic field) plasma current density, given on various grid subsets",
        units: "A.m^-2",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_GGD_J_PARALLEL),
    },
    Node {
        name: "b_field_r",
        documentation: "R component of the poloidal magnetic field, given on various grid subsets",
        units: "T",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_GGD_B_FIELD_R),
    },
    Node {
        name: "b_field_phi",
        documentation: "Toroidal component of the magnetic field, given on various grid subsets",
        units: "T",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_GGD_B_FIELD_PHI),
    },
    Node {
        name: "b_field_z",
        documentation: "Z component of the magnetic field, given on various grid subsets",
        units: "T",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_GGD_B_FIELD_Z),
    },
];

static NODES_TIME_SLICE_COORDINATE_SYSTEM_GRID_TYPE: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> { equilibrium.time_slice.get(at[0])?.coordinate_system.grid_type.name.clone() },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.coordinate_system.grid_type.index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.coordinate_system.grid_type.description.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_COORDINATE_SYSTEM_GRID: &[Node] = &[
    Node {
        name: "dim1",
        documentation: "First dimension values",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.coordinate_system.grid.dim1.clone() },
                )
            },
        }),
    },
    Node {
        name: "dim2",
        documentation: "Second dimension values",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.coordinate_system.grid.dim2.clone() },
                )
            },
        }),
    },
    Node {
        name: "volume_element",
        documentation: "Elementary plasma volume of plasma enclosed in the cell formed by the nodes [dim1(i) dim2(j)], [dim1(i+1) dim2(j)], [dim1(i) dim2(j+1)] and [dim1(i+1) dim2(j+1)]",
        units: "m^3",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> {
                        equilibrium.time_slice.get(at[0])?.coordinate_system.grid.volume_element.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "d_area",
        documentation: "Area of one grid cell in the poloidal plane, `d_dim1 * d_dim2`. A single value, because
the grid is rectangular and uniformly spaced in both directions",
        units: "m^2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.coordinate_system.grid.d_area.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_COORDINATE_SYSTEM: &[Node] = &[
    Node {
        name: "grid_type",
        documentation: "Type of coordinate system",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_COORDINATE_SYSTEM_GRID_TYPE),
    },
    Node {
        name: "grid",
        documentation: "Definition of the 2D grid",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_COORDINATE_SYSTEM_GRID),
    },
    Node {
        name: "r",
        documentation: "Values of the major radius on the grid",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.time_slice.get(at[0])?.coordinate_system.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Values of the Height on the grid",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.time_slice.get(at[0])?.coordinate_system.z.clone() },
                )
            },
        }),
    },
    Node {
        name: "jacobian",
        documentation: "Absolute value of the jacobian of the coordinate system",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.time_slice.get(at[0])?.coordinate_system.jacobian.clone() },
                )
            },
        }),
    },
    Node {
        name: "tensor_covariant",
        documentation: "Covariant metric tensor on every point of the grid described by grid_type",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_4D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_4D> {
                        equilibrium.time_slice.get(at[0])?.coordinate_system.tensor_covariant.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "tensor_contravariant",
        documentation: "Contravariant metric tensor on every point of the grid described by grid_type",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_4D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_4D> {
                        equilibrium.time_slice.get(at[0])?.coordinate_system.tensor_contravariant.clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONVERGENCE_GRAD_SHAFRANOV_DEVIATION_EXPRESSION: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium.time_slice.get(at[0])?.convergence.grad_shafranov_deviation_expression.name.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> {
                        equilibrium.time_slice.get(at[0])?.convergence.grad_shafranov_deviation_expression.index.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> {
                        equilibrium
                            .time_slice
                            .get(at[0])?
                            .convergence
                            .grad_shafranov_deviation_expression
                            .description
                            .clone()
                    },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONVERGENCE_RESULT: &[Node] = &[
    Node {
        name: "name",
        documentation: "Short string identifier",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> { equilibrium.time_slice.get(at[0])?.convergence.result.name.clone() },
                )
            },
        }),
    },
    Node {
        name: "index",
        documentation: "Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> { equilibrium.time_slice.get(at[0])?.convergence.result.index.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Verbose description",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> { equilibrium.time_slice.get(at[0])?.convergence.result.description.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_CONVERGENCE: &[Node] = &[
    Node {
        name: "iterations_n",
        documentation: "Number of iterations carried out in the convergence loop",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<INT_0D> { equilibrium.time_slice.get(at[0])?.convergence.iterations_n.clone() },
                )
            },
        }),
    },
    Node {
        name: "grad_shafranov_deviation_expression",
        documentation: "Expression for calculating the residual deviation between the left and right hand side of the Grad Shafranov equation",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONVERGENCE_GRAD_SHAFRANOV_DEVIATION_EXPRESSION),
    },
    Node {
        name: "grad_shafranov_deviation_value",
        documentation: "Value of the residual deviation between the left and right hand side of the Grad Shafranov equation, evaluated as per grad_shafranov_deviation_expression",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> {
                        equilibrium.time_slice.get(at[0])?.convergence.grad_shafranov_deviation_value.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "result",
        documentation: "Convergence result",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONVERGENCE_RESULT),
    },
    Node {
        name: "delta_z",
        documentation: "Vertical shift applied by the vertical feedback controller. Unset until the first inverse
solve has run, and 0 while the vertical feedback is switched off",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.convergence.delta_z.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_SOURCE_FUNCTIONS_P_PRIME: &[Node] = &[Node {
    name: "coefficients",
    documentation: "Fitted degrees of freedom of the source function, in the basis the source function defines",
    units: "",
    kind: NodeKind::Leaf(Leaf {
        data_type: "FLT_1D",
        read: |ids: &dyn Any, indices: &[IndexSpec]| {
            let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
            gather(
                equilibrium,
                indices,
                1,
                lengths_time_slice,
                |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                    equilibrium.time_slice.get(at[0])?.source_functions.p_prime.coefficients.clone()
                },
            )
        },
    }),
}];

static NODES_TIME_SLICE_SOURCE_FUNCTIONS_FF_PRIME: &[Node] = &[Node {
    name: "coefficients",
    documentation: "Fitted degrees of freedom of the source function, in the basis the source function defines",
    units: "",
    kind: NodeKind::Leaf(Leaf {
        data_type: "FLT_1D",
        read: |ids: &dyn Any, indices: &[IndexSpec]| {
            let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
            gather(
                equilibrium,
                indices,
                1,
                lengths_time_slice,
                |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                    equilibrium.time_slice.get(at[0])?.source_functions.ff_prime.coefficients.clone()
                },
            )
        },
    }),
}];

static NODES_TIME_SLICE_SOURCE_FUNCTIONS: &[Node] = &[
    Node {
        name: "p_prime",
        documentation: "The p' source function, dp/dpsi",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_SOURCE_FUNCTIONS_P_PRIME),
    },
    Node {
        name: "ff_prime",
        documentation: "The FF' source function, F dF/dpsi",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_SOURCE_FUNCTIONS_FF_PRIME),
    },
];

static NODES_TIME_SLICE_PROFILES_R_MIDPLANE: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius of each point along the mid-plane, which is the grid's own radial axis",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_r_midplane.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "pressure",
        documentation: "Plasma pressure along the mid-plane. Zero outside the plasma boundary",
        units: "Pa",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.profiles_r_midplane.pressure.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_SOL_HFS_CONTOUR: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius of each point along the leg",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.sol.hfs.contour.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height of each point along the leg",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.sol.hfs.contour.z.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_SOL_HFS_STRIKE_POINT: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius of the strike point",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.sol.hfs.strike_point.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height of the strike point",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.sol.hfs.strike_point.z.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_SOL_HFS: &[Node] = &[
    Node {
        name: "contour",
        documentation: "The field line itself, traced from the X-point to where it meets the wall",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_SOL_HFS_CONTOUR),
    },
    Node {
        name: "strike_point",
        documentation: "Where the leg meets the wall, which is the last point of the contour",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_SOL_HFS_STRIKE_POINT),
    },
];

static NODES_TIME_SLICE_SOL_LFS_CONTOUR: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius of each point along the leg",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.sol.lfs.contour.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height of each point along the leg",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.sol.lfs.contour.z.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_SOL_LFS_STRIKE_POINT: &[Node] = &[
    Node {
        name: "r",
        documentation: "Major radius of the strike point",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.sol.lfs.strike_point.r.clone() },
                )
            },
        }),
    },
    Node {
        name: "z",
        documentation: "Height of the strike point",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.sol.lfs.strike_point.z.clone() },
                )
            },
        }),
    },
];

static NODES_TIME_SLICE_SOL_LFS: &[Node] = &[
    Node {
        name: "contour",
        documentation: "The field line itself, traced from the X-point to where it meets the wall",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_SOL_LFS_CONTOUR),
    },
    Node {
        name: "strike_point",
        documentation: "Where the leg meets the wall, which is the last point of the contour",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_SOL_LFS_STRIKE_POINT),
    },
];

static NODES_TIME_SLICE_SOL: &[Node] = &[
    Node {
        name: "hfs",
        documentation: "High field side leg, on the inboard side of the machine",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_SOL_HFS),
    },
    Node {
        name: "lfs",
        documentation: "Low field side leg, on the outboard side of the machine",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_SOL_LFS),
    },
];

static NODES_TIME_SLICE: &[Node] = &[
    Node {
        name: "boundary",
        documentation: "Description of the plasma boundary. The boundary can be either the real separatrix (provided by a free boundary equilibrium solver) or the 0.99x psi_norm flux surface provided by a fixed boundary equilibrium",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_BOUNDARY),
    },
    Node {
        name: "contour_tree",
        documentation: "Contour tree (Reeb graph) of the poloidal flux, encoding the topological structure of the equilibrium through its critical points and limiter points. Nodes represent critical points of the poloidal flux (O-points, X-points) and limiter points where the plasma boundary touches the first wall. Edges connect nodes that are topologically adjacent in psi_norm space, meaning no other node lies between them. The tree captures the nesting structure of flux surfaces, the connectivity between plasma regions, the separatrix topology, and the locations of limiter contacts. Normalised poloidal flux, psi_norm = (psi - psi_axis) / (psi_boundary - psi_axis), defines all ordering conventions.",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONTOUR_TREE),
    },
    Node {
        name: "constraints",
        documentation: "In case of equilibrium reconstruction under constraints, measurements used to constrain the equilibrium, reconstructed values and accuracy of the fit. The names of the child nodes correspond to the following definition: the solver aims at minimizing a cost function defined as : J=1/2*sum_i [ weight_i^2 (reconstructed_i - measured_i)^2 / sigma_i^2 ]. in which sigma_i is the standard deviation of the measurement error (to be found in the IDS of the measurement)",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONSTRAINTS),
    },
    Node {
        name: "global_quantities",
        documentation: "0D parameters of the equilibrium",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_GLOBAL_QUANTITIES),
    },
    Node {
        name: "profiles_1d",
        documentation: "Equilibrium profiles (1D radial grid) as a function of the poloidal flux",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_PROFILES_1D),
    },
    Node {
        name: "profiles_2d",
        documentation: "Equilibrium 2D profiles in the poloidal plane. Multiple 2D representations of the equilibrium can be stored here.",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_PROFILES_2D),
    },
    Node {
        name: "ggd",
        documentation: "Set of equilibrium representations using the generic grid description",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE_GGD),
    },
    Node {
        name: "coordinate_system",
        documentation: "Flux surface coordinate system on a square grid of flux and poloidal angle",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_COORDINATE_SYSTEM),
    },
    Node {
        name: "convergence",
        documentation: "Convergence details",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_CONVERGENCE),
    },
    Node {
        name: "time",
        documentation: "Time",
        units: "s",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_0D> { equilibrium.time_slice.get(at[0])?.time.clone() },
                )
            },
        }),
    },
    Node {
        name: "source_functions",
        documentation: "Source functions which parameterise the plasma current profile",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_SOURCE_FUNCTIONS),
    },
    Node {
        name: "profiles_r_midplane",
        documentation: "Profiles along the horizontal line through the middle of the grid",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_PROFILES_R_MIDPLANE),
    },
    Node {
        name: "sol",
        documentation: "Scrape-off layer: the open field lines outside the last closed flux surface",
        units: "",
        kind: NodeKind::Structure(NODES_TIME_SLICE_SOL),
    },
    Node {
        name: "passive_dof_values",
        documentation: "Fitted degrees of freedom of the passive structure currents",
        units: "A",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_time_slice,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.time_slice.get(at[0])?.passive_dof_values.clone() },
                )
            },
        }),
    },
];

static NODES_CODE_LIBRARY: &[Node] = &[
    Node {
        name: "name",
        documentation: "Name of software",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_code_library,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> { equilibrium.code.library.get(at[0])?.name.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Short description of the software (type, purpose)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_code_library,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> { equilibrium.code.library.get(at[0])?.description.clone() },
                )
            },
        }),
    },
    Node {
        name: "commit",
        documentation: "Unique commit reference of software",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_code_library,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> { equilibrium.code.library.get(at[0])?.commit.clone() },
                )
            },
        }),
    },
    Node {
        name: "version",
        documentation: "Unique version (tag) of software",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_code_library,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> { equilibrium.code.library.get(at[0])?.version.clone() },
                )
            },
        }),
    },
    Node {
        name: "repository",
        documentation: "URL of software repository",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_code_library,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> { equilibrium.code.library.get(at[0])?.repository.clone() },
                )
            },
        }),
    },
    Node {
        name: "parameters",
        documentation: "List of the code specific parameters in XML format",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_code_library,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> { equilibrium.code.library.get(at[0])?.parameters.clone() },
                )
            },
        }),
    },
];

static NODES_CODE_INITIAL_GUESS: &[Node] = &[
    Node {
        name: "ip",
        documentation: "Initial total plasma current",
        units: "A",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_0D> { equilibrium.code.initial_guess.ip.clone() },
                )
            },
        }),
    },
    Node {
        name: "cur_r",
        documentation: "Radial centre of the initial current distribution",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_0D> { equilibrium.code.initial_guess.cur_r.clone() },
                )
            },
        }),
    },
    Node {
        name: "cur_z",
        documentation: "Vertical centre of the initial current distribution",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_0D> { equilibrium.code.initial_guess.cur_z.clone() },
                )
            },
        }),
    },
    Node {
        name: "minor_radius",
        documentation: "Radial semi-axis of the initial current distribution",
        units: "m",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_0D> { equilibrium.code.initial_guess.minor_radius.clone() },
                )
            },
        }),
    },
    Node {
        name: "elongation",
        documentation: "Elongation of the initial current distribution",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_0D> { equilibrium.code.initial_guess.elongation.clone() },
                )
            },
        }),
    },
];

static NODES_CODE: &[Node] = &[
    Node {
        name: "name",
        documentation: "Name of software generating IDS",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<STR_0D> { equilibrium.code.name.clone() },
                )
            },
        }),
    },
    Node {
        name: "description",
        documentation: "Short description of the software (type, purpose)",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<STR_0D> { equilibrium.code.description.clone() },
                )
            },
        }),
    },
    Node {
        name: "commit",
        documentation: "Unique commit reference of software",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<STR_0D> { equilibrium.code.commit.clone() },
                )
            },
        }),
    },
    Node {
        name: "version",
        documentation: "Unique version (tag) of software",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<STR_0D> { equilibrium.code.version.clone() },
                )
            },
        }),
    },
    Node {
        name: "repository",
        documentation: "URL of software repository",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<STR_0D> { equilibrium.code.repository.clone() },
                )
            },
        }),
    },
    Node {
        name: "parameters",
        documentation: "List of the code specific parameters in XML format",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<STR_0D> { equilibrium.code.parameters.clone() },
                )
            },
        }),
    },
    Node {
        name: "output_flag",
        documentation: "Output flag : 0 means the run is successful, other values mean some difficulty has been encountered, the exact meaning is then code specific. Negative values mean the result shall not be used.",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<INT_1D> { equilibrium.code.output_flag.clone() },
                )
            },
        }),
    },
    Node {
        name: "library",
        documentation: "List of external libraries used by the code that has produced this IDS",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_CODE_LIBRARY),
    },
    Node {
        name: "iterations_n_max",
        documentation: "Maximum number of iterations the convergence loop is allowed to run for",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<INT_0D> { equilibrium.code.iterations_n_max.clone() },
                )
            },
        }),
    },
    Node {
        name: "iterations_n_min",
        documentation: "Minimum number of iterations before the convergence test is allowed to pass",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<INT_0D> { equilibrium.code.iterations_n_min.clone() },
                )
            },
        }),
    },
    Node {
        name: "iterations_n_no_vertical_feedback",
        documentation: "Number of initial iterations for which the vertical feedback is switched off",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "INT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<INT_0D> { equilibrium.code.iterations_n_no_vertical_feedback.clone() },
                )
            },
        }),
    },
    Node {
        name: "grad_shafranov_deviation_value_tolerance",
        documentation: "Value of convergence/grad_shafranov_deviation_value below which the solution is taken as
converged",
        units: "mixed",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_0D> { equilibrium.code.grad_shafranov_deviation_value_tolerance.clone() },
                )
            },
        }),
    },
    Node {
        name: "initial_guess",
        documentation: "Initial guess for the plasma, used to seed the first iteration",
        units: "",
        kind: NodeKind::Structure(NODES_CODE_INITIAL_GUESS),
    },
];

static NODES_GREENS_PF_ACTIVE: &[Node] = &[
    Node {
        name: "name",
        documentation: "Name of the coil, e.g. `\"BVL\"`",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_greens_pf_active,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> { equilibrium.greens.pf_active.get(at[0])?.name.clone() },
                )
            },
        }),
    },
    Node {
        name: "psi",
        documentation: "Poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z, n_r)`",
        units: "Wb.A^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_greens_pf_active,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.pf_active.get(at[0])?.psi.clone() },
                )
            },
        }),
    },
    Node {
        name: "br",
        documentation: "Radial magnetic field at every grid point, per ampere in the source.
Shape = `(n_z, n_r)`",
        units: "T.A^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_greens_pf_active,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.pf_active.get(at[0])?.br.clone() },
                )
            },
        }),
    },
    Node {
        name: "bz",
        documentation: "Vertical magnetic field at every grid point, per ampere in the source.
Shape = `(n_z, n_r)`",
        units: "T.A^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_greens_pf_active,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.pf_active.get(at[0])?.bz.clone() },
                )
            },
        }),
    },
    Node {
        name: "d_br_d_z",
        documentation: "Vertical derivative of the radial magnetic field at every grid point, per ampere in the source.
Shape = `(n_z, n_r)`",
        units: "T.A^-1.m^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_greens_pf_active,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.pf_active.get(at[0])?.d_br_d_z.clone() },
                )
            },
        }),
    },
    Node {
        name: "d_bz_d_z",
        documentation: "Vertical derivative of the vertical magnetic field at every grid point, per ampere in the source.
Shape = `(n_z, n_r)`",
        units: "T.A^-1.m^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_greens_pf_active,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.pf_active.get(at[0])?.d_bz_d_z.clone() },
                )
            },
        }),
    },
    Node {
        name: "d_psi_d_r",
        documentation: "Radial derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z, n_r)`",
        units: "Wb.A^-1.m^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_greens_pf_active,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.pf_active.get(at[0])?.d_psi_d_r.clone() },
                )
            },
        }),
    },
    Node {
        name: "d_psi_d_z",
        documentation: "Vertical derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z, n_r)`",
        units: "Wb.A^-1.m^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_greens_pf_active,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.pf_active.get(at[0])?.d_psi_d_z.clone() },
                )
            },
        }),
    },
    Node {
        name: "d2_psi_d_r2",
        documentation: "Second radial derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z, n_r)`",
        units: "Wb.A^-1.m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_greens_pf_active,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.pf_active.get(at[0])?.d2_psi_d_r2.clone() },
                )
            },
        }),
    },
    Node {
        name: "d2_psi_d_r_d_z",
        documentation: "Mixed second derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z, n_r)`",
        units: "Wb.A^-1.m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_greens_pf_active,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.pf_active.get(at[0])?.d2_psi_d_r_d_z.clone() },
                )
            },
        }),
    },
    Node {
        name: "d2_psi_d_z2",
        documentation: "Second vertical derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z, n_r)`",
        units: "Wb.A^-1.m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_greens_pf_active,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.pf_active.get(at[0])?.d2_psi_d_z2.clone() },
                )
            },
        }),
    },
    Node {
        name: "d3_psi_d_r2_d_z",
        documentation: "Third derivative of the poloidal flux, twice by r, once by z at every grid point, per ampere in the source.
Shape = `(n_z, n_r)`",
        units: "Wb.A^-1.m^-3",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_greens_pf_active,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.pf_active.get(at[0])?.d3_psi_d_r2_d_z.clone() },
                )
            },
        }),
    },
    Node {
        name: "d3_psi_d_r_d_z2",
        documentation: "Third derivative of the poloidal flux, once by r, twice by z at every grid point, per ampere in the source.
Shape = `(n_z, n_r)`",
        units: "Wb.A^-1.m^-3",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_greens_pf_active,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.pf_active.get(at[0])?.d3_psi_d_r_d_z2.clone() },
                )
            },
        }),
    },
    Node {
        name: "d3_psi_d_z3",
        documentation: "Third vertical derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z, n_r)`",
        units: "Wb.A^-1.m^-3",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_greens_pf_active,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.pf_active.get(at[0])?.d3_psi_d_z3.clone() },
                )
            },
        }),
    },
];

static NODES_GREENS_PF_PASSIVE_DOF: &[Node] = &[
    Node {
        name: "name",
        documentation: "Name of the degree of freedom, e.g. `\"EIG_01\"`",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_greens_pf_passive_dof,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> { equilibrium.greens.pf_passive.get(at[0])?.dof.get(at[1])?.name.clone() },
                )
            },
        }),
    },
    Node {
        name: "psi",
        documentation: "Poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z * n_r)`",
        units: "Wb.A^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_greens_pf_passive_dof,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.greens.pf_passive.get(at[0])?.dof.get(at[1])?.psi.clone() },
                )
            },
        }),
    },
    Node {
        name: "br",
        documentation: "Radial magnetic field at every grid point, per ampere in the source.
Shape = `(n_z * n_r)`",
        units: "T.A^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_greens_pf_passive_dof,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.greens.pf_passive.get(at[0])?.dof.get(at[1])?.br.clone() },
                )
            },
        }),
    },
    Node {
        name: "bz",
        documentation: "Vertical magnetic field at every grid point, per ampere in the source.
Shape = `(n_z * n_r)`",
        units: "T.A^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_greens_pf_passive_dof,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.greens.pf_passive.get(at[0])?.dof.get(at[1])?.bz.clone() },
                )
            },
        }),
    },
    Node {
        name: "d_br_d_z",
        documentation: "Vertical derivative of the radial magnetic field at every grid point, per ampere in the source.
Shape = `(n_z * n_r)`",
        units: "T.A^-1.m^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_greens_pf_passive_dof,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.greens.pf_passive.get(at[0])?.dof.get(at[1])?.d_br_d_z.clone() },
                )
            },
        }),
    },
    Node {
        name: "d_bz_d_z",
        documentation: "Vertical derivative of the vertical magnetic field at every grid point, per ampere in the source.
Shape = `(n_z * n_r)`",
        units: "T.A^-1.m^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_greens_pf_passive_dof,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.greens.pf_passive.get(at[0])?.dof.get(at[1])?.d_bz_d_z.clone() },
                )
            },
        }),
    },
    Node {
        name: "d_psi_d_r",
        documentation: "Radial derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z * n_r)`",
        units: "Wb.A^-1.m^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_greens_pf_passive_dof,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.greens.pf_passive.get(at[0])?.dof.get(at[1])?.d_psi_d_r.clone() },
                )
            },
        }),
    },
    Node {
        name: "d_psi_d_z",
        documentation: "Vertical derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z * n_r)`",
        units: "Wb.A^-1.m^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_greens_pf_passive_dof,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> { equilibrium.greens.pf_passive.get(at[0])?.dof.get(at[1])?.d_psi_d_z.clone() },
                )
            },
        }),
    },
    Node {
        name: "d2_psi_d_r2",
        documentation: "Second radial derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z * n_r)`",
        units: "Wb.A^-1.m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_greens_pf_passive_dof,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.greens.pf_passive.get(at[0])?.dof.get(at[1])?.d2_psi_d_r2.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "d2_psi_d_r_d_z",
        documentation: "Mixed second derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z * n_r)`",
        units: "Wb.A^-1.m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_greens_pf_passive_dof,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.greens.pf_passive.get(at[0])?.dof.get(at[1])?.d2_psi_d_r_d_z.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "d2_psi_d_z2",
        documentation: "Second vertical derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z * n_r)`",
        units: "Wb.A^-1.m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_greens_pf_passive_dof,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.greens.pf_passive.get(at[0])?.dof.get(at[1])?.d2_psi_d_z2.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "d3_psi_d_r2_d_z",
        documentation: "Third derivative of the poloidal flux, twice by r, once by z at every grid point, per ampere in the source.
Shape = `(n_z * n_r)`",
        units: "Wb.A^-1.m^-3",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_greens_pf_passive_dof,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.greens.pf_passive.get(at[0])?.dof.get(at[1])?.d3_psi_d_r2_d_z.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "d3_psi_d_r_d_z2",
        documentation: "Third derivative of the poloidal flux, once by r, twice by z at every grid point, per ampere in the source.
Shape = `(n_z * n_r)`",
        units: "Wb.A^-1.m^-3",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_greens_pf_passive_dof,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.greens.pf_passive.get(at[0])?.dof.get(at[1])?.d3_psi_d_r_d_z2.clone()
                    },
                )
            },
        }),
    },
    Node {
        name: "d3_psi_d_z3",
        documentation: "Third vertical derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z * n_r)`",
        units: "Wb.A^-1.m^-3",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_1D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    2,
                    lengths_greens_pf_passive_dof,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<FLT_1D> {
                        equilibrium.greens.pf_passive.get(at[0])?.dof.get(at[1])?.d3_psi_d_z3.clone()
                    },
                )
            },
        }),
    },
];

static NODES_GREENS_PF_PASSIVE: &[Node] = &[
    Node {
        name: "name",
        documentation: "Name of the passive conductor, e.g. `\"IVC\"`",
        units: "",
        kind: NodeKind::Leaf(Leaf {
            data_type: "STR_0D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    1,
                    lengths_greens_pf_passive,
                    |equilibrium: &Equilibrium, at: &[usize]| -> Option<STR_0D> { equilibrium.greens.pf_passive.get(at[0])?.name.clone() },
                )
            },
        }),
    },
    Node {
        name: "dof",
        documentation: "Degrees of freedom of this conductor, one entry per current distribution",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_GREENS_PF_PASSIVE_DOF),
    },
];

static NODES_GREENS_GRID_GRID: &[Node] = &[
    Node {
        name: "psi",
        documentation: "Poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z * n_r, n_r)`, which unflattens to `(i_offset_z, i_r, i_current_r)`",
        units: "Wb.A^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.grid_grid.psi.clone() },
                )
            },
        }),
    },
    Node {
        name: "br",
        documentation: "Radial magnetic field at every grid point, per ampere in the source.
Shape = `(n_z * n_r, n_r)`, which unflattens to `(i_offset_z, i_r, i_current_r)`",
        units: "T.A^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.grid_grid.br.clone() },
                )
            },
        }),
    },
    Node {
        name: "bz",
        documentation: "Vertical magnetic field at every grid point, per ampere in the source.
Shape = `(n_z * n_r, n_r)`, which unflattens to `(i_offset_z, i_r, i_current_r)`",
        units: "T.A^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.grid_grid.bz.clone() },
                )
            },
        }),
    },
    Node {
        name: "d_br_d_z",
        documentation: "Vertical derivative of the radial magnetic field at every grid point, per ampere in the source.
Shape = `(n_z * n_r, n_r)`, which unflattens to `(i_offset_z, i_r, i_current_r)`",
        units: "T.A^-1.m^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.grid_grid.d_br_d_z.clone() },
                )
            },
        }),
    },
    Node {
        name: "d_bz_d_z",
        documentation: "Vertical derivative of the vertical magnetic field at every grid point, per ampere in the source.
Shape = `(n_z * n_r, n_r)`, which unflattens to `(i_offset_z, i_r, i_current_r)`",
        units: "T.A^-1.m^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.grid_grid.d_bz_d_z.clone() },
                )
            },
        }),
    },
    Node {
        name: "d_psi_d_r",
        documentation: "Radial derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z * n_r, n_r)`, which unflattens to `(i_offset_z, i_r, i_current_r)`",
        units: "Wb.A^-1.m^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.grid_grid.d_psi_d_r.clone() },
                )
            },
        }),
    },
    Node {
        name: "d_psi_d_z",
        documentation: "Vertical derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z * n_r, n_r)`, which unflattens to `(i_offset_z, i_r, i_current_r)`",
        units: "Wb.A^-1.m^-1",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.grid_grid.d_psi_d_z.clone() },
                )
            },
        }),
    },
    Node {
        name: "d2_psi_d_r2",
        documentation: "Second radial derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z * n_r, n_r)`, which unflattens to `(i_offset_z, i_r, i_current_r)`",
        units: "Wb.A^-1.m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.grid_grid.d2_psi_d_r2.clone() },
                )
            },
        }),
    },
    Node {
        name: "d2_psi_d_r_d_z",
        documentation: "Mixed second derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z * n_r, n_r)`, which unflattens to `(i_offset_z, i_r, i_current_r)`",
        units: "Wb.A^-1.m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.grid_grid.d2_psi_d_r_d_z.clone() },
                )
            },
        }),
    },
    Node {
        name: "d2_psi_d_z2",
        documentation: "Second vertical derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z * n_r, n_r)`, which unflattens to `(i_offset_z, i_r, i_current_r)`",
        units: "Wb.A^-1.m^-2",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.grid_grid.d2_psi_d_z2.clone() },
                )
            },
        }),
    },
    Node {
        name: "d3_psi_d_r2_d_z",
        documentation: "Third derivative of the poloidal flux, twice by r, once by z at every grid point, per ampere in the source.
Shape = `(n_z * n_r, n_r)`, which unflattens to `(i_offset_z, i_r, i_current_r)`",
        units: "Wb.A^-1.m^-3",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.grid_grid.d3_psi_d_r2_d_z.clone() },
                )
            },
        }),
    },
    Node {
        name: "d3_psi_d_r_d_z2",
        documentation: "Third derivative of the poloidal flux, once by r, twice by z at every grid point, per ampere in the source.
Shape = `(n_z * n_r, n_r)`, which unflattens to `(i_offset_z, i_r, i_current_r)`",
        units: "Wb.A^-1.m^-3",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.grid_grid.d3_psi_d_r_d_z2.clone() },
                )
            },
        }),
    },
    Node {
        name: "d3_psi_d_z3",
        documentation: "Third vertical derivative of the poloidal flux at every grid point, per ampere in the source.
Shape = `(n_z * n_r, n_r)`, which unflattens to `(i_offset_z, i_r, i_current_r)`",
        units: "Wb.A^-1.m^-3",
        kind: NodeKind::Leaf(Leaf {
            data_type: "FLT_2D",
            read: |ids: &dyn Any, indices: &[IndexSpec]| {
                let equilibrium: &Equilibrium = ids.downcast_ref().ok_or_else(|| "not a equilibrium IDS".to_string())?;
                gather(
                    equilibrium,
                    indices,
                    0,
                    no_levels,
                    |equilibrium: &Equilibrium, _at: &[usize]| -> Option<FLT_2D> { equilibrium.greens.grid_grid.d3_psi_d_z3.clone() },
                )
            },
        }),
    },
];

static NODES_GREENS: &[Node] = &[
    Node {
        name: "pf_active",
        documentation: "Active poloidal field coils, one entry per coil",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_GREENS_PF_ACTIVE),
    },
    Node {
        name: "pf_passive",
        documentation: "Passive conductors, one entry per conductor. Each carries its own degrees of freedom,
because a passive is represented by a set of current distributions (e.g. eigenmodes)
rather than by a single current",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_GREENS_PF_PASSIVE),
    },
    Node {
        name: "grid_grid",
        documentation: "The plasma grid onto itself: the flux at each grid point due to a unit current at each
other grid point. Toroidal symmetry makes this a function of the vertical *offset* between
the two points, not of their absolute heights, which is why only one vertical index is
stored",
        units: "",
        kind: NodeKind::Structure(NODES_GREENS_GRID_GRID),
    },
];

static NODES_ROOT: &[Node] = &[
    Node {
        name: "vacuum_toroidal_field",
        documentation: "Characteristics of the vacuum toroidal field (used in rho_tor definition and in the normalization of current densities)",
        units: "",
        kind: NodeKind::Structure(NODES_VACUUM_TOROIDAL_FIELD),
    },
    Node {
        name: "grids_ggd",
        documentation: "Grids (using the Generic Grid Description), for various time slices. The timebase of this array of structure must be a subset of the time_slice timebase",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_GRIDS_GGD),
    },
    Node {
        name: "time_slice",
        documentation: "Set of equilibria at various time slices",
        units: "",
        kind: NodeKind::ArrayOfStructures(NODES_TIME_SLICE),
    },
    Node {
        name: "code",
        documentation: "",
        units: "",
        kind: NodeKind::Structure(NODES_CODE),
    },
    Node {
        name: "greens",
        documentation: "Greens tables: the poloidal flux and its derivatives per ampere in each current source",
        units: "",
        kind: NodeKind::Structure(NODES_GREENS),
    },
];

/// The root of the equilibrium data dictionary.
pub static EQUILIBRIUM_ROOT: Node = Node {
    name: "equilibrium",
    documentation: "Description of a 2D, axi-symmetric, tokamak equilibrium; result of an equilibrium code.",
    units: "",
    kind: NodeKind::Structure(NODES_ROOT),
};
