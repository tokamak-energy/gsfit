//! IMAS Pulse_schedule IDS
//!
//! **WARNING** This file is autogenerated by `rust/imas_rs/imas_updater/build_ids.py`.
//! Any changes will be overwritten.
//!
//! This module defines the pulse_schedule Interface Data Structure (IDS), generated from the
//! IMAS Data Dictionary XSD schema.

#![allow(dead_code)]
#![allow(non_camel_case_types)]

use crate::dd_base_types::{Accumulator, EMPTY_INT, Elements, FLT_0D, FLT_1D, INT_0D, INT_1D, STR_0D, STR_1D};
use ndarray::{Dimension, Ix1};
use std::slice::SliceIndex;

// ============================================================================
// Complex Types
// ============================================================================

/// Pulse schedule reference for the pf_active structure
#[derive(Debug, Clone)]
pub struct PulseScheduleReferencePfActive {
    /// Reference name (e.g. in the native pulse schedule system of the device)
    pub reference_name: STR_0D,
    /// Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.
    /// Units: mixed
    pub reference: FLT_1D,
    /// Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node
    pub reference_type: INT_0D,
    /// Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data
    pub envelope_type: INT_0D,
}

impl Default for PulseScheduleReferencePfActive {
    fn default() -> Self {
        Self {
            reference_name: String::new(),
            reference: Default::default(),
            reference_type: EMPTY_INT,
            envelope_type: EMPTY_INT,
        }
    }
}

/// Pulse schedule reference for the density_control structure
#[derive(Debug, Clone)]
pub struct PulseScheduleReferenceDensity {
    /// Reference name (e.g. in the native pulse schedule system of the device)
    pub reference_name: STR_0D,
    /// Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.
    /// Units: mixed
    pub reference: FLT_1D,
    /// Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node
    pub reference_type: INT_0D,
    /// Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data
    pub envelope_type: INT_0D,
}

impl Default for PulseScheduleReferenceDensity {
    fn default() -> Self {
        Self {
            reference_name: String::new(),
            reference: Default::default(),
            reference_type: EMPTY_INT,
            envelope_type: EMPTY_INT,
        }
    }
}

/// Pulse schedule reference for the nbi structure
#[derive(Debug, Clone)]
pub struct PulseScheduleReferenceNbi {
    /// Reference name (e.g. in the native pulse schedule system of the device)
    pub reference_name: STR_0D,
    /// Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.
    /// Units: mixed
    pub reference: FLT_1D,
    /// Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node
    pub reference_type: INT_0D,
    /// Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data
    pub envelope_type: INT_0D,
}

impl Default for PulseScheduleReferenceNbi {
    fn default() -> Self {
        Self {
            reference_name: String::new(),
            reference: Default::default(),
            reference_type: EMPTY_INT,
            envelope_type: EMPTY_INT,
        }
    }
}

/// Pulse schedule reference for the lh structure
#[derive(Debug, Clone)]
pub struct PulseScheduleReferenceLh {
    /// Reference name (e.g. in the native pulse schedule system of the device)
    pub reference_name: STR_0D,
    /// Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.
    /// Units: mixed
    pub reference: FLT_1D,
    /// Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node
    pub reference_type: INT_0D,
    /// Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data
    pub envelope_type: INT_0D,
}

impl Default for PulseScheduleReferenceLh {
    fn default() -> Self {
        Self {
            reference_name: String::new(),
            reference: Default::default(),
            reference_type: EMPTY_INT,
            envelope_type: EMPTY_INT,
        }
    }
}

/// Pulse schedule reference for the ec structure
#[derive(Debug, Clone)]
pub struct PulseScheduleReferenceEc {
    /// Reference name (e.g. in the native pulse schedule system of the device)
    pub reference_name: STR_0D,
    /// Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.
    /// Units: mixed
    pub reference: FLT_1D,
    /// Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node
    pub reference_type: INT_0D,
    /// Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data
    pub envelope_type: INT_0D,
}

impl Default for PulseScheduleReferenceEc {
    fn default() -> Self {
        Self {
            reference_name: String::new(),
            reference: Default::default(),
            reference_type: EMPTY_INT,
            envelope_type: EMPTY_INT,
        }
    }
}

/// Pulse schedule reference for the ic structure
#[derive(Debug, Clone)]
pub struct PulseScheduleReferenceIc {
    /// Reference name (e.g. in the native pulse schedule system of the device)
    pub reference_name: STR_0D,
    /// Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.
    /// Units: mixed
    pub reference: FLT_1D,
    /// Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node
    pub reference_type: INT_0D,
    /// Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower and upper are taken in the strict mathematical sense, without considering absolute values of the data
    pub envelope_type: INT_0D,
}

impl Default for PulseScheduleReferenceIc {
    fn default() -> Self {
        Self {
            reference_name: String::new(),
            reference: Default::default(),
            reference_type: EMPTY_INT,
            envelope_type: EMPTY_INT,
        }
    }
}

/// Pulse schedule reference for the position_control structure
#[derive(Debug, Clone)]
pub struct PulseScheduleReferencePosition {
    /// Reference name (e.g. in the native pulse schedule system of the device)
    pub reference_name: STR_0D,
    /// Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.
    /// Units: mixed
    pub reference: FLT_1D,
    /// Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node
    pub reference_type: INT_0D,
    /// Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data
    pub envelope_type: INT_0D,
}

impl Default for PulseScheduleReferencePosition {
    fn default() -> Self {
        Self {
            reference_name: String::new(),
            reference: Default::default(),
            reference_type: EMPTY_INT,
            envelope_type: EMPTY_INT,
        }
    }
}

/// Pulse schedule reference with relative path to common time not traversing an AoS
#[derive(Debug, Clone)]
pub struct PulseScheduleReferenceNoAos {
    /// Reference name (e.g. in the native pulse schedule system of the device)
    pub reference_name: STR_0D,
    /// Reference waveform. Caution : error bars of the reference/data node are not used in the usual sense, instead they are used to describe the control envelope, with a meaning depending on the chosen envelope_type option.
    /// Units: mixed
    pub reference: FLT_1D,
    /// Reference type:  0:relative (don't use for the moment, to be defined later when segments are introduced in the IDS structure); 1: absolute: the reference time trace is provided in the reference/data node
    pub reference_type: INT_0D,
    /// Envelope type:  0:relative: means that the envelope upper and lower bound values are defined respectively as reference.data * reference.data_error_upper and reference.data * reference.data_error_lower. 1: absolute: the envelope upper and lower bound values are given respectively by reference/data_error_upper and reference/data_error_lower. Lower are upper are taken in the strict mathematical sense, without considering absolute values of the data
    pub envelope_type: INT_0D,
}

impl Default for PulseScheduleReferenceNoAos {
    fn default() -> Self {
        Self {
            reference_name: String::new(),
            reference: Default::default(),
            reference_type: EMPTY_INT,
            envelope_type: EMPTY_INT,
        }
    }
}

/// R,Z position
#[derive(Debug, Clone, Default)]
pub struct PulseScheduleRz {
    /// Major radius
    /// Units: m
    pub r: PulseScheduleReferencePosition,
    /// Height
    /// Units: m
    pub z: PulseScheduleReferencePosition,
}

/// Gap for describing the plasma boundary
#[derive(Debug, Clone)]
pub struct PulseScheduleGap {
    /// Short string identifier (unique for a given device)
    pub name: STR_0D,
    /// Description, e.g. “channel viewing the upper divertor”
    pub description: STR_0D,
    /// Major radius of the reference point
    /// Units: m
    pub r: FLT_0D,
    /// Height of the reference point
    /// Units: m
    pub z: FLT_0D,
    /// Angle between the direction in which the gap is measured (in the poloidal cross-section) and the horizontal axis.
    /// Units: rad
    pub angle: FLT_0D,
    /// Value of the gap, i.e. distance between the reference point and the separatrix along the gap direction
    /// Units: m
    pub value: PulseScheduleReferencePosition,
}

impl Default for PulseScheduleGap {
    fn default() -> Self {
        Self {
            name: String::new(),
            description: String::new(),
            r: f64::NAN,
            z: f64::NAN,
            angle: f64::NAN,
            value: PulseScheduleReferencePosition::default(),
        }
    }
}

/// RZ outline
#[derive(Debug, Clone, Default)]
pub struct PulseScheduleOutline {
    /// Major radius
    /// Units: m
    pub r: PulseScheduleReferencePosition,
    /// Height
    /// Units: m
    pub z: PulseScheduleReferencePosition,
}

/// Event
#[derive(Debug, Clone)]
pub struct PulseScheduleEvent {
    /// Type of this event
    pub r#type: Identifier,
    /// Unique identifier of this event provided by the scheduling / event handler
    pub identifier: STR_0D,
    /// Time stamp of this event
    /// Units: s
    pub time_stamp: FLT_0D,
    /// Duration of this event
    /// Units: s
    pub duration: FLT_0D,
    /// Acquisition strategy related to this event: index = 1 : on-trigger; index = 2 : pre-trigger; index = 3 : post-trigger
    pub acquisition_strategy: Identifier,
    /// Acquisition state of the related system : index = 1 : armed; index = 2 : on; index = 3 : off; index = 4 : closed
    pub acquisition_state: Identifier,
    /// System having generated this event
    pub provider: STR_0D,
    /// Systems listening to this event
    pub listeners: STR_1D,
}

impl Default for PulseScheduleEvent {
    fn default() -> Self {
        Self {
            r#type: Identifier::default(),
            identifier: String::new(),
            time_stamp: f64::NAN,
            duration: f64::NAN,
            acquisition_strategy: Identifier::default(),
            acquisition_state: Identifier::default(),
            provider: String::new(),
            listeners: Default::default(),
        }
    }
}

/// IC antenna
#[derive(Debug, Clone, Default)]
pub struct PulseScheduleIcAntenna {
    /// Short string identifier (unique for a given device)
    pub name: STR_0D,
    /// Description, e.g. “channel viewing the upper divertor”
    pub description: STR_0D,
    /// Type of power used in the sibling power node (defining which power is referred to in this pulse_schedule). Index = 1: power_launched, 2: power_forward (see definitions in the ic_antennas  IDS)
    pub power_type: IdentifierStatic,
    /// Power
    /// Units: W
    pub power: PulseScheduleReferenceIc,
    /// Phase
    /// Units: rad
    pub phase: PulseScheduleReferenceIc,
    /// Frequency
    /// Units: Hz
    pub frequency: PulseScheduleReferenceIc,
}

/// EC beam
#[derive(Debug, Clone, Default)]
pub struct PulseScheduleEcBeam {
    /// Short string identifier (unique for a given device)
    pub name: STR_0D,
    /// Description, e.g. “channel viewing the upper divertor”
    pub description: STR_0D,
    /// Beam power launched into the vacuum vessel
    /// Units: W
    pub power_launched: PulseScheduleReferenceEc,
    /// Frequency
    /// Units: Hz
    pub frequency: PulseScheduleReferenceEc,
    /// Normalized toroidal flux coordinate at which the main deposition should occur
    pub deposition_rho_tor_norm: PulseScheduleReferenceEc,
    /// Steering angle of the EC beam in the R,Z plane (from the -R axis towards the -Z axis), angle_pol=atan2(-k_Z,-k_R), where k_Z and k_R are the Z and R components of the mean wave vector in the EC beam
    /// Units: rad
    pub steering_angle_pol: PulseScheduleReferenceEc,
    /// Steering angle of the EC beam away from the poloidal plane that is increasing towards the positive phi axis, angle_tor=arcsin(k_phi/k), where k_phi is the component of the wave vector in the phi direction and k is the length of the wave vector. Here the term wave vector refers to the mean wave vector in the EC beam
    /// Units: rad
    pub steering_angle_tor: PulseScheduleReferenceEc,
}

/// LH antenna
#[derive(Debug, Clone, Default)]
pub struct PulseScheduleLhAntenna {
    /// Short string identifier (unique for a given device)
    pub name: STR_0D,
    /// Type of power used in the sibling power node (defining which power is referred to in this pulse_schedule). Index = 1: power_launched, 2: power_forward (see definitions in the lh_antennas  IDS)
    pub power_type: IdentifierStatic,
    /// Power
    /// Units: W
    pub power: PulseScheduleReferenceLh,
    /// Phasing between neighbour waveguides (in the toroidal direction)
    /// Units: rad
    pub phase: PulseScheduleReferenceLh,
    /// Main parallel refractive index of the injected wave power spectrum
    pub n_parallel: PulseScheduleReferenceLh,
    /// Frequency
    /// Units: Hz
    pub frequency: PulseScheduleReferenceLh,
    /// Description, e.g. “channel viewing the upper divertor”
    pub description: STR_0D,
}

/// NBI unit
#[derive(Debug, Clone, Default)]
pub struct PulseScheduleNbiUnit {
    /// Short string identifier (unique for a given device)
    pub name: STR_0D,
    /// Description, e.g. “channel viewing the upper divertor”
    pub description: STR_0D,
    /// Species injected by the NBI unit (may be more than one in case the unit injects a gas mixture)
    pub species: Vec<GasMixtureConstant>,
    /// Power launched from this unit into the vacuum vessel
    /// Units: W
    pub power: PulseScheduleReferenceNbi,
    /// Full energy of the injected species (acceleration of a single atom)
    /// Units: eV
    pub energy: PulseScheduleReferenceNbi,
}

/// Neutral beam heating and current drive system
#[derive(Debug, Clone, Default)]
pub struct PulseScheduleNbi {
    /// Set of NBI units
    pub unit: Vec<PulseScheduleNbiUnit>,
    /// Total NBI power (sum over the units)
    /// Units: W
    pub power: PulseScheduleReferenceNbi,
    /// Control mode (operation mode and/or settings used by the controller)
    pub mode: INT_1D,
    /// Timebase for the dynamic nodes located at this level of the IDS structure and below
    /// Units: s
    pub time: FLT_1D,
}

/// Ion cyclotron heating and current drive system
#[derive(Debug, Clone, Default)]
pub struct PulseScheduleIc {
    /// Set of ICRH antennas
    pub antenna: Vec<PulseScheduleIcAntenna>,
    /// Total IC power (sum over the antennas)
    /// Units: W
    pub power: PulseScheduleReferenceIc,
    /// Control mode (operation mode and/or settings used by the controller)
    pub mode: INT_1D,
    /// Timebase for the dynamic nodes located at this level of the IDS structure and below
    /// Units: s
    pub time: FLT_1D,
}

/// Lower hybrid heating and current drive system
#[derive(Debug, Clone, Default)]
pub struct PulseScheduleLh {
    /// Set of LH antennas
    pub antenna: Vec<PulseScheduleLhAntenna>,
    /// Total LH power (sum over the antennas)
    /// Units: W
    pub power: PulseScheduleReferenceLh,
    /// Control mode (operation mode and/or settings used by the controller)
    pub mode: INT_1D,
    /// Timebase for the dynamic nodes located at this level of the IDS structure and below
    /// Units: s
    pub time: FLT_1D,
}

/// Electron cyclotron heating and current drive system
#[derive(Debug, Clone, Default)]
pub struct PulseScheduleEc {
    /// Set of Electron Cyclotron beams
    pub beam: Vec<PulseScheduleEcBeam>,
    /// Total EC power launched in the plasma (sum over the beams)
    /// Units: W
    pub power_launched: PulseScheduleReferenceEc,
    /// Control mode (operation mode and/or settings used by the controller)
    pub mode: INT_1D,
    /// Timebase for the dynamic nodes located at this level of the IDS structure and below
    /// Units: s
    pub time: FLT_1D,
}

/// References for ion species
#[derive(Debug, Clone)]
pub struct PulseScheduleDensityControlIon {
    /// List of elements forming the atom or molecule
    pub element: Vec<PlasmaCompositionNeutralElementConstant>,
    /// Ion charge
    /// Units: e
    pub z_ion: FLT_0D,
    /// String identifying ion (e.g. H, D, T, He, C, D2, ...)
    pub name: STR_0D,
    /// Volume averaged ion density (average over the plasma volume up to the LCFS)
    /// Units: m^-3
    pub n_i_volume_average: PulseScheduleReferenceDensity,
}

impl Default for PulseScheduleDensityControlIon {
    fn default() -> Self {
        Self {
            element: Vec::new(),
            z_ion: f64::NAN,
            name: String::new(),
            n_i_volume_average: PulseScheduleReferenceDensity::default(),
        }
    }
}

/// Gas injection valve
#[derive(Debug, Clone, Default)]
pub struct PulseScheduleDensityControlValve {
    /// Short string identifier (unique for a given device)
    pub name: STR_0D,
    /// Description, e.g. “channel viewing the upper divertor”
    pub description: STR_0D,
    /// Flow rate of the valve
    /// Units: Pa.m^3.s^-1
    pub flow_rate: PulseScheduleReferenceDensity,
    /// Species injected by the valve (may be more than one in case the valve injects a gas mixture)
    pub species: Vec<GasMixtureConstant>,
}

/// Gas injection system
#[derive(Debug, Clone, Default)]
pub struct PulseScheduleDensityControl {
    /// Set of injection valves. Time-dependent
    pub valve: Vec<PulseScheduleDensityControlValve>,
    /// Line integrated electron density over a line of sight in the whole vacuum chamber
    /// Units: m^-2
    pub n_e_line: PulseScheduleReferenceDensity,
    /// Line integrated electron density over a line of sight within the LCFS
    /// Units: m^-2
    pub n_e_line_lcfs: PulseScheduleReferenceDensity,
    /// Integral of a 1D core profile over rho_tor_norm up to the LCFS
    /// Units: m^-3
    pub n_e_profile_average: PulseScheduleReferenceDensity,
    /// Description of the line of sight for calculating n_e, defined by two points when the beam is not reflected, a third point is added to define the reflected beam path
    pub n_e_line_of_sight: LineOfSight3points,
    /// Volume averaged electron density (average over the plasma volume up to the LCFS)
    /// Units: m^-3
    pub n_e_volume_average: PulseScheduleReferenceDensity,
    /// Line averaged effective charge
    pub zeff: PulseScheduleReferenceDensity,
    /// Method for zeff calculation : Index = 1: average over a line of sight in the whole vacuum chamber, 2 : average over a line of sight within the LCFS, 3 : average of a 1D core profile over rho_tor_norm up to the LCFS
    pub zeff_method: IdentifierStatic,
    /// Description of the line of sight for calculating zeff, defined by two points when the beam is not reflected, a third point is added to define the reflected beam path
    pub zeff_line_of_sight: LineOfSight3points,
    /// Average ratio of tritium over deuterium density
    pub n_t_over_n_d: PulseScheduleReferenceDensity,
    /// Average ratio of hydrogen over deuterium density
    pub n_h_over_n_d: PulseScheduleReferenceDensity,
    /// Quantities related to the different ion species, in the sense of isonuclear or isomolecular sequences
    pub ion: Vec<PulseScheduleDensityControlIon>,
    /// Control mode (operation mode and/or settings used by the controller)
    pub mode: INT_1D,
    /// Timebase for the dynamic nodes located at this level of the IDS structure and below
    /// Units: s
    pub time: FLT_1D,
}

/// Flux control references
#[derive(Debug, Clone, Default)]
pub struct PulseScheduleFluxControl {
    /// Plasma current
    /// Units: A
    pub ip: PulseScheduleReferenceNoAos,
    /// Loop voltage
    /// Units: V
    pub v_loop: PulseScheduleReferenceNoAos,
    /// Internal inductance
    pub li_3: PulseScheduleReferenceNoAos,
    /// Normalized toroidal beta, defined as 100 * beta_tor * a[m] * B0 [T] / ip [MA]
    pub beta_tor_norm: PulseScheduleReferenceNoAos,
    /// Control mode (operation mode and/or settings used by the controller)
    pub mode: INT_1D,
    /// Timebase for the dynamic nodes located at this level of the IDS structure and below
    /// Units: s
    pub time: FLT_1D,
}

/// PF supply
#[derive(Debug, Clone, Default)]
pub struct PulseSchedulePfActiveSupply {
    /// Short string identifier (unique for a given device)
    pub name: STR_0D,
    /// Description, e.g. “channel viewing the upper divertor”
    pub description: STR_0D,
    /// Voltage at the supply output (Vside1-Vside2)
    /// Units: V
    pub voltage: PulseScheduleReferencePfActive,
    /// Current fed into one turn of the coil. (Multiply by number of turns to obtain generated magnetic field). Positive when flowing from side 1 to side 2 of the coil, this numbering being made consistently with the convention that the current flows counter-clockwise when viewed from above.
    /// Units: A
    pub current: PulseScheduleReferencePfActive,
}

/// PF coil
#[derive(Debug, Clone, Default)]
pub struct PulseSchedulePfActiveCoil {
    /// Short string identifier (unique for a given device)
    pub name: STR_0D,
    /// Description, e.g. “channel viewing the upper divertor”
    pub description: STR_0D,
    /// Reference waveform of current to be fed into the coil (for 1 turn, to be multiplied by the number of turns to obtain the generated magnetic field).  Positive when the current flows counter-clockwise in the coil when viewed from above.
    /// Units: A
    pub current: PulseScheduleReferencePfActive,
    /// Additional resistance due to e.g. dynamically switchable resistors
    /// Units: ohm
    pub resistance_additional: PulseScheduleReferencePfActive,
}

/// PF coils references
#[derive(Debug, Clone, Default)]
pub struct PulseSchedulePfActive {
    /// Set of poloidal field coils
    pub coil: Vec<PulseSchedulePfActiveCoil>,
    /// Set of PF power supplies
    pub supply: Vec<PulseSchedulePfActiveSupply>,
    /// Control mode (operation mode and/or settings used by the controller)
    pub mode: INT_1D,
    /// Timebase for the dynamic nodes located at this level of the IDS structure and below
    /// Units: s
    pub time: FLT_1D,
}

/// Position control references
#[derive(Debug, Clone, Default)]
pub struct PulseSchedulePosition {
    /// Magnetic axis position
    pub magnetic_axis: PulseScheduleRz,
    /// RZ position of the geometric axis (defined as (Rmin+Rmax) / 2 and (Zmin+Zmax) / 2 of the boundary)
    pub geometric_axis: PulseScheduleRz,
    /// Minor radius of the plasma boundary (defined as (Rmax-Rmin) / 2 of the boundary)
    /// Units: m
    pub minor_radius: PulseScheduleReferencePosition,
    /// Elongation of the plasma boundary
    pub elongation: PulseScheduleReferencePosition,
    /// Elongation (upper half w.r.t. geometric axis) of the plasma boundary
    pub elongation_upper: PulseScheduleReferencePosition,
    /// Elongation (lower half w.r.t. geometric axis) of the plasma boundary
    pub elongation_lower: PulseScheduleReferencePosition,
    /// Triangularity of the plasma boundary
    pub triangularity: PulseScheduleReferencePosition,
    /// Upper triangularity of the plasma boundary
    pub triangularity_upper: PulseScheduleReferencePosition,
    /// Lower triangularity of the plasma boundary
    pub triangularity_lower: PulseScheduleReferencePosition,
    /// Inner triangularity of the plasma boundary
    pub triangularity_inner: PulseScheduleReferencePosition,
    /// Outer triangularity of the plasma boundary
    pub triangularity_outer: PulseScheduleReferencePosition,
    /// Minor triangularity of the plasma boundary
    pub triangularity_minor: PulseScheduleReferencePosition,
    /// Upper outer squareness of the plasma boundary (definition from T. Luce, Plasma Phys. Control. Fusion 55 (2013) 095009)
    pub squareness_upper_outer: PulseScheduleReferencePosition,
    /// Upper inner squareness of the plasma boundary (definition from T. Luce, Plasma Phys. Control. Fusion 55 (2013) 095009)
    pub squareness_upper_inner: PulseScheduleReferencePosition,
    /// Lower outer squareness of the plasma boundary (definition from T. Luce, Plasma Phys. Control. Fusion 55 (2013) 095009)
    pub squareness_lower_outer: PulseScheduleReferencePosition,
    /// Lower inner squareness of the plasma boundary (definition from T. Luce, Plasma Phys. Control. Fusion 55 (2013) 095009)
    pub squareness_lower_inner: PulseScheduleReferencePosition,
    /// Array of X-points, for each of them the RZ position is given
    pub x_point: Vec<PulseScheduleRz>,
    /// Array of strike points, for each of them the RZ position is given
    pub strike_point: Vec<PulseScheduleRz>,
    /// RZ position of the active limiter point (point of the plasma boundary in contact with the limiter)
    pub active_limiter_point: PulseScheduleRz,
    /// Set of (R,Z) points defining the outline of the plasma boundary
    pub boundary_outline: Vec<PulseScheduleOutline>,
    /// Height of the separatrix point of maximum major radius
    /// Units: m
    pub z_r_max: PulseScheduleReferencePosition,
    /// Height of the separatrix point of minimum major radius
    /// Units: m
    pub z_r_min: PulseScheduleReferencePosition,
    /// Set of gaps, defined by a reference point and a direction.
    pub gap: Vec<PulseScheduleGap>,
    /// RZ position of the current centroid
    pub current_centroid: PulseScheduleRz,
    /// Control mode (operation mode and/or settings used by the controller)
    pub mode: INT_1D,
    /// Timebase for the dynamic nodes located at this level of the IDS structure and below
    /// Units: s
    pub time: FLT_1D,
}

/// Toroidal field references
#[derive(Debug, Clone, Default)]
pub struct PulseScheduleTf {
    /// Vacuum magnetic field times major radius within the cage of toroidal field coils. Positive sign means magnetic field is orientated counter-clockwise when viewed from above.
    /// Units: T.m
    pub b_field_tor_vacuum_r: PulseScheduleReferenceNoAos,
    /// Control mode (operation mode and/or settings used by the controller)
    pub mode: INT_1D,
    /// Timebase for the dynamic nodes located at this level of the IDS structure and below
    /// Units: s
    pub time: FLT_1D,
}

/// Standard type for identifiers (constant). The three fields: name, index and description are all representations of the same information. Associated with each application of this identifier-type, there should be a translation table defining the three fields for all objects to be identified.
#[derive(Debug, Clone)]
pub struct Identifier {
    /// Short string identifier
    pub name: STR_0D,
    /// Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.
    pub index: INT_0D,
    /// Verbose description
    pub description: STR_0D,
}

impl Default for Identifier {
    fn default() -> Self {
        Self {
            name: String::new(),
            index: EMPTY_INT,
            description: String::new(),
        }
    }
}

/// Standard type for identifiers (static). The three fields: name, index and description are all representations of the same information. Associated with each application of this identifier-type, there should be a translation table defining the three fields for all objects to be identified.
#[derive(Debug, Clone)]
pub struct IdentifierStatic {
    /// Short string identifier
    pub name: STR_0D,
    /// Integer identifier (enumeration index within a list). Private identifier values must be indicated by a negative index.
    pub index: INT_0D,
    /// Verbose description
    pub description: STR_0D,
}

impl Default for IdentifierStatic {
    fn default() -> Self {
        Self {
            name: String::new(),
            index: EMPTY_INT,
            description: String::new(),
        }
    }
}

/// Description of a neutral species within a gas mixture (constant)
#[derive(Debug, Clone)]
pub struct GasMixtureConstant {
    /// List of elements forming the atom or molecule
    pub element: Vec<PlasmaCompositionNeutralElementConstant>,
    /// String identifying neutral (e.g. H, D, T, He, C, ...)
    pub name: STR_0D,
    /// Relative fraction of this species (in molecules) in the gas mixture
    pub fraction: FLT_0D,
}

impl Default for GasMixtureConstant {
    fn default() -> Self {
        Self {
            element: Vec::new(),
            name: String::new(),
            fraction: f64::NAN,
        }
    }
}

/// Element entering in the composition of the neutral atom or molecule (constant)
#[derive(Debug, Clone)]
pub struct PlasmaCompositionNeutralElementConstant {
    /// Mass of atom
    /// Units: u
    pub a: FLT_0D,
    /// Nuclear charge
    /// Units: e
    pub z_n: INT_0D,
    /// Number of atoms of this element in the molecule
    pub atoms_n: INT_0D,
}

impl Default for PlasmaCompositionNeutralElementConstant {
    fn default() -> Self {
        Self {
            a: f64::NAN,
            z_n: EMPTY_INT,
            atoms_n: EMPTY_INT,
        }
    }
}

/// Generic description of a line of sight, defined by two points (one way) and an optional third point to indicate the direction of reflection if the second point is e.g. the position of a mirror reflecting the line-of-sight
#[derive(Debug, Clone, Default)]
pub struct LineOfSight3points {
    /// Position of the first point
    pub first_point: Rphiz0dStatic,
    /// Position of the second point
    pub second_point: Rphiz0dStatic,
    /// Position of the third point
    pub third_point: Rphiz0dStatic,
}

/// Generic decription of the code-specific parameters for the code that has produced this IDS
#[derive(Debug, Clone, Default)]
pub struct Code {
    /// Name of software generating IDS
    pub name: STR_0D,
    /// Short description of the software (type, purpose)
    pub description: STR_0D,
    /// Unique commit reference of software
    pub commit: STR_0D,
    /// Unique version (tag) of software
    pub version: STR_0D,
    /// URL of software repository
    pub repository: STR_0D,
    /// List of the code specific parameters in XML format
    pub parameters: STR_0D,
    /// Output flag : 0 means the run is successful, other values mean some difficulty has been encountered, the exact meaning is then code specific. Negative values mean the result shall not be used.
    pub output_flag: INT_1D,
    /// List of external libraries used by the code that has produced this IDS
    pub library: Vec<Library>,
}

/// Structure for R, Z, Phi positions (0D, static)
#[derive(Debug, Clone)]
pub struct Rphiz0dStatic {
    /// Major radius
    /// Units: m
    pub r: FLT_0D,
    /// Toroidal angle (oriented counter-clockwise when viewed from above)
    /// Units: rad
    pub phi: FLT_0D,
    /// Height
    /// Units: m
    pub z: FLT_0D,
}

impl Default for Rphiz0dStatic {
    fn default() -> Self {
        Self {
            r: f64::NAN,
            phi: f64::NAN,
            z: f64::NAN,
        }
    }
}

/// Library used by the code that has produced this IDS
#[derive(Debug, Clone, Default)]
pub struct Library {
    /// Name of software
    pub name: STR_0D,
    /// Short description of the software (type, purpose)
    pub description: STR_0D,
    /// Unique commit reference of software
    pub commit: STR_0D,
    /// Unique version (tag) of software
    pub version: STR_0D,
    /// URL of software repository
    pub repository: STR_0D,
    /// List of the code specific parameters in XML format
    pub parameters: STR_0D,
}

// ============================================================================
// Root IDS Structure
// ============================================================================

/// Description of Pulse Schedule, described by subsystems waveform references and an enveloppe around them. The controllers, pulse schedule and SDN are defined in separate IDSs. All names and identifiers of subsystems appearing in the pulse_schedule must be identical to those used in the IDSs describing the related subsystems.
#[derive(Debug, Clone, Default)]
pub struct PulseSchedule {
    /// Ion cyclotron heating and current drive system
    pub ic: PulseScheduleIc,
    /// Electron cyclotron heating and current drive system
    pub ec: PulseScheduleEc,
    /// Lower Hybrid heating and current drive system
    pub lh: PulseScheduleLh,
    /// Neutral beam heating and current drive system
    pub nbi: PulseScheduleNbi,
    /// Gas injection system and density control references
    pub density_control: PulseScheduleDensityControl,
    /// List of events, either predefined triggers  or events recorded during the pulse
    pub event: Vec<PulseScheduleEvent>,
    /// Magnetic flux control references
    pub flux_control: PulseScheduleFluxControl,
    /// Poloidal field coil references
    pub pf_active: PulseSchedulePfActive,
    /// Plasma position and shape control references
    pub position_control: PulseSchedulePosition,
    /// Toroidal field references
    pub tf: PulseScheduleTf,
    pub code: Code,
}

// ============================================================================
// View, Accessor, and Accumulator Types
// ============================================================================

// --- GasMixtureConstant View Types ---

/// View over multiple GasMixtureConstant with field accumulation
pub struct GasMixtureConstantSliceView<'a, D> {
    pub name: Accumulator<'a, GasMixtureConstant, STR_0D, D>,
    pub fraction: Accumulator<'a, GasMixtureConstant, FLT_0D, D>,
    slice_elements: Elements<'a, GasMixtureConstant, D>,
}

impl<'a, D: Dimension> GasMixtureConstantSliceView<'a, D> {
    pub fn new(elements: Elements<'a, GasMixtureConstant, D>) -> Self {
        Self {
            name: Accumulator::new(elements.clone(), |item: &GasMixtureConstant| item.name.clone()),
            fraction: Accumulator::new(elements.clone(), |item: &GasMixtureConstant| item.fraction),
            slice_elements: elements,
        }
    }

    /// Total number of elements, across every sliced level
    pub fn len(&self) -> usize {
        self.slice_elements.len()
    }

    pub fn is_empty(&self) -> bool {
        self.slice_elements.is_empty()
    }

    /// The length of each sliced level, in path order like the gathered arrays
    pub fn shape(&self) -> Vec<usize> {
        self.slice_elements.shape()
    }

    /// Every element, in path order: the first array of structures on the path varies slowest
    pub fn iter(&self) -> impl Iterator<Item = &'a GasMixtureConstant> + '_ {
        self.slice_elements.iter()
    }

    /// The slice view over a range of `element` under every element, e.g. `.element(..)`.
    /// It adds one dimension, after those of the levels sliced above it and before the leaf's own.
    ///
    /// # Panics
    /// If `range` does not select the same number of elements under every element.
    pub fn element<R>(&self, range: R) -> PlasmaCompositionNeutralElementConstantSliceView<'a, D::Larger>
    where
        R: SliceIndex<[PlasmaCompositionNeutralElementConstant], Output = [PlasmaCompositionNeutralElementConstant]> + Clone,
    {
        PlasmaCompositionNeutralElementConstantSliceView::new(self.slice_elements.nest("element", |item: &GasMixtureConstant| item.element.as_slice(), range))
    }
}

// --- PulseScheduleNbiUnit View Types ---

/// View over `power` (PulseScheduleReferenceNbi) across multiple PulseScheduleNbiUnit
pub struct PulseScheduleNbiUnitPowerView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleNbiUnit, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleNbiUnit, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleNbiUnit, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleNbiUnit, D>,
}

impl<'a, D: Dimension> PulseScheduleNbiUnitPowerView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleNbiUnit, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleNbiUnit| item.power.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleNbiUnit| item.power.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleNbiUnit| item.power.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over `energy` (PulseScheduleReferenceNbi) across multiple PulseScheduleNbiUnit
pub struct PulseScheduleNbiUnitEnergyView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleNbiUnit, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleNbiUnit, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleNbiUnit, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleNbiUnit, D>,
}

impl<'a, D: Dimension> PulseScheduleNbiUnitEnergyView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleNbiUnit, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleNbiUnit| item.energy.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleNbiUnit| item.energy.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleNbiUnit| item.energy.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over multiple PulseScheduleNbiUnit with field accumulation
pub struct PulseScheduleNbiUnitSliceView<'a, D> {
    pub name: Accumulator<'a, PulseScheduleNbiUnit, STR_0D, D>,
    pub description: Accumulator<'a, PulseScheduleNbiUnit, STR_0D, D>,
    pub power: PulseScheduleNbiUnitPowerView<'a, D>,
    pub energy: PulseScheduleNbiUnitEnergyView<'a, D>,
    slice_elements: Elements<'a, PulseScheduleNbiUnit, D>,
}

impl<'a, D: Dimension> PulseScheduleNbiUnitSliceView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleNbiUnit, D>) -> Self {
        Self {
            name: Accumulator::new(elements.clone(), |item: &PulseScheduleNbiUnit| item.name.clone()),
            description: Accumulator::new(elements.clone(), |item: &PulseScheduleNbiUnit| item.description.clone()),
            power: PulseScheduleNbiUnitPowerView::new(elements.clone()),
            energy: PulseScheduleNbiUnitEnergyView::new(elements.clone()),
            slice_elements: elements,
        }
    }

    /// Total number of elements, across every sliced level
    pub fn len(&self) -> usize {
        self.slice_elements.len()
    }

    pub fn is_empty(&self) -> bool {
        self.slice_elements.is_empty()
    }

    /// The length of each sliced level, in path order like the gathered arrays
    pub fn shape(&self) -> Vec<usize> {
        self.slice_elements.shape()
    }

    /// Every element, in path order: the first array of structures on the path varies slowest
    pub fn iter(&self) -> impl Iterator<Item = &'a PulseScheduleNbiUnit> + '_ {
        self.slice_elements.iter()
    }

    /// The slice view over a range of `species` under every element, e.g. `.species(..)`.
    /// It adds one dimension, after those of the levels sliced above it and before the leaf's own.
    ///
    /// # Panics
    /// If `range` does not select the same number of elements under every element.
    pub fn species<R>(&self, range: R) -> GasMixtureConstantSliceView<'a, D::Larger>
    where
        R: SliceIndex<[GasMixtureConstant], Output = [GasMixtureConstant]> + Clone,
    {
        GasMixtureConstantSliceView::new(
            self.slice_elements
                .nest("species", |item: &PulseScheduleNbiUnit| item.species.as_slice(), range),
        )
    }
}

// --- PulseScheduleIcAntenna View Types ---

/// View over `power_type` (IdentifierStatic) across multiple PulseScheduleIcAntenna
pub struct PulseScheduleIcAntennaPowerTypeView<'a, D> {
    pub name: Accumulator<'a, PulseScheduleIcAntenna, STR_0D, D>,
    pub index: Accumulator<'a, PulseScheduleIcAntenna, INT_0D, D>,
    pub description: Accumulator<'a, PulseScheduleIcAntenna, STR_0D, D>,
    slice_elements: Elements<'a, PulseScheduleIcAntenna, D>,
}

impl<'a, D: Dimension> PulseScheduleIcAntennaPowerTypeView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleIcAntenna, D>) -> Self {
        Self {
            name: Accumulator::new(elements.clone(), |item: &PulseScheduleIcAntenna| item.power_type.name.clone()),
            index: Accumulator::new(elements.clone(), |item: &PulseScheduleIcAntenna| item.power_type.index),
            description: Accumulator::new(elements.clone(), |item: &PulseScheduleIcAntenna| item.power_type.description.clone()),
            slice_elements: elements,
        }
    }
}

/// View over `power` (PulseScheduleReferenceIc) across multiple PulseScheduleIcAntenna
pub struct PulseScheduleIcAntennaPowerView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleIcAntenna, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleIcAntenna, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleIcAntenna, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleIcAntenna, D>,
}

impl<'a, D: Dimension> PulseScheduleIcAntennaPowerView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleIcAntenna, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleIcAntenna| item.power.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleIcAntenna| item.power.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleIcAntenna| item.power.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over `phase` (PulseScheduleReferenceIc) across multiple PulseScheduleIcAntenna
pub struct PulseScheduleIcAntennaPhaseView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleIcAntenna, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleIcAntenna, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleIcAntenna, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleIcAntenna, D>,
}

impl<'a, D: Dimension> PulseScheduleIcAntennaPhaseView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleIcAntenna, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleIcAntenna| item.phase.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleIcAntenna| item.phase.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleIcAntenna| item.phase.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over `frequency` (PulseScheduleReferenceIc) across multiple PulseScheduleIcAntenna
pub struct PulseScheduleIcAntennaFrequencyView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleIcAntenna, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleIcAntenna, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleIcAntenna, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleIcAntenna, D>,
}

impl<'a, D: Dimension> PulseScheduleIcAntennaFrequencyView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleIcAntenna, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleIcAntenna| item.frequency.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleIcAntenna| item.frequency.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleIcAntenna| item.frequency.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over multiple PulseScheduleIcAntenna with field accumulation
pub struct PulseScheduleIcAntennaSliceView<'a, D> {
    pub name: Accumulator<'a, PulseScheduleIcAntenna, STR_0D, D>,
    pub description: Accumulator<'a, PulseScheduleIcAntenna, STR_0D, D>,
    pub power_type: PulseScheduleIcAntennaPowerTypeView<'a, D>,
    pub power: PulseScheduleIcAntennaPowerView<'a, D>,
    pub phase: PulseScheduleIcAntennaPhaseView<'a, D>,
    pub frequency: PulseScheduleIcAntennaFrequencyView<'a, D>,
    slice_elements: Elements<'a, PulseScheduleIcAntenna, D>,
}

impl<'a, D: Dimension> PulseScheduleIcAntennaSliceView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleIcAntenna, D>) -> Self {
        Self {
            name: Accumulator::new(elements.clone(), |item: &PulseScheduleIcAntenna| item.name.clone()),
            description: Accumulator::new(elements.clone(), |item: &PulseScheduleIcAntenna| item.description.clone()),
            power_type: PulseScheduleIcAntennaPowerTypeView::new(elements.clone()),
            power: PulseScheduleIcAntennaPowerView::new(elements.clone()),
            phase: PulseScheduleIcAntennaPhaseView::new(elements.clone()),
            frequency: PulseScheduleIcAntennaFrequencyView::new(elements.clone()),
            slice_elements: elements,
        }
    }

    /// Total number of elements, across every sliced level
    pub fn len(&self) -> usize {
        self.slice_elements.len()
    }

    pub fn is_empty(&self) -> bool {
        self.slice_elements.is_empty()
    }

    /// The length of each sliced level, in path order like the gathered arrays
    pub fn shape(&self) -> Vec<usize> {
        self.slice_elements.shape()
    }

    /// Every element, in path order: the first array of structures on the path varies slowest
    pub fn iter(&self) -> impl Iterator<Item = &'a PulseScheduleIcAntenna> + '_ {
        self.slice_elements.iter()
    }
}

// --- PulseScheduleLhAntenna View Types ---

/// View over `power_type` (IdentifierStatic) across multiple PulseScheduleLhAntenna
pub struct PulseScheduleLhAntennaPowerTypeView<'a, D> {
    pub name: Accumulator<'a, PulseScheduleLhAntenna, STR_0D, D>,
    pub index: Accumulator<'a, PulseScheduleLhAntenna, INT_0D, D>,
    pub description: Accumulator<'a, PulseScheduleLhAntenna, STR_0D, D>,
    slice_elements: Elements<'a, PulseScheduleLhAntenna, D>,
}

impl<'a, D: Dimension> PulseScheduleLhAntennaPowerTypeView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleLhAntenna, D>) -> Self {
        Self {
            name: Accumulator::new(elements.clone(), |item: &PulseScheduleLhAntenna| item.power_type.name.clone()),
            index: Accumulator::new(elements.clone(), |item: &PulseScheduleLhAntenna| item.power_type.index),
            description: Accumulator::new(elements.clone(), |item: &PulseScheduleLhAntenna| item.power_type.description.clone()),
            slice_elements: elements,
        }
    }
}

/// View over `power` (PulseScheduleReferenceLh) across multiple PulseScheduleLhAntenna
pub struct PulseScheduleLhAntennaPowerView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleLhAntenna, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleLhAntenna, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleLhAntenna, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleLhAntenna, D>,
}

impl<'a, D: Dimension> PulseScheduleLhAntennaPowerView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleLhAntenna, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleLhAntenna| item.power.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleLhAntenna| item.power.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleLhAntenna| item.power.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over `phase` (PulseScheduleReferenceLh) across multiple PulseScheduleLhAntenna
pub struct PulseScheduleLhAntennaPhaseView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleLhAntenna, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleLhAntenna, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleLhAntenna, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleLhAntenna, D>,
}

impl<'a, D: Dimension> PulseScheduleLhAntennaPhaseView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleLhAntenna, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleLhAntenna| item.phase.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleLhAntenna| item.phase.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleLhAntenna| item.phase.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over `n_parallel` (PulseScheduleReferenceLh) across multiple PulseScheduleLhAntenna
pub struct PulseScheduleLhAntennaNParallelView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleLhAntenna, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleLhAntenna, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleLhAntenna, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleLhAntenna, D>,
}

impl<'a, D: Dimension> PulseScheduleLhAntennaNParallelView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleLhAntenna, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleLhAntenna| item.n_parallel.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleLhAntenna| item.n_parallel.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleLhAntenna| item.n_parallel.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over `frequency` (PulseScheduleReferenceLh) across multiple PulseScheduleLhAntenna
pub struct PulseScheduleLhAntennaFrequencyView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleLhAntenna, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleLhAntenna, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleLhAntenna, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleLhAntenna, D>,
}

impl<'a, D: Dimension> PulseScheduleLhAntennaFrequencyView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleLhAntenna, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleLhAntenna| item.frequency.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleLhAntenna| item.frequency.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleLhAntenna| item.frequency.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over multiple PulseScheduleLhAntenna with field accumulation
pub struct PulseScheduleLhAntennaSliceView<'a, D> {
    pub name: Accumulator<'a, PulseScheduleLhAntenna, STR_0D, D>,
    pub power_type: PulseScheduleLhAntennaPowerTypeView<'a, D>,
    pub power: PulseScheduleLhAntennaPowerView<'a, D>,
    pub phase: PulseScheduleLhAntennaPhaseView<'a, D>,
    pub n_parallel: PulseScheduleLhAntennaNParallelView<'a, D>,
    pub frequency: PulseScheduleLhAntennaFrequencyView<'a, D>,
    pub description: Accumulator<'a, PulseScheduleLhAntenna, STR_0D, D>,
    slice_elements: Elements<'a, PulseScheduleLhAntenna, D>,
}

impl<'a, D: Dimension> PulseScheduleLhAntennaSliceView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleLhAntenna, D>) -> Self {
        Self {
            name: Accumulator::new(elements.clone(), |item: &PulseScheduleLhAntenna| item.name.clone()),
            power_type: PulseScheduleLhAntennaPowerTypeView::new(elements.clone()),
            power: PulseScheduleLhAntennaPowerView::new(elements.clone()),
            phase: PulseScheduleLhAntennaPhaseView::new(elements.clone()),
            n_parallel: PulseScheduleLhAntennaNParallelView::new(elements.clone()),
            frequency: PulseScheduleLhAntennaFrequencyView::new(elements.clone()),
            description: Accumulator::new(elements.clone(), |item: &PulseScheduleLhAntenna| item.description.clone()),
            slice_elements: elements,
        }
    }

    /// Total number of elements, across every sliced level
    pub fn len(&self) -> usize {
        self.slice_elements.len()
    }

    pub fn is_empty(&self) -> bool {
        self.slice_elements.is_empty()
    }

    /// The length of each sliced level, in path order like the gathered arrays
    pub fn shape(&self) -> Vec<usize> {
        self.slice_elements.shape()
    }

    /// Every element, in path order: the first array of structures on the path varies slowest
    pub fn iter(&self) -> impl Iterator<Item = &'a PulseScheduleLhAntenna> + '_ {
        self.slice_elements.iter()
    }
}

// --- PulseScheduleEcBeam View Types ---

/// View over `power_launched` (PulseScheduleReferenceEc) across multiple PulseScheduleEcBeam
pub struct PulseScheduleEcBeamPowerLaunchedView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleEcBeam, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleEcBeam, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleEcBeam, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleEcBeam, D>,
}

impl<'a, D: Dimension> PulseScheduleEcBeamPowerLaunchedView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleEcBeam, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleEcBeam| item.power_launched.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleEcBeam| item.power_launched.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleEcBeam| item.power_launched.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over `frequency` (PulseScheduleReferenceEc) across multiple PulseScheduleEcBeam
pub struct PulseScheduleEcBeamFrequencyView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleEcBeam, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleEcBeam, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleEcBeam, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleEcBeam, D>,
}

impl<'a, D: Dimension> PulseScheduleEcBeamFrequencyView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleEcBeam, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleEcBeam| item.frequency.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleEcBeam| item.frequency.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleEcBeam| item.frequency.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over `deposition_rho_tor_norm` (PulseScheduleReferenceEc) across multiple PulseScheduleEcBeam
pub struct PulseScheduleEcBeamDepositionRhoTorNormView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleEcBeam, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleEcBeam, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleEcBeam, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleEcBeam, D>,
}

impl<'a, D: Dimension> PulseScheduleEcBeamDepositionRhoTorNormView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleEcBeam, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleEcBeam| {
                item.deposition_rho_tor_norm.reference_name.clone()
            }),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleEcBeam| item.deposition_rho_tor_norm.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleEcBeam| item.deposition_rho_tor_norm.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over `steering_angle_pol` (PulseScheduleReferenceEc) across multiple PulseScheduleEcBeam
pub struct PulseScheduleEcBeamSteeringAnglePolView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleEcBeam, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleEcBeam, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleEcBeam, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleEcBeam, D>,
}

impl<'a, D: Dimension> PulseScheduleEcBeamSteeringAnglePolView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleEcBeam, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleEcBeam| item.steering_angle_pol.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleEcBeam| item.steering_angle_pol.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleEcBeam| item.steering_angle_pol.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over `steering_angle_tor` (PulseScheduleReferenceEc) across multiple PulseScheduleEcBeam
pub struct PulseScheduleEcBeamSteeringAngleTorView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleEcBeam, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleEcBeam, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleEcBeam, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleEcBeam, D>,
}

impl<'a, D: Dimension> PulseScheduleEcBeamSteeringAngleTorView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleEcBeam, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleEcBeam| item.steering_angle_tor.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleEcBeam| item.steering_angle_tor.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleEcBeam| item.steering_angle_tor.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over multiple PulseScheduleEcBeam with field accumulation
pub struct PulseScheduleEcBeamSliceView<'a, D> {
    pub name: Accumulator<'a, PulseScheduleEcBeam, STR_0D, D>,
    pub description: Accumulator<'a, PulseScheduleEcBeam, STR_0D, D>,
    pub power_launched: PulseScheduleEcBeamPowerLaunchedView<'a, D>,
    pub frequency: PulseScheduleEcBeamFrequencyView<'a, D>,
    pub deposition_rho_tor_norm: PulseScheduleEcBeamDepositionRhoTorNormView<'a, D>,
    pub steering_angle_pol: PulseScheduleEcBeamSteeringAnglePolView<'a, D>,
    pub steering_angle_tor: PulseScheduleEcBeamSteeringAngleTorView<'a, D>,
    slice_elements: Elements<'a, PulseScheduleEcBeam, D>,
}

impl<'a, D: Dimension> PulseScheduleEcBeamSliceView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleEcBeam, D>) -> Self {
        Self {
            name: Accumulator::new(elements.clone(), |item: &PulseScheduleEcBeam| item.name.clone()),
            description: Accumulator::new(elements.clone(), |item: &PulseScheduleEcBeam| item.description.clone()),
            power_launched: PulseScheduleEcBeamPowerLaunchedView::new(elements.clone()),
            frequency: PulseScheduleEcBeamFrequencyView::new(elements.clone()),
            deposition_rho_tor_norm: PulseScheduleEcBeamDepositionRhoTorNormView::new(elements.clone()),
            steering_angle_pol: PulseScheduleEcBeamSteeringAnglePolView::new(elements.clone()),
            steering_angle_tor: PulseScheduleEcBeamSteeringAngleTorView::new(elements.clone()),
            slice_elements: elements,
        }
    }

    /// Total number of elements, across every sliced level
    pub fn len(&self) -> usize {
        self.slice_elements.len()
    }

    pub fn is_empty(&self) -> bool {
        self.slice_elements.is_empty()
    }

    /// The length of each sliced level, in path order like the gathered arrays
    pub fn shape(&self) -> Vec<usize> {
        self.slice_elements.shape()
    }

    /// Every element, in path order: the first array of structures on the path varies slowest
    pub fn iter(&self) -> impl Iterator<Item = &'a PulseScheduleEcBeam> + '_ {
        self.slice_elements.iter()
    }
}

// --- PlasmaCompositionNeutralElementConstant View Types ---

/// View over multiple PlasmaCompositionNeutralElementConstant with field accumulation
pub struct PlasmaCompositionNeutralElementConstantSliceView<'a, D> {
    pub a: Accumulator<'a, PlasmaCompositionNeutralElementConstant, FLT_0D, D>,
    pub z_n: Accumulator<'a, PlasmaCompositionNeutralElementConstant, INT_0D, D>,
    pub atoms_n: Accumulator<'a, PlasmaCompositionNeutralElementConstant, INT_0D, D>,
    slice_elements: Elements<'a, PlasmaCompositionNeutralElementConstant, D>,
}

impl<'a, D: Dimension> PlasmaCompositionNeutralElementConstantSliceView<'a, D> {
    pub fn new(elements: Elements<'a, PlasmaCompositionNeutralElementConstant, D>) -> Self {
        Self {
            a: Accumulator::new(elements.clone(), |item: &PlasmaCompositionNeutralElementConstant| item.a),
            z_n: Accumulator::new(elements.clone(), |item: &PlasmaCompositionNeutralElementConstant| item.z_n),
            atoms_n: Accumulator::new(elements.clone(), |item: &PlasmaCompositionNeutralElementConstant| item.atoms_n),
            slice_elements: elements,
        }
    }

    /// Total number of elements, across every sliced level
    pub fn len(&self) -> usize {
        self.slice_elements.len()
    }

    pub fn is_empty(&self) -> bool {
        self.slice_elements.is_empty()
    }

    /// The length of each sliced level, in path order like the gathered arrays
    pub fn shape(&self) -> Vec<usize> {
        self.slice_elements.shape()
    }

    /// Every element, in path order: the first array of structures on the path varies slowest
    pub fn iter(&self) -> impl Iterator<Item = &'a PlasmaCompositionNeutralElementConstant> + '_ {
        self.slice_elements.iter()
    }
}

// --- PulseScheduleDensityControlValve View Types ---

/// View over `flow_rate` (PulseScheduleReferenceDensity) across multiple PulseScheduleDensityControlValve
pub struct PulseScheduleDensityControlValveFlowRateView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleDensityControlValve, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleDensityControlValve, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleDensityControlValve, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleDensityControlValve, D>,
}

impl<'a, D: Dimension> PulseScheduleDensityControlValveFlowRateView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleDensityControlValve, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleDensityControlValve| {
                item.flow_rate.reference_name.clone()
            }),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleDensityControlValve| item.flow_rate.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleDensityControlValve| item.flow_rate.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over multiple PulseScheduleDensityControlValve with field accumulation
pub struct PulseScheduleDensityControlValveSliceView<'a, D> {
    pub name: Accumulator<'a, PulseScheduleDensityControlValve, STR_0D, D>,
    pub description: Accumulator<'a, PulseScheduleDensityControlValve, STR_0D, D>,
    pub flow_rate: PulseScheduleDensityControlValveFlowRateView<'a, D>,
    slice_elements: Elements<'a, PulseScheduleDensityControlValve, D>,
}

impl<'a, D: Dimension> PulseScheduleDensityControlValveSliceView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleDensityControlValve, D>) -> Self {
        Self {
            name: Accumulator::new(elements.clone(), |item: &PulseScheduleDensityControlValve| item.name.clone()),
            description: Accumulator::new(elements.clone(), |item: &PulseScheduleDensityControlValve| item.description.clone()),
            flow_rate: PulseScheduleDensityControlValveFlowRateView::new(elements.clone()),
            slice_elements: elements,
        }
    }

    /// Total number of elements, across every sliced level
    pub fn len(&self) -> usize {
        self.slice_elements.len()
    }

    pub fn is_empty(&self) -> bool {
        self.slice_elements.is_empty()
    }

    /// The length of each sliced level, in path order like the gathered arrays
    pub fn shape(&self) -> Vec<usize> {
        self.slice_elements.shape()
    }

    /// Every element, in path order: the first array of structures on the path varies slowest
    pub fn iter(&self) -> impl Iterator<Item = &'a PulseScheduleDensityControlValve> + '_ {
        self.slice_elements.iter()
    }

    /// The slice view over a range of `species` under every element, e.g. `.species(..)`.
    /// It adds one dimension, after those of the levels sliced above it and before the leaf's own.
    ///
    /// # Panics
    /// If `range` does not select the same number of elements under every element.
    pub fn species<R>(&self, range: R) -> GasMixtureConstantSliceView<'a, D::Larger>
    where
        R: SliceIndex<[GasMixtureConstant], Output = [GasMixtureConstant]> + Clone,
    {
        GasMixtureConstantSliceView::new(
            self.slice_elements
                .nest("species", |item: &PulseScheduleDensityControlValve| item.species.as_slice(), range),
        )
    }
}

// --- PulseScheduleDensityControlIon View Types ---

/// View over `n_i_volume_average` (PulseScheduleReferenceDensity) across multiple PulseScheduleDensityControlIon
pub struct PulseScheduleDensityControlIonNIVolumeAverageView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleDensityControlIon, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleDensityControlIon, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleDensityControlIon, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleDensityControlIon, D>,
}

impl<'a, D: Dimension> PulseScheduleDensityControlIonNIVolumeAverageView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleDensityControlIon, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleDensityControlIon| {
                item.n_i_volume_average.reference_name.clone()
            }),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleDensityControlIon| item.n_i_volume_average.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleDensityControlIon| item.n_i_volume_average.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over multiple PulseScheduleDensityControlIon with field accumulation
pub struct PulseScheduleDensityControlIonSliceView<'a, D> {
    pub z_ion: Accumulator<'a, PulseScheduleDensityControlIon, FLT_0D, D>,
    pub name: Accumulator<'a, PulseScheduleDensityControlIon, STR_0D, D>,
    pub n_i_volume_average: PulseScheduleDensityControlIonNIVolumeAverageView<'a, D>,
    slice_elements: Elements<'a, PulseScheduleDensityControlIon, D>,
}

impl<'a, D: Dimension> PulseScheduleDensityControlIonSliceView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleDensityControlIon, D>) -> Self {
        Self {
            z_ion: Accumulator::new(elements.clone(), |item: &PulseScheduleDensityControlIon| item.z_ion),
            name: Accumulator::new(elements.clone(), |item: &PulseScheduleDensityControlIon| item.name.clone()),
            n_i_volume_average: PulseScheduleDensityControlIonNIVolumeAverageView::new(elements.clone()),
            slice_elements: elements,
        }
    }

    /// Total number of elements, across every sliced level
    pub fn len(&self) -> usize {
        self.slice_elements.len()
    }

    pub fn is_empty(&self) -> bool {
        self.slice_elements.is_empty()
    }

    /// The length of each sliced level, in path order like the gathered arrays
    pub fn shape(&self) -> Vec<usize> {
        self.slice_elements.shape()
    }

    /// Every element, in path order: the first array of structures on the path varies slowest
    pub fn iter(&self) -> impl Iterator<Item = &'a PulseScheduleDensityControlIon> + '_ {
        self.slice_elements.iter()
    }

    /// The slice view over a range of `element` under every element, e.g. `.element(..)`.
    /// It adds one dimension, after those of the levels sliced above it and before the leaf's own.
    ///
    /// # Panics
    /// If `range` does not select the same number of elements under every element.
    pub fn element<R>(&self, range: R) -> PlasmaCompositionNeutralElementConstantSliceView<'a, D::Larger>
    where
        R: SliceIndex<[PlasmaCompositionNeutralElementConstant], Output = [PlasmaCompositionNeutralElementConstant]> + Clone,
    {
        PlasmaCompositionNeutralElementConstantSliceView::new(self.slice_elements.nest(
            "element",
            |item: &PulseScheduleDensityControlIon| item.element.as_slice(),
            range,
        ))
    }
}

// --- PulseSchedulePfActiveCoil View Types ---

/// View over `current` (PulseScheduleReferencePfActive) across multiple PulseSchedulePfActiveCoil
pub struct PulseSchedulePfActiveCoilCurrentView<'a, D> {
    pub reference_name: Accumulator<'a, PulseSchedulePfActiveCoil, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseSchedulePfActiveCoil, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseSchedulePfActiveCoil, INT_0D, D>,
    slice_elements: Elements<'a, PulseSchedulePfActiveCoil, D>,
}

impl<'a, D: Dimension> PulseSchedulePfActiveCoilCurrentView<'a, D> {
    pub fn new(elements: Elements<'a, PulseSchedulePfActiveCoil, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseSchedulePfActiveCoil| item.current.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseSchedulePfActiveCoil| item.current.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseSchedulePfActiveCoil| item.current.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over `resistance_additional` (PulseScheduleReferencePfActive) across multiple PulseSchedulePfActiveCoil
pub struct PulseSchedulePfActiveCoilResistanceAdditionalView<'a, D> {
    pub reference_name: Accumulator<'a, PulseSchedulePfActiveCoil, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseSchedulePfActiveCoil, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseSchedulePfActiveCoil, INT_0D, D>,
    slice_elements: Elements<'a, PulseSchedulePfActiveCoil, D>,
}

impl<'a, D: Dimension> PulseSchedulePfActiveCoilResistanceAdditionalView<'a, D> {
    pub fn new(elements: Elements<'a, PulseSchedulePfActiveCoil, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseSchedulePfActiveCoil| {
                item.resistance_additional.reference_name.clone()
            }),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseSchedulePfActiveCoil| item.resistance_additional.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseSchedulePfActiveCoil| item.resistance_additional.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over multiple PulseSchedulePfActiveCoil with field accumulation
pub struct PulseSchedulePfActiveCoilSliceView<'a, D> {
    pub name: Accumulator<'a, PulseSchedulePfActiveCoil, STR_0D, D>,
    pub description: Accumulator<'a, PulseSchedulePfActiveCoil, STR_0D, D>,
    pub current: PulseSchedulePfActiveCoilCurrentView<'a, D>,
    pub resistance_additional: PulseSchedulePfActiveCoilResistanceAdditionalView<'a, D>,
    slice_elements: Elements<'a, PulseSchedulePfActiveCoil, D>,
}

impl<'a, D: Dimension> PulseSchedulePfActiveCoilSliceView<'a, D> {
    pub fn new(elements: Elements<'a, PulseSchedulePfActiveCoil, D>) -> Self {
        Self {
            name: Accumulator::new(elements.clone(), |item: &PulseSchedulePfActiveCoil| item.name.clone()),
            description: Accumulator::new(elements.clone(), |item: &PulseSchedulePfActiveCoil| item.description.clone()),
            current: PulseSchedulePfActiveCoilCurrentView::new(elements.clone()),
            resistance_additional: PulseSchedulePfActiveCoilResistanceAdditionalView::new(elements.clone()),
            slice_elements: elements,
        }
    }

    /// Total number of elements, across every sliced level
    pub fn len(&self) -> usize {
        self.slice_elements.len()
    }

    pub fn is_empty(&self) -> bool {
        self.slice_elements.is_empty()
    }

    /// The length of each sliced level, in path order like the gathered arrays
    pub fn shape(&self) -> Vec<usize> {
        self.slice_elements.shape()
    }

    /// Every element, in path order: the first array of structures on the path varies slowest
    pub fn iter(&self) -> impl Iterator<Item = &'a PulseSchedulePfActiveCoil> + '_ {
        self.slice_elements.iter()
    }
}

// --- PulseSchedulePfActiveSupply View Types ---

/// View over `voltage` (PulseScheduleReferencePfActive) across multiple PulseSchedulePfActiveSupply
pub struct PulseSchedulePfActiveSupplyVoltageView<'a, D> {
    pub reference_name: Accumulator<'a, PulseSchedulePfActiveSupply, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseSchedulePfActiveSupply, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseSchedulePfActiveSupply, INT_0D, D>,
    slice_elements: Elements<'a, PulseSchedulePfActiveSupply, D>,
}

impl<'a, D: Dimension> PulseSchedulePfActiveSupplyVoltageView<'a, D> {
    pub fn new(elements: Elements<'a, PulseSchedulePfActiveSupply, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseSchedulePfActiveSupply| item.voltage.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseSchedulePfActiveSupply| item.voltage.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseSchedulePfActiveSupply| item.voltage.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over `current` (PulseScheduleReferencePfActive) across multiple PulseSchedulePfActiveSupply
pub struct PulseSchedulePfActiveSupplyCurrentView<'a, D> {
    pub reference_name: Accumulator<'a, PulseSchedulePfActiveSupply, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseSchedulePfActiveSupply, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseSchedulePfActiveSupply, INT_0D, D>,
    slice_elements: Elements<'a, PulseSchedulePfActiveSupply, D>,
}

impl<'a, D: Dimension> PulseSchedulePfActiveSupplyCurrentView<'a, D> {
    pub fn new(elements: Elements<'a, PulseSchedulePfActiveSupply, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseSchedulePfActiveSupply| item.current.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseSchedulePfActiveSupply| item.current.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseSchedulePfActiveSupply| item.current.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over multiple PulseSchedulePfActiveSupply with field accumulation
pub struct PulseSchedulePfActiveSupplySliceView<'a, D> {
    pub name: Accumulator<'a, PulseSchedulePfActiveSupply, STR_0D, D>,
    pub description: Accumulator<'a, PulseSchedulePfActiveSupply, STR_0D, D>,
    pub voltage: PulseSchedulePfActiveSupplyVoltageView<'a, D>,
    pub current: PulseSchedulePfActiveSupplyCurrentView<'a, D>,
    slice_elements: Elements<'a, PulseSchedulePfActiveSupply, D>,
}

impl<'a, D: Dimension> PulseSchedulePfActiveSupplySliceView<'a, D> {
    pub fn new(elements: Elements<'a, PulseSchedulePfActiveSupply, D>) -> Self {
        Self {
            name: Accumulator::new(elements.clone(), |item: &PulseSchedulePfActiveSupply| item.name.clone()),
            description: Accumulator::new(elements.clone(), |item: &PulseSchedulePfActiveSupply| item.description.clone()),
            voltage: PulseSchedulePfActiveSupplyVoltageView::new(elements.clone()),
            current: PulseSchedulePfActiveSupplyCurrentView::new(elements.clone()),
            slice_elements: elements,
        }
    }

    /// Total number of elements, across every sliced level
    pub fn len(&self) -> usize {
        self.slice_elements.len()
    }

    pub fn is_empty(&self) -> bool {
        self.slice_elements.is_empty()
    }

    /// The length of each sliced level, in path order like the gathered arrays
    pub fn shape(&self) -> Vec<usize> {
        self.slice_elements.shape()
    }

    /// Every element, in path order: the first array of structures on the path varies slowest
    pub fn iter(&self) -> impl Iterator<Item = &'a PulseSchedulePfActiveSupply> + '_ {
        self.slice_elements.iter()
    }
}

// --- PulseScheduleRz View Types ---

/// View over `r` (PulseScheduleReferencePosition) across multiple PulseScheduleRz
pub struct PulseScheduleRzRView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleRz, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleRz, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleRz, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleRz, D>,
}

impl<'a, D: Dimension> PulseScheduleRzRView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleRz, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleRz| item.r.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleRz| item.r.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleRz| item.r.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over `z` (PulseScheduleReferencePosition) across multiple PulseScheduleRz
pub struct PulseScheduleRzZView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleRz, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleRz, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleRz, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleRz, D>,
}

impl<'a, D: Dimension> PulseScheduleRzZView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleRz, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleRz| item.z.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleRz| item.z.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleRz| item.z.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over multiple PulseScheduleRz with field accumulation
pub struct PulseScheduleRzSliceView<'a, D> {
    pub r: PulseScheduleRzRView<'a, D>,
    pub z: PulseScheduleRzZView<'a, D>,
    slice_elements: Elements<'a, PulseScheduleRz, D>,
}

impl<'a, D: Dimension> PulseScheduleRzSliceView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleRz, D>) -> Self {
        Self {
            r: PulseScheduleRzRView::new(elements.clone()),
            z: PulseScheduleRzZView::new(elements.clone()),
            slice_elements: elements,
        }
    }

    /// Total number of elements, across every sliced level
    pub fn len(&self) -> usize {
        self.slice_elements.len()
    }

    pub fn is_empty(&self) -> bool {
        self.slice_elements.is_empty()
    }

    /// The length of each sliced level, in path order like the gathered arrays
    pub fn shape(&self) -> Vec<usize> {
        self.slice_elements.shape()
    }

    /// Every element, in path order: the first array of structures on the path varies slowest
    pub fn iter(&self) -> impl Iterator<Item = &'a PulseScheduleRz> + '_ {
        self.slice_elements.iter()
    }
}

// --- PulseScheduleOutline View Types ---

/// View over `r` (PulseScheduleReferencePosition) across multiple PulseScheduleOutline
pub struct PulseScheduleOutlineRView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleOutline, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleOutline, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleOutline, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleOutline, D>,
}

impl<'a, D: Dimension> PulseScheduleOutlineRView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleOutline, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleOutline| item.r.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleOutline| item.r.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleOutline| item.r.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over `z` (PulseScheduleReferencePosition) across multiple PulseScheduleOutline
pub struct PulseScheduleOutlineZView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleOutline, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleOutline, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleOutline, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleOutline, D>,
}

impl<'a, D: Dimension> PulseScheduleOutlineZView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleOutline, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleOutline| item.z.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleOutline| item.z.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleOutline| item.z.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over multiple PulseScheduleOutline with field accumulation
pub struct PulseScheduleOutlineSliceView<'a, D> {
    pub r: PulseScheduleOutlineRView<'a, D>,
    pub z: PulseScheduleOutlineZView<'a, D>,
    slice_elements: Elements<'a, PulseScheduleOutline, D>,
}

impl<'a, D: Dimension> PulseScheduleOutlineSliceView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleOutline, D>) -> Self {
        Self {
            r: PulseScheduleOutlineRView::new(elements.clone()),
            z: PulseScheduleOutlineZView::new(elements.clone()),
            slice_elements: elements,
        }
    }

    /// Total number of elements, across every sliced level
    pub fn len(&self) -> usize {
        self.slice_elements.len()
    }

    pub fn is_empty(&self) -> bool {
        self.slice_elements.is_empty()
    }

    /// The length of each sliced level, in path order like the gathered arrays
    pub fn shape(&self) -> Vec<usize> {
        self.slice_elements.shape()
    }

    /// Every element, in path order: the first array of structures on the path varies slowest
    pub fn iter(&self) -> impl Iterator<Item = &'a PulseScheduleOutline> + '_ {
        self.slice_elements.iter()
    }
}

// --- PulseScheduleGap View Types ---

/// View over `value` (PulseScheduleReferencePosition) across multiple PulseScheduleGap
pub struct PulseScheduleGapValueView<'a, D> {
    pub reference_name: Accumulator<'a, PulseScheduleGap, STR_0D, D>,
    pub reference_type: Accumulator<'a, PulseScheduleGap, INT_0D, D>,
    pub envelope_type: Accumulator<'a, PulseScheduleGap, INT_0D, D>,
    slice_elements: Elements<'a, PulseScheduleGap, D>,
}

impl<'a, D: Dimension> PulseScheduleGapValueView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleGap, D>) -> Self {
        Self {
            reference_name: Accumulator::new(elements.clone(), |item: &PulseScheduleGap| item.value.reference_name.clone()),
            reference_type: Accumulator::new(elements.clone(), |item: &PulseScheduleGap| item.value.reference_type),
            envelope_type: Accumulator::new(elements.clone(), |item: &PulseScheduleGap| item.value.envelope_type),
            slice_elements: elements,
        }
    }
}

/// View over multiple PulseScheduleGap with field accumulation
pub struct PulseScheduleGapSliceView<'a, D> {
    pub name: Accumulator<'a, PulseScheduleGap, STR_0D, D>,
    pub description: Accumulator<'a, PulseScheduleGap, STR_0D, D>,
    pub r: Accumulator<'a, PulseScheduleGap, FLT_0D, D>,
    pub z: Accumulator<'a, PulseScheduleGap, FLT_0D, D>,
    pub angle: Accumulator<'a, PulseScheduleGap, FLT_0D, D>,
    pub value: PulseScheduleGapValueView<'a, D>,
    slice_elements: Elements<'a, PulseScheduleGap, D>,
}

impl<'a, D: Dimension> PulseScheduleGapSliceView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleGap, D>) -> Self {
        Self {
            name: Accumulator::new(elements.clone(), |item: &PulseScheduleGap| item.name.clone()),
            description: Accumulator::new(elements.clone(), |item: &PulseScheduleGap| item.description.clone()),
            r: Accumulator::new(elements.clone(), |item: &PulseScheduleGap| item.r),
            z: Accumulator::new(elements.clone(), |item: &PulseScheduleGap| item.z),
            angle: Accumulator::new(elements.clone(), |item: &PulseScheduleGap| item.angle),
            value: PulseScheduleGapValueView::new(elements.clone()),
            slice_elements: elements,
        }
    }

    /// Total number of elements, across every sliced level
    pub fn len(&self) -> usize {
        self.slice_elements.len()
    }

    pub fn is_empty(&self) -> bool {
        self.slice_elements.is_empty()
    }

    /// The length of each sliced level, in path order like the gathered arrays
    pub fn shape(&self) -> Vec<usize> {
        self.slice_elements.shape()
    }

    /// Every element, in path order: the first array of structures on the path varies slowest
    pub fn iter(&self) -> impl Iterator<Item = &'a PulseScheduleGap> + '_ {
        self.slice_elements.iter()
    }
}

// --- Library View Types ---

/// View over multiple Library with field accumulation
pub struct LibrarySliceView<'a, D> {
    pub name: Accumulator<'a, Library, STR_0D, D>,
    pub description: Accumulator<'a, Library, STR_0D, D>,
    pub commit: Accumulator<'a, Library, STR_0D, D>,
    pub version: Accumulator<'a, Library, STR_0D, D>,
    pub repository: Accumulator<'a, Library, STR_0D, D>,
    pub parameters: Accumulator<'a, Library, STR_0D, D>,
    slice_elements: Elements<'a, Library, D>,
}

impl<'a, D: Dimension> LibrarySliceView<'a, D> {
    pub fn new(elements: Elements<'a, Library, D>) -> Self {
        Self {
            name: Accumulator::new(elements.clone(), |item: &Library| item.name.clone()),
            description: Accumulator::new(elements.clone(), |item: &Library| item.description.clone()),
            commit: Accumulator::new(elements.clone(), |item: &Library| item.commit.clone()),
            version: Accumulator::new(elements.clone(), |item: &Library| item.version.clone()),
            repository: Accumulator::new(elements.clone(), |item: &Library| item.repository.clone()),
            parameters: Accumulator::new(elements.clone(), |item: &Library| item.parameters.clone()),
            slice_elements: elements,
        }
    }

    /// Total number of elements, across every sliced level
    pub fn len(&self) -> usize {
        self.slice_elements.len()
    }

    pub fn is_empty(&self) -> bool {
        self.slice_elements.is_empty()
    }

    /// The length of each sliced level, in path order like the gathered arrays
    pub fn shape(&self) -> Vec<usize> {
        self.slice_elements.shape()
    }

    /// Every element, in path order: the first array of structures on the path varies slowest
    pub fn iter(&self) -> impl Iterator<Item = &'a Library> + '_ {
        self.slice_elements.iter()
    }
}

// --- PulseScheduleEvent View Types ---

/// View over `type` (Identifier) across multiple PulseScheduleEvent
pub struct PulseScheduleEventTypeView<'a, D> {
    pub name: Accumulator<'a, PulseScheduleEvent, STR_0D, D>,
    pub index: Accumulator<'a, PulseScheduleEvent, INT_0D, D>,
    pub description: Accumulator<'a, PulseScheduleEvent, STR_0D, D>,
    slice_elements: Elements<'a, PulseScheduleEvent, D>,
}

impl<'a, D: Dimension> PulseScheduleEventTypeView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleEvent, D>) -> Self {
        Self {
            name: Accumulator::new(elements.clone(), |item: &PulseScheduleEvent| item.r#type.name.clone()),
            index: Accumulator::new(elements.clone(), |item: &PulseScheduleEvent| item.r#type.index),
            description: Accumulator::new(elements.clone(), |item: &PulseScheduleEvent| item.r#type.description.clone()),
            slice_elements: elements,
        }
    }
}

/// View over `acquisition_strategy` (Identifier) across multiple PulseScheduleEvent
pub struct PulseScheduleEventAcquisitionStrategyView<'a, D> {
    pub name: Accumulator<'a, PulseScheduleEvent, STR_0D, D>,
    pub index: Accumulator<'a, PulseScheduleEvent, INT_0D, D>,
    pub description: Accumulator<'a, PulseScheduleEvent, STR_0D, D>,
    slice_elements: Elements<'a, PulseScheduleEvent, D>,
}

impl<'a, D: Dimension> PulseScheduleEventAcquisitionStrategyView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleEvent, D>) -> Self {
        Self {
            name: Accumulator::new(elements.clone(), |item: &PulseScheduleEvent| item.acquisition_strategy.name.clone()),
            index: Accumulator::new(elements.clone(), |item: &PulseScheduleEvent| item.acquisition_strategy.index),
            description: Accumulator::new(elements.clone(), |item: &PulseScheduleEvent| item.acquisition_strategy.description.clone()),
            slice_elements: elements,
        }
    }
}

/// View over `acquisition_state` (Identifier) across multiple PulseScheduleEvent
pub struct PulseScheduleEventAcquisitionStateView<'a, D> {
    pub name: Accumulator<'a, PulseScheduleEvent, STR_0D, D>,
    pub index: Accumulator<'a, PulseScheduleEvent, INT_0D, D>,
    pub description: Accumulator<'a, PulseScheduleEvent, STR_0D, D>,
    slice_elements: Elements<'a, PulseScheduleEvent, D>,
}

impl<'a, D: Dimension> PulseScheduleEventAcquisitionStateView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleEvent, D>) -> Self {
        Self {
            name: Accumulator::new(elements.clone(), |item: &PulseScheduleEvent| item.acquisition_state.name.clone()),
            index: Accumulator::new(elements.clone(), |item: &PulseScheduleEvent| item.acquisition_state.index),
            description: Accumulator::new(elements.clone(), |item: &PulseScheduleEvent| item.acquisition_state.description.clone()),
            slice_elements: elements,
        }
    }
}

/// View over multiple PulseScheduleEvent with field accumulation
pub struct PulseScheduleEventSliceView<'a, D> {
    pub r#type: PulseScheduleEventTypeView<'a, D>,
    pub identifier: Accumulator<'a, PulseScheduleEvent, STR_0D, D>,
    pub time_stamp: Accumulator<'a, PulseScheduleEvent, FLT_0D, D>,
    pub duration: Accumulator<'a, PulseScheduleEvent, FLT_0D, D>,
    pub acquisition_strategy: PulseScheduleEventAcquisitionStrategyView<'a, D>,
    pub acquisition_state: PulseScheduleEventAcquisitionStateView<'a, D>,
    pub provider: Accumulator<'a, PulseScheduleEvent, STR_0D, D>,
    slice_elements: Elements<'a, PulseScheduleEvent, D>,
}

impl<'a, D: Dimension> PulseScheduleEventSliceView<'a, D> {
    pub fn new(elements: Elements<'a, PulseScheduleEvent, D>) -> Self {
        Self {
            r#type: PulseScheduleEventTypeView::new(elements.clone()),
            identifier: Accumulator::new(elements.clone(), |item: &PulseScheduleEvent| item.identifier.clone()),
            time_stamp: Accumulator::new(elements.clone(), |item: &PulseScheduleEvent| item.time_stamp),
            duration: Accumulator::new(elements.clone(), |item: &PulseScheduleEvent| item.duration),
            acquisition_strategy: PulseScheduleEventAcquisitionStrategyView::new(elements.clone()),
            acquisition_state: PulseScheduleEventAcquisitionStateView::new(elements.clone()),
            provider: Accumulator::new(elements.clone(), |item: &PulseScheduleEvent| item.provider.clone()),
            slice_elements: elements,
        }
    }

    /// Total number of elements, across every sliced level
    pub fn len(&self) -> usize {
        self.slice_elements.len()
    }

    pub fn is_empty(&self) -> bool {
        self.slice_elements.is_empty()
    }

    /// The length of each sliced level, in path order like the gathered arrays
    pub fn shape(&self) -> Vec<usize> {
        self.slice_elements.shape()
    }

    /// Every element, in path order: the first array of structures on the path varies slowest
    pub fn iter(&self) -> impl Iterator<Item = &'a PulseScheduleEvent> + '_ {
        self.slice_elements.iter()
    }
}

// ============================================================================
// Struct Impl Blocks for Vec Field Access
// ============================================================================

impl PulseScheduleNbiUnit {
    /// The slice view over a range of species, e.g. `.species(0..2)` or `.species(..)`,
    /// whose leaves gather one value per element. A single element is `.species[i]`.
    pub fn species<R>(&self, range: R) -> GasMixtureConstantSliceView<'_, Ix1>
    where
        R: SliceIndex<[GasMixtureConstant], Output = [GasMixtureConstant]>,
    {
        GasMixtureConstantSliceView::new(Elements::from_slice(&self.species[range]))
    }

    /// Get the number of species elements
    pub fn species_len(&self) -> usize {
        self.species.len()
    }
}

impl PulseScheduleNbi {
    /// The slice view over a range of unit, e.g. `.unit(0..2)` or `.unit(..)`,
    /// whose leaves gather one value per element. A single element is `.unit[i]`.
    pub fn unit<R>(&self, range: R) -> PulseScheduleNbiUnitSliceView<'_, Ix1>
    where
        R: SliceIndex<[PulseScheduleNbiUnit], Output = [PulseScheduleNbiUnit]>,
    {
        PulseScheduleNbiUnitSliceView::new(Elements::from_slice(&self.unit[range]))
    }

    /// Get the number of unit elements
    pub fn unit_len(&self) -> usize {
        self.unit.len()
    }
}

impl PulseScheduleIc {
    /// The slice view over a range of antenna, e.g. `.antenna(0..2)` or `.antenna(..)`,
    /// whose leaves gather one value per element. A single element is `.antenna[i]`.
    pub fn antenna<R>(&self, range: R) -> PulseScheduleIcAntennaSliceView<'_, Ix1>
    where
        R: SliceIndex<[PulseScheduleIcAntenna], Output = [PulseScheduleIcAntenna]>,
    {
        PulseScheduleIcAntennaSliceView::new(Elements::from_slice(&self.antenna[range]))
    }

    /// Get the number of antenna elements
    pub fn antenna_len(&self) -> usize {
        self.antenna.len()
    }
}

impl PulseScheduleLh {
    /// The slice view over a range of antenna, e.g. `.antenna(0..2)` or `.antenna(..)`,
    /// whose leaves gather one value per element. A single element is `.antenna[i]`.
    pub fn antenna<R>(&self, range: R) -> PulseScheduleLhAntennaSliceView<'_, Ix1>
    where
        R: SliceIndex<[PulseScheduleLhAntenna], Output = [PulseScheduleLhAntenna]>,
    {
        PulseScheduleLhAntennaSliceView::new(Elements::from_slice(&self.antenna[range]))
    }

    /// Get the number of antenna elements
    pub fn antenna_len(&self) -> usize {
        self.antenna.len()
    }
}

impl PulseScheduleEc {
    /// The slice view over a range of beam, e.g. `.beam(0..2)` or `.beam(..)`,
    /// whose leaves gather one value per element. A single element is `.beam[i]`.
    pub fn beam<R>(&self, range: R) -> PulseScheduleEcBeamSliceView<'_, Ix1>
    where
        R: SliceIndex<[PulseScheduleEcBeam], Output = [PulseScheduleEcBeam]>,
    {
        PulseScheduleEcBeamSliceView::new(Elements::from_slice(&self.beam[range]))
    }

    /// Get the number of beam elements
    pub fn beam_len(&self) -> usize {
        self.beam.len()
    }
}

impl PulseScheduleDensityControlIon {
    /// The slice view over a range of element, e.g. `.element(0..2)` or `.element(..)`,
    /// whose leaves gather one value per element. A single element is `.element[i]`.
    pub fn element<R>(&self, range: R) -> PlasmaCompositionNeutralElementConstantSliceView<'_, Ix1>
    where
        R: SliceIndex<[PlasmaCompositionNeutralElementConstant], Output = [PlasmaCompositionNeutralElementConstant]>,
    {
        PlasmaCompositionNeutralElementConstantSliceView::new(Elements::from_slice(&self.element[range]))
    }

    /// Get the number of element elements
    pub fn element_len(&self) -> usize {
        self.element.len()
    }
}

impl PulseScheduleDensityControlValve {
    /// The slice view over a range of species, e.g. `.species(0..2)` or `.species(..)`,
    /// whose leaves gather one value per element. A single element is `.species[i]`.
    pub fn species<R>(&self, range: R) -> GasMixtureConstantSliceView<'_, Ix1>
    where
        R: SliceIndex<[GasMixtureConstant], Output = [GasMixtureConstant]>,
    {
        GasMixtureConstantSliceView::new(Elements::from_slice(&self.species[range]))
    }

    /// Get the number of species elements
    pub fn species_len(&self) -> usize {
        self.species.len()
    }
}

impl PulseScheduleDensityControl {
    /// The slice view over a range of valve, e.g. `.valve(0..2)` or `.valve(..)`,
    /// whose leaves gather one value per element. A single element is `.valve[i]`.
    pub fn valve<R>(&self, range: R) -> PulseScheduleDensityControlValveSliceView<'_, Ix1>
    where
        R: SliceIndex<[PulseScheduleDensityControlValve], Output = [PulseScheduleDensityControlValve]>,
    {
        PulseScheduleDensityControlValveSliceView::new(Elements::from_slice(&self.valve[range]))
    }

    /// Get the number of valve elements
    pub fn valve_len(&self) -> usize {
        self.valve.len()
    }
}

impl PulseScheduleDensityControl {
    /// The slice view over a range of ion, e.g. `.ion(0..2)` or `.ion(..)`,
    /// whose leaves gather one value per element. A single element is `.ion[i]`.
    pub fn ion<R>(&self, range: R) -> PulseScheduleDensityControlIonSliceView<'_, Ix1>
    where
        R: SliceIndex<[PulseScheduleDensityControlIon], Output = [PulseScheduleDensityControlIon]>,
    {
        PulseScheduleDensityControlIonSliceView::new(Elements::from_slice(&self.ion[range]))
    }

    /// Get the number of ion elements
    pub fn ion_len(&self) -> usize {
        self.ion.len()
    }
}

impl PulseSchedulePfActive {
    /// The slice view over a range of coil, e.g. `.coil(0..2)` or `.coil(..)`,
    /// whose leaves gather one value per element. A single element is `.coil[i]`.
    pub fn coil<R>(&self, range: R) -> PulseSchedulePfActiveCoilSliceView<'_, Ix1>
    where
        R: SliceIndex<[PulseSchedulePfActiveCoil], Output = [PulseSchedulePfActiveCoil]>,
    {
        PulseSchedulePfActiveCoilSliceView::new(Elements::from_slice(&self.coil[range]))
    }

    /// Get the number of coil elements
    pub fn coil_len(&self) -> usize {
        self.coil.len()
    }
}

impl PulseSchedulePfActive {
    /// The slice view over a range of supply, e.g. `.supply(0..2)` or `.supply(..)`,
    /// whose leaves gather one value per element. A single element is `.supply[i]`.
    pub fn supply<R>(&self, range: R) -> PulseSchedulePfActiveSupplySliceView<'_, Ix1>
    where
        R: SliceIndex<[PulseSchedulePfActiveSupply], Output = [PulseSchedulePfActiveSupply]>,
    {
        PulseSchedulePfActiveSupplySliceView::new(Elements::from_slice(&self.supply[range]))
    }

    /// Get the number of supply elements
    pub fn supply_len(&self) -> usize {
        self.supply.len()
    }
}

impl PulseSchedulePosition {
    /// The slice view over a range of x_point, e.g. `.x_point(0..2)` or `.x_point(..)`,
    /// whose leaves gather one value per element. A single element is `.x_point[i]`.
    pub fn x_point<R>(&self, range: R) -> PulseScheduleRzSliceView<'_, Ix1>
    where
        R: SliceIndex<[PulseScheduleRz], Output = [PulseScheduleRz]>,
    {
        PulseScheduleRzSliceView::new(Elements::from_slice(&self.x_point[range]))
    }

    /// Get the number of x_point elements
    pub fn x_point_len(&self) -> usize {
        self.x_point.len()
    }
}

impl PulseSchedulePosition {
    /// The slice view over a range of strike_point, e.g. `.strike_point(0..2)` or `.strike_point(..)`,
    /// whose leaves gather one value per element. A single element is `.strike_point[i]`.
    pub fn strike_point<R>(&self, range: R) -> PulseScheduleRzSliceView<'_, Ix1>
    where
        R: SliceIndex<[PulseScheduleRz], Output = [PulseScheduleRz]>,
    {
        PulseScheduleRzSliceView::new(Elements::from_slice(&self.strike_point[range]))
    }

    /// Get the number of strike_point elements
    pub fn strike_point_len(&self) -> usize {
        self.strike_point.len()
    }
}

impl PulseSchedulePosition {
    /// The slice view over a range of boundary_outline, e.g. `.boundary_outline(0..2)` or `.boundary_outline(..)`,
    /// whose leaves gather one value per element. A single element is `.boundary_outline[i]`.
    pub fn boundary_outline<R>(&self, range: R) -> PulseScheduleOutlineSliceView<'_, Ix1>
    where
        R: SliceIndex<[PulseScheduleOutline], Output = [PulseScheduleOutline]>,
    {
        PulseScheduleOutlineSliceView::new(Elements::from_slice(&self.boundary_outline[range]))
    }

    /// Get the number of boundary_outline elements
    pub fn boundary_outline_len(&self) -> usize {
        self.boundary_outline.len()
    }
}

impl PulseSchedulePosition {
    /// The slice view over a range of gap, e.g. `.gap(0..2)` or `.gap(..)`,
    /// whose leaves gather one value per element. A single element is `.gap[i]`.
    pub fn gap<R>(&self, range: R) -> PulseScheduleGapSliceView<'_, Ix1>
    where
        R: SliceIndex<[PulseScheduleGap], Output = [PulseScheduleGap]>,
    {
        PulseScheduleGapSliceView::new(Elements::from_slice(&self.gap[range]))
    }

    /// Get the number of gap elements
    pub fn gap_len(&self) -> usize {
        self.gap.len()
    }
}

impl GasMixtureConstant {
    /// The slice view over a range of element, e.g. `.element(0..2)` or `.element(..)`,
    /// whose leaves gather one value per element. A single element is `.element[i]`.
    pub fn element<R>(&self, range: R) -> PlasmaCompositionNeutralElementConstantSliceView<'_, Ix1>
    where
        R: SliceIndex<[PlasmaCompositionNeutralElementConstant], Output = [PlasmaCompositionNeutralElementConstant]>,
    {
        PlasmaCompositionNeutralElementConstantSliceView::new(Elements::from_slice(&self.element[range]))
    }

    /// Get the number of element elements
    pub fn element_len(&self) -> usize {
        self.element.len()
    }
}

impl Code {
    /// The slice view over a range of library, e.g. `.library(0..2)` or `.library(..)`,
    /// whose leaves gather one value per element. A single element is `.library[i]`.
    pub fn library<R>(&self, range: R) -> LibrarySliceView<'_, Ix1>
    where
        R: SliceIndex<[Library], Output = [Library]>,
    {
        LibrarySliceView::new(Elements::from_slice(&self.library[range]))
    }

    /// Get the number of library elements
    pub fn library_len(&self) -> usize {
        self.library.len()
    }
}

impl PulseSchedule {
    /// The slice view over a range of event, e.g. `.event(0..2)` or `.event(..)`,
    /// whose leaves gather one value per element. A single element is `.event[i]`.
    pub fn event<R>(&self, range: R) -> PulseScheduleEventSliceView<'_, Ix1>
    where
        R: SliceIndex<[PulseScheduleEvent], Output = [PulseScheduleEvent]>,
    {
        PulseScheduleEventSliceView::new(Elements::from_slice(&self.event[range]))
    }

    /// Get the number of event elements
    pub fn event_len(&self) -> usize {
        self.event.len()
    }
}
